# Cluster 7 verification: Candidate/Employee Nurture & Portals (EPIC-P1, EPIC-P2, EPIC-P3)

Verified directly against code in:
- Backend: `C:\Users\AvinashMukund\Documents\Claude\OnboardingModule-Backend`
- Frontend: `C:\Users\AvinashMukund\Documents\Claude\OnboardingModule-Frontend-main`

Sheet's Status column was not trusted; every row below is a direct code read. Two rows (S-102, S-105) were marked "Done" on the sheet but are **PARTIAL** in real code -- flagged explicitly.

## EPIC-P1 -- Candidate Portal & Universal Identity Engine (sheet: 0/15)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-081 | HRMS-P101 | Universal Candidate Identity Engine (dedup+merge on email/phone/LinkedIn) | Planned | **PARTIAL** | `app/services/candidate_service.py:47-131` (`find_duplicate_candidate`, `create_candidate_safe`) | Real exact-match dedup on email, phone, LinkedIn URL (checked independently, in that order) gates every candidate-creation call site (R-07). But it only **refuses** creation of a duplicate (`DuplicateCandidateError`) -- there is no merge behavior, no `MergeEvent`/audit log ("log merges with source" from the story is not built), and no admin-facing merge/split UI. "One person = one record" is enforced at creation time only. |
| S-082 | HRMS-P102 | LinkedIn Entry -- Skeleton Profile Creation | Planned | **NOT-DONE** | `app/services/linkedin_sourcing_service.py` (whole file); grep for "skeleton" returns 0 hits in `app/` | The only LinkedIn-related candidate flow in this codebase is HRMS-1103's RM-initiated sourcing agent: it searches LinkedIn *for a specific open demand*, stages results in `staged_candidates`, and requires an explicit human "Promote" action (`promote_staged_candidate`) before a real `Candidate` row is created. This is a different mechanism entirely from S-082's spec: candidate-initiated inbound LinkedIn contact auto-creating a skeleton profile (name+LinkedIn ID+headline+location+continent), auto-assigning an AI recruiter, and having the AI immediately request email/phone. No such inbound skeleton-creation path exists. |
| S-083 | HRMS-P103 | Cross-Channel Conversation Merge | Planned | **CONFIRMED-DONE** | `app/services/ai_conversation_service.py:380-393` (one `CandidateConversation` opened per candidate at AI-assignment, not per channel); `app/services/candidate_portal_service.py:94-109` (`_map_thread_message` reads `channel` from each event's `event_data`) | A candidate has exactly one active conversation regardless of channel; every inbound/outbound message (WhatsApp, email, portal) is logged as a `ConversationEvent` on that same conversation with its own `channel` tag, and `get_conversation_thread()` / `build_candidate_context()` render/consume it as one merged, chronological timeline. `candidate_memory` (facts) is shared across the whole candidate, not per-channel. |
| S-084 | HRMS-P104 | Session Resume (buildCandidateContext) | UPDATE -- Ready for Build | **CONFIRMED-DONE** | `app/services/candidate_context_service.py:107-184` (`build_candidate_context`) | Real, tenant-scoped, cached (30s TTL) context builder assembling candidate summary, shared memory, cross-channel recent messages, missing fields, next question, and job context. Heavily wired: called from `thunder_service.py` (multiple sites), `detect_intent_service.py`, `sentiment_analysis_service.py`, `objection_handling_service.py`, `escalation_detection_service.py` -- always before any reply is generated. 20+ dedicated tests in `tests/test_candidate_context_service.py` and `tests/test_thunder_conversation_core.py`. |
| S-085 | HRMS-P105 | Candidate Portal -- Home Dashboard | Planned | **PARTIAL** | `app/services/candidate_portal_service.py:128-156` (`get_portal_home`); `app/schemas/candidate_portal.py:24-29` (`PortalHomeResponse`); `src/screens/CandidatePortalScreen.js:136-188` (`HomeTab`) | Real `/portal/home` route + UI at `/candidate/{token}` (JWT-based, mobile-first). Cards for stage, pending actions, recent messages all real. **Missing:** no "offer status" card/field anywhere in `PortalHomeResponse` or `HomeTab` -- the story's fourth required card doesn't exist. |
| S-086 | HRMS-P106 | Candidate Portal -- Message Thread View | Planned | **PARTIAL** | `src/screens/CandidatePortalScreen.js:190-278` (`MessagesTab`); `app/api/v1/endpoints/candidate_portal.py:65-70` (`GET /portal/messages`) | Full cross-channel history + inline reply (`sendPortalReply`) are real. **Missing:** no file-upload control in the message composer (only a text `<textarea>`); no channel icon rendered per message in the UI despite the API returning a `channel` field per message (`PortalThreadMessage.channel` is fetched but never displayed); "AI recruiter name + avatar" is just the static "Thunder" brand header, not a per-message avatar. |
| S-087 | HRMS-P107 | Candidate Portal -- Profile Completion Wizard | Planned | **CONFIRMED-DONE** | `app/api/v1/endpoints/candidate_portal.py:81-96` (`PATCH /portal/profile` -> `merge_fields_to_db`); `src/screens/CandidatePortalScreen.js:287-368` (`ProfileTab`) | Self-service completion of skills/experience/salary/notice etc. via `get_missing_fields()` (the same missing-field detector S-011/014/016 already standardized on), with a real completeness bar and per-field auto-clearing from `missing_fields` on save. Resume itself is a file, not a text field, and is handled by the separate document-upload surface (see S-090), not this text-field wizard -- consistent with the rest of the form being text-only. |
| S-088 | HRMS-P108 | Candidate Portal -- Interview Tracker | Planned | **CONFIRMED-DONE** | `app/services/candidate_portal_service.py:173-266` (`get_portal_interviews`, `build_ics`, `create_reschedule_request`); `src/screens/CandidatePortalScreen.js:370-449` (`InterviewsTab`) | Upcoming interviews with local-browser-rendered date/time, format (video vs onsite, derived from `meeting_link`), a static prep tip, a real "Add to Calendar" ICS download, and a working reschedule-request flow that logs a `ConversationEvent` and flips `escalation_state` to "pending" for human follow-up. |
| S-089 | HRMS-P109 | Candidate Portal -- Offer Viewer | Planned | **PARTIAL** | `src/screens/CandidatePortalScreen.js` (no offer tab/route at all); contrast with `src/screens/CandidateSelfService.js:43-45,139-424` (`getMyOffers`, `respondToOffer`, `signOfferLetter`) | A real, full offer-viewing/accept/decline/e-sign flow **exists**, but it lives entirely in `CandidateSelfService.js` -- the separate, password-login candidate experience gated behind `hrms_token` (`src/routes/Approutes.jsx:277-283`) -- not in `CandidatePortalScreen.js`, the magic-link `/candidate/{token}` surface this story (and S-085/086/087/088) is about. A candidate who reaches the portal via the WhatsApp/email magic link (per S-091/S-094) cannot view or act on their offer there; the offer-release WhatsApp/email message links to the generic portal home, not an offer view. No "AI FAQ bot inline" was found wired into either surface. |
| S-090 | HRMS-P110 | Candidate Portal -- Document Upload | Planned | **PARTIAL** | `app/api/v1/endpoints/documents.py:156-235` (candidate-authenticated upload routes for PAN/Aadhar/education/experience/salary-slip/bank-statement/UAN-PF + `/my-documents`); `src/screens/CandidateSelfService.js:48-56,101-119` (`DocumentUploadRow`, upload wiring) | Same pattern as S-089: real, working document upload with per-type status exists, candidate-authenticated (`get_current_candidate`), but only reachable through `CandidateSelfService.js`'s password-login flow, not through the magic-link Candidate Portal (`CandidatePortalScreen.js` has no Documents tab). |
| S-091 | HRMS-P111 | Magic Link Authentication | Planned | **PARTIAL** | `app/services/candidate_portal_service.py:55-78` (`generate_portal_link_token/url`, 14-day expiry); `app/services/offer_release_notification_service.py:86-115` (sends the link via both WhatsApp and email on offer release); `app/api/v1/endpoints/ai_agent.py:226-245` (recruiter-triggered manual link generation) | The "magic link" is real -- a long-lived candidate JWT embedded in a URL, validated by the existing `get_current_candidate` dependency, no password. It is genuinely sent via both WhatsApp and email, but only automatically on offer release; the general case is a recruiter manually pulling the link from an admin endpoint, not the AI proactively issuing/re-issuing it on every relevant touchpoint. No revoke-on-rejection: rejecting an offer does not invalidate the JWT (it stays valid until its 14-day expiry). No dedicated "generate/validate" table exists (deliberate architecture choice, documented in the module), so there's nothing to revoke server-side even if the code wanted to. |
| S-092 | HRMS-P112 | Duplicate Detection Audit Dashboard | Planned | **NOT-DONE** | grep for "audit dashboard / merge history / flagged duplicate / manual merge / split record" across `app/` and frontend `src/`: 0 hits | No admin-facing view of merge events, flagged duplicates, or a manual merge/split UI exists anywhere. Consistent with S-081 also having no merge-event log to display. |
| S-093 | HRMS-P113 | Candidate Source Attribution Tracking | Planned | **NOT-DONE** | `app/models/candidate.py` has a single `candidateSource` string field set once at creation (e.g. `"linkedin_sourcing_agent"` in `linkedin_sourcing_service.py:246`); no attribution-chain, multi-touch tracking, or ROI analytics model/service exists | Source is recorded but never tracked as a chain (LinkedIn→WhatsApp→Portal) and there is no admin attribution view or ROI analytics anywhere in the codebase. |
| S-094 | HRMS-P114 | WhatsApp Deep Link to Portal | Planned | **PARTIAL** | `app/services/offer_release_notification_service.py:76-104` (`_build_whatsapp_message` embeds `portal_link`) | The one-tap, pre-authenticated portal link is real -- but only inside the offer-release WhatsApp message. Grepping `thunder_service.py`'s general WhatsApp send path (`send_thunder_message` and friends) shows no portal-link injection into ordinary AI conversational messages, so "every AI WhatsApp message includes one-tap portal link" is false as a general claim; it's one specific message type. |
| S-095 | HRMS-P115 | Candidate Portal Mobile Optimization | Planned | **CONFIRMED-DONE** | `src/screens/CandidatePortalScreen.js:1-16` (module docstring: "Mobile-first (BR-03): single column, large touch targets, built for a 375px-wide screen"); 44px min-height touch targets throughout (`min-h-[44px]` on every interactive element) | Real mobile-first single-column layout with bottom tab nav, large touch targets, and a `max-w-xl` content column that degrades gracefully on wider screens. "iOS+Android tested" (a QA claim) cannot be verified from code, but the responsive implementation itself is real and complete for the four tabs that exist (Home/Messages/Profile/Interviews). |

## EPIC-P2 -- Employee Portal: Admin & Technical Split (sheet: 2/10 = 20%)

**Note:** none of these 10 stories are about timesheets or the employee directory. `MyTimesheetScreen.js` (built 2026-08-04, real self-service timesheet at `/my-timesheet`) and `EmployeeDirectoryScreen.js` do not map to any story in this list -- they satisfy stories in a different backlog cluster (employee self-service), not EPIC-P2's admin/technical portal-view stories.

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-096 | HRMS-P201 | Admin Portal -- Recruiting Dashboard | Planned | **PARTIAL** | `src/screens/Dashboard.js:29-75` | Real generic dashboard exists (Open Jobs / Candidates in Pipeline / Interviews Scheduled stat cards + `InterventionQueueWidget` + quick actions), but it is not the role-specific recruiting dashboard the story describes: no pipeline-by-stage breakdown, no "AI activity today" feed, no open-demand count, no dedicated SLA-alert widget. |
| S-097 | HRMS-P202 | Admin Portal -- HR Dashboard | Planned | **NOT-DONE** | Grep for "HRDashboard/hr_dashboard" and for "onboarding pipeline / document collection status / joining readiness / compliance checklist" across both repos: no dedicated screen or endpoint found | The underlying data (onboarding pipeline, document collection, offer status) exists piecemeal in other screens (`PreOnboarding.js`, `documents.py`), but no consolidated HR Dashboard view was found. |
| S-098 | HRMS-P203 | Admin Portal -- Resource Management Dashboard | Planned | **PARTIAL** | `src/screens/ResourceManagementScreen.js` (bench-scan recommendation queue, HRMS-1105/S-320); `src/screens/UtilizationDashboardScreen.js` (S-254/S-255 utilization + bench cost); `src/screens/AllocationsScreen.js` (allocation records) | Real bench pool, bench aging, utilization, and allocation data all exist -- but as three/four separate screens built for different, earlier stories, not one consolidated RM dashboard with "allocation map," "utilization heatmap," "demand vs supply," or "NO MARKET violation alerts" as the story specifies. No "NO MARKET" violation concept was found in RM context at all (a same-named term exists in `submission_service.py` for a different, submission-eligibility purpose). |
| S-099 | HRMS-P204 | Admin Portal -- Client Services Dashboard | Planned | **NOT-DONE** | Grep for "ClientServicesDashboard / client health / delivery milestone / staffing gap / escalation flag / RFP queue" across both repos: no matches | `ClientManagementScreen.js` exists for basic client CRUD but has none of the dashboard elements (health score, milestones, staffing gaps, escalation flags, RFP queue) the story specifies. |
| S-100 | HRMS-P205 | Technical Portal -- Interview Queue | Planned | **PARTIAL** | `src/screens/MyWorkspace.js:96-156` (`getMyInterviews`, pending-feedback tracking); `app/api/v1/endpoints/interviews.py:1561-1569` (`/my-interviews`) | A real interviewer home exists with candidate summary cards, a feedback-submitted/pending status per interview, and a link into the feedback form (`FeedbackTab.js`). Missing: no explicit "today" vs "this week" grouping -- interviews are shown as a flat list, not date-bucketed as the story specifies. |
| S-101 | HRMS-P206 | Technical Portal -- Interview Feedback Form | Planned | **CONFIRMED-DONE** (adapted) | `app/api/v1/endpoints/interviews.py:2045-2076` (score submission + recommendation validation); `src/screens/tabs/FeedbackTab.js:16-23` (recommendation options) | Real structured feedback form (technical/communication/problem-solving/culture-fit scores + comments), submitted to `offer_decision_service`. Vocabulary is adapted from the spec: scores are 1-10 per category (not "1-5 per skill" broken out individually) and recommendation is `No Hire/Not sure/Average/Hire/Must Hire` (not the spec's `Pass/Fail/Strong Pass/Strong Fail`) -- a pre-existing, already-shipped legacy scale reused rather than replaced, functionally equivalent. |
| S-102 | HRMS-P207 | Technical Portal -- Hiring Manager Decision View | **Done** | **PARTIAL -- sheet is wrong** | `src/screens/HmCandidateReviewScreen.js:1-14,100-120` (screen's own subtitle: "Approve/Reject/Hold happens on the candidate's own status action, not here"); `app/services/interview_sequencing_service.py:105-118`; `app/api/v1/endpoints/interviews.py:2614-2643` | Real, IDOR-fixed (2026-08-05) read-only view of candidate profile + all interview feedback by round, with a per-round Hire/Mixed/No-Feedback rollup. But it is explicitly **not** the decision-making screen the story describes: no Approve/Reject/Hold action on this screen at all (deferred to a separate, pre-existing `PUT /status/{candidate_id}` endpoint), no panel-split flags, no AI recommendation field, no salary-vs-budget comparison, and the code comments explicitly state the story's core business rule -- "HM must view all feedback before approving" -- is **not enforced anywhere**, neither here nor at the approval endpoint. The sheet's "Done" status is a real, material overstatement. |
| S-103 | HRMS-P208 | Technical Portal -- BU Head Oversight View | Planned | **NOT-DONE** | Grep for "BuHeadOversight/bu_head_oversight" and for "override requests pending / workforce plan vs actuals / headcount budget" across both repos: no matches | No BU Head-specific oversight screen or endpoint exists. |
| S-104 | HRMS-P209 | Org Role Mapping to Portal View | Planned | **PARTIAL** | `app/core/bu_scope.py`, `app/core/revenue_visibility_scope.py`, `app/services/bu_context_service.py` (real BU-scoped data-access RBAC built 2026-08-05 for EPIC-02/03) | Real role/BU-based *data visibility* scoping exists (who can see which BU's revenue/clients), but that is a data-access filter, not the story's "map user's role to a distinct **portal view**" concept (CEO/BU Head→full-org dashboard, Director→BU-scoped dashboard, Tech Manager→team dashboard, Tech Lead→personal-queue-only dashboard). Since none of the role-tiered dashboards this would route between (S-096-S-099, S-103) fully exist yet, there's no view-routing layer to build on top of them either. |
| S-105 | HRMS-P210 | Portal Notification Center | **Done** | **PARTIAL -- sheet overstates** | `app/api/v1/endpoints/notifications.py` (`GET /notifications`, `POST /notifications/{id}/mark-read`); `src/layout/TopBar.js:38-74,306-359` | Real unified in-app notification feed with unread count and mark-as-read, polling every 60s, wired to the real `notification_service` dispatch engine (HRMS-0113). Matches most of the story. Missing: "click to deep-link" -- clicking a notification in `TopBar.js` only marks it read (`handleMarkRead`); there is no navigation to the related interview/candidate/override/SLA-breach entity. |

## EPIC-P3 -- Proactive AI Nurture & Candidate Relationship Engine (sheet: 0/9)

All 9 stories in this epic form a dependency chain (S-119 → S-120 → S-121 → S-122/S-123 → S-124 → S-125; S-121 → S-126; S-120 → S-127). None of the chain's foundational concepts exist in code.

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-119 | HRMS-P301 | Career Goal Conversation Engine | Planned | **NOT-DONE** | Grep for "career_goal/CareerGoal/career check-in" across `app/`: 0 hits | No 30/90-day career check-in cadence or conversation flow exists. |
| S-120 | HRMS-P302 | Personalized Value Messaging Engine | Planned | **NOT-DONE** | No dedicated "why BlitzenX is right for THEM" generation service or A/B-test variant mechanism found | `mellow_keepwarm_service.py` (a different, earlier story -- fixed weekly keep-warm cadence, S-012-era) sends a single fixed template, the opposite of this story's "never generic, memory-driven, A/B tested" requirement. |
| S-121 | HRMS-P303 | Candidate Relationship Score (CRS) | Planned | **NOT-DONE** | Grep for "CRS/candidate_relationship_score/relationship_score" across `app/`: 0 hits | No scoring model (response rate/sentiment/recency/interest/completeness weighted formula) exists anywhere. |
| S-122 | HRMS-P304 | Passive Candidate Nurture Campaign | Planned | **NOT-DONE** | Grep for "nurture_campaign/NurtureCampaign/passive_candidate": 0 hits | Depends on S-303 (CRS), which doesn't exist. |
| S-123 | HRMS-P305 | Candidate Interest Signal Detection | Planned | **NOT-DONE** | No LinkedIn-profile-change, title-stagnation, or negative-sentiment-toward-employer detection service found | `candidate_desire_signal.py`/`desire_signal_service.py` exist but track a different concept (portal page-view behavioral signals for the DESIRE engagement scoring epic, not readiness-to-move signals). |
| S-124 | HRMS-P306 | Nurture Campaign Scheduler & Config UI | Planned | **NOT-DONE** | Depends on S-302/S-303, neither of which exist; no admin campaign-config screen found | -- |
| S-125 | HRMS-P307 | Nurture Campaign Analytics | Planned | **NOT-DONE** | Depends on S-306, which doesn't exist | -- |
| S-126 | HRMS-P308 | Candidate Re-Engagement on Market Signal | Planned | **NOT-DONE** | `app/services/ready_for_opportunity_service.py:1-40` is an adjacent but distinct real mechanism | `ready_for_opportunity_service.py` (built for a different, 2026-08-04-scoped backlog item) does re-engage a watched candidate when a **new job is published** and its skills/title keyword-overlap with the candidate, triggered event-driven off `scan_new_job_for_matches()`. This is conceptually similar (event-driven re-engagement, not scheduled) but is not S-126: it isn't keyed off Guidewire-specific demand spikes, doesn't use CRS to select which passive candidates to target, and applies to any declined-offer candidate rather than a "passive Guidewire candidate" pool. Noted as a partial conceptual overlap, not counted as satisfying the story. |
| S-127 | HRMS-P309 | WhatsApp Broadcast Compliance Engine | Planned | **NOT-DONE** | Grep for "opt-out/do-not-contact/broadcast complian*" across `app/services/whatsapp_routing_service.py` and `app/models/consent.py`: 0 hits; grep for "STOP/unsubscribe" in `whatsapp_webhook_service.py`: 0 hits | No approved-template enforcement, opt-out/STOP-keyword handling, frequency-limit tracking, or do-not-contact list exists for WhatsApp broadcasts anywhere in the codebase. |

## Summary counts

- **CONFIRMED-DONE:** 6 (S-083, S-084, S-087, S-088, S-095, S-101)
- **PARTIAL:** 13 (S-081, S-085, S-086, S-089, S-090, S-091, S-094, S-096, S-098, S-100, S-102, S-104, S-105)
- **NOT-DONE:** 15 (S-082, S-092, S-093, S-097, S-099, S-103, S-119, S-120, S-121, S-122, S-123, S-124, S-125, S-126, S-127)
- **CANT-DETERMINE:** 0
