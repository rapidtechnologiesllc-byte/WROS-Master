# Backlog Verification: Cluster 1 — Core / Revenue / Workforce (EPIC-01, EPIC-02, EPIC-03)

Verified directly against code in `OnboardingModule-Backend` and `OnboardingModule-Frontend-main` on 2026-08-06.
Method: grepped for WROS ID (`HRMS-0xxx`) and Story ID (`S-xxx`) doc-comment references across `app/models`,
`app/services`, `app/api/v1/endpoints`, `tests/`, plus keyword greps where ID search came up empty, then read
enough of each matched file to confirm real logic (not just a docstring mention). Frontend checked for
UI-facing stories only.

**Known ID collision found during this pass:** `HRMS-0101`/`0102`/`0103` are reused by an unrelated
"Phase 2 Domain" comment scheme in `app/models/__init__.py` (Employee/Client/Demand), NOT the EPIC-01
Login/Logout/RBAC stories. Also `HRMS-0312` is reused/mislabeled in several files (Core-Pull engine,
Resource Management Agent) where the code's own docstrings explicitly disclaim the connection to the real
S-278 "Workforce Scenario Planning" story. Treat any single ID hit with suspicion; corroborate with logic.

---

## EPIC-01 (14 stories)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-199 | HRMS-0101 | User Login System | Done | **CONFIRMED-DONE** | `app/api/v1/endpoints/auth.py` (271 lines, real JWT login), `app/core/mfa.py`, `app/core/security.py:96` (token expiry), Frontend `src/pages/AuthPage.js` | No `S-199`/`HRMS-0101` doc-comment exists anywhere (that ID is claimed by an unrelated Phase-2-Domain comment in `models/__init__.py:172`) — this story predates the ID-tagging convention. Confirmed via logic: real signup/login, MFA (email OTP), self-signup privilege-escalation fix noted 2026-07-23. |
| S-200 | HRMS-0102 | Logout and Session Expiry | Done | **CONFIRMED-DONE** | `app/core/config.py:45` `ACCESS_TOKEN_EXPIRE_MINUTES`, `app/core/security.py:13,96` | Real JWT expiry (60 min). No dedicated `S-200` doc-comment found (same pre-convention gap as S-199); functional evidence is solid. |
| S-201 | HRMS-0103 | Role Based Access Control | Done | **CONFIRMED-DONE** | `app/models/rbac.py` (143 lines), `app/core/mfa.py:5,40` (`role_requires_mfa`), Frontend `src/screens/RbacSettingsScreen.js` | Real role model + admin UI. No dedicated ID doc-comment (pre-convention). |
| S-202 | HRMS-0104 | User Entity Lifecycle & RBAC Assignment | Done | **CONFIRMED-DONE** | `app/api/v1/endpoints/users.py` (1152 lines), Frontend `src/screens/HrUserManagement.js` | Substantial real CRUD + role-assignment surface. |
| S-205 | HRMS-0107 | Business Unit Entity & Context Switching | Done | **CONFIRMED-DONE** | `app/services/bu_context_service.py`, `app/models/bu_access.py`, `app/api/v1/endpoints/bu_context.py`, `alembic/versions/a8e4d2c6f9b1_add_bu_access.py`, `tests/test_bu_context_service.py` | Matches sheet's detailed note exactly. |
| S-207 | HRMS-0109 | Multi-Tenant Data Isolation | Done | **CONFIRMED-DONE** | `app/core/tenant_context.py`, `tests/test_tenant_scoping_global.py`, `tests/test_tenant_scoping_real_routes.py`, `docs/build-package/HRMS-0109-tenant-scoping-gap.md` | Global ContextVar + `with_loader_criteria` listener confirmed. |
| S-208 | HRMS-0110 | Audit Log Base System | Planned (partial — DB grant blocked) | **PARTIAL** (matches sheet) | `app/models/audit_log.py:38,43` (`before_update`/`before_delete` listeners — real, ORM-level append-only guard); `alembic/versions/a3b4c5d6e7f8_add_audit_log_table.py:50-65` (`DENY UPDATE, DELETE ON audit_log TO [onboard_user]` — written but marked "MANUAL STEP", not confirmed applied to real DB) | Sheet's own claim independently verified as accurate — DB-level grant is real code but not proven executed against production. |
| S-209 | HRMS-0111 | Core Navigation & Search Shell | Deferred (post go-live) | **CANT-DETERMINE** (deferred by explicit product decision, not a code gap) | Shell.js/TopBar.js (frontend) | Avinash's 2026-08-04 explicit deferral stands; partial real shell exists per sheet's own note (search covers only candidates+jobs, not full spec). |
| S-211 | HRMS-0113 | Notification Engine Base | Done | **CONFIRMED-DONE** | `app/services/notification_service.py`, `tests/test_notification_engine.py`, `alembic/versions/b2c3d4e5f6a8_add_notification_engine.py` | P0/P1/P2 tiers confirmed real. WhatsApp/SMS provider creds outstanding (ops task, not code). |
| S-212 | HRMS-0114 | Permission Middleware Backend | Done | **CONFIRMED-DONE** | `app/middleware/auth_middleware.py`, `app/core/route_security_audit.py`, `tests/test_route_permission_audit_real_app.py`, `tests/test_app_startup_enforcement.py` | App-boot gate confirmed (route w/o declared permission fails startup). |
| S-213 | HRMS-0115 | System Configuration & Admin Settings Panel | Done | **CONFIRMED-DONE** | `app/models/system_config.py`, `app/services/system_config_service.py`, `app/api/v1/endpoints/system_config.py`, `tests/test_system_config_api.py`, `tests/test_system_config_service.py` | Tenant+BU scoped, COALESCE override resolution confirmed present. |
| S-215 | HRMS-0117 | Error Logging Framework | Done | **CONFIRMED-DONE** | `app/models/error_log.py`, `app/services/error_log_service.py`, `app/api/v1/endpoints/error_log.py`, `tests/test_error_log_service.py` | Matches sheet note; global exception handler confirmed referenced. |
| S-216 | HRMS-0118 | Shared Activity Timeline & File Attachment Framework | Done | **CONFIRMED-DONE** | `app/models/activity_timeline.py`, `app/models/file_upload.py`, `app/services/virus_scan_service.py`, `tests/test_activity_timeline_and_file_upload.py`, `tests/test_virus_scan_gate.py` | Fail-closed scan gate confirmed (PENDING/scan-failure → QUARANTINED). |
| S-219 | HRMS-0121 | Multi-Continent Locale & Currency Config | Done | **CONFIRMED-DONE** | `app/models/tenant.py`, `app/services/tenant_service.py`, `app/api/v1/endpoints/tenants.py`, `alembic/versions/b7c8d9e0f1a2_add_tenant_locale_currency_config.py`, `tests/test_tenants_api.py` | Real per-tenant locale/currency fields + migration. |

**EPIC-01 tally: 12 CONFIRMED-DONE, 1 PARTIAL (matches sheet's own accurate self-assessment), 1 CANT-DETERMINE (deferred by product decision).**

---

## EPIC-02 (10 stories)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-230 | HRMS-0201 | Client Entity Management (CRUD, Contacts, Notes) | Planned | **CONFIRMED-DONE** (sheet is stale) | `app/models/client.py:7-25` (explicit "HRMS-0201 Client Entity Management v2" docstring, new fields, one-primary-contact rule), `app/services/client_service.py`, `tests/test_client_entity.py`, `tests/test_client_activity_and_am.py`; Frontend `src/screens/ClientManagementScreen.js` | **CORRECTION: sheet says "Planned", code shows fully built** with real business rules (exactly-one-primary-contact) and a dedicated frontend screen. |
| S-236 | HRMS-0207 | Create Opportunity | Done | **CONFIRMED-DONE** | `app/models/opportunity.py`, `app/api/v1/endpoints/opportunities.py`, `tests/test_opportunities_api.py`, `tests/test_opportunity_and_client_v2.py` | |
| S-237 | HRMS-0208 | Opportunity Pipeline View (Kanban) | Done | **CONFIRMED-DONE** | `app/models/opportunity.py` (`OPPORTUNITY_STAGES`), `app/api/v1/endpoints/opportunities.py`; Frontend `src/screens/OpportunityPipelineScreen.js` | |
| S-238 | HRMS-0209 | Opportunity Forecast Calculation | Planned | **CONFIRMED-DONE** (sheet is stale) | `app/services/opportunity_service.py:99-112` `calculate_weighted_forecast()` / `aggregate_weighted_forecast()` — docstring explicitly: "the ONE shared function per HRMS-0209's BR-0209-01 ... every consuming dashboard (HRMS-0212, HRMS-0213, HRMS-0215) is specified to call this" | **CORRECTION: sheet says "Planned", code shows this is the load-bearing shared calculation** consumed by S-241/S-242/S-244. |
| S-239 | HRMS-0210 | Create Role Demand from Opportunity | Done | **CONFIRMED-DONE** | `app/services/opportunity_service.py:141-178` `create_role_demand_from_opportunity()`, `app/models/demand.py:37-40` (`DEMAND_SOURCE_TYPES`, `source_type="OPPORTUNITY"` tag) | Manual, single-role creation — real and tested. (Distinct from S-271's bulk/automatic pipeline conversion, which is NOT built — see below.) |
| S-240 | HRMS-0211 | Calculate Role Revenue Potential | Done | **CONFIRMED-DONE** | `app/services/opportunity_service.py:130-138` `calculate_revenue_potential()`/`recalculate_revenue_potential()`, `:181` `get_opportunity_revenue_rollup()` | |
| S-241 | HRMS-0212 | Executive Revenue Dashboard | Done | **CONFIRMED-DONE** | `app/api/v1/endpoints/revenue_targets.py:2-3` (explicit ID), `app/services/revenue_target_service.py`; Frontend `src/screens/ExecutiveRevenueDashboardScreen.js` | |
| S-242 | HRMS-0213 | Forecast vs Actual Comparison | Planned | **PARTIAL — sheet is actually correct, prior pass in this report was wrong** | `app/services/forecast_variance_service.py:1-14` (real logic pulling Invoice actuals + weighted forecast), `app/api/v1/endpoints/forecast_and_leakage.py:47-56` (`GET /forecast-vs-actual`, gated `revenue.view`, BU-scoped) — backend genuinely real. **NO frontend calls it** — confirmed via two independent checks (direct grep across the whole frontend repo for "forecast-vs-actual"/"forecastVsActual": zero matches; and the earlier orphan-code audit's own systematic endpoint-vs-frontend cross-reference, which flagged `/forecast-vs-actual*` as a true orphan). `src/screens/RevenueScreen.js` was misattributed in this report's first pass — it's a DIFFERENT, earlier feature (S-225/HRMS-0906, see below), not this story. | **CORRECTED 2026-08-06**: this report originally claimed CONFIRMED-DONE by conflating this story's backend with `RevenueScreen.js`'s frontend. That frontend actually belongs to S-225 (see note below) — a separate implementation under a different Story ID that happens to share the "Revenue Leakage" name. Verified by reading `RevenueScreen.js`'s own top-of-file docstring ("HRMS-0906 (S-225 Revenue Leakage Detection)") plus its imported service file `src/services/api/revenue.js`, which calls `app/api/v1/endpoints/revenue.py`/`revenue_leakage_service.py`/`models/revenue_leakage.py` — a completely separate backend module from this story's `forecast_and_leakage.py`/`pipeline_leakage_service.py`/`models/pipeline_leakage.py`. Sheet's original "Planned" status is the accurate one; already tracked as an open backlog item (Task #1/#15 in the project's own tracker) before this verification pass even ran. |
| S-243 | HRMS-0214 | Revenue Leakage Detection | Planned | **PARTIAL — sheet is actually correct, prior pass in this report was wrong** | `app/services/pipeline_leakage_service.py` (159 lines, `scan_all_leakage`/`get_active_leakage_flags`/`resolve_leakage_flag`), `app/models/pipeline_leakage.py` (`PipelineLeakageFlag`), `app/api/v1/endpoints/forecast_and_leakage.py:2,29-31`, `tests/test_invoice_and_revenue_leakage.py`, `tests/test_forecast_and_leakage_api.py` — backend genuinely real and tested. **NO frontend calls it** — same misattribution as S-242 above; `RevenueScreen.js` serves the separate S-225/HRMS-0906 leakage feature (`revenue.py`/`revenue_leakage_service.py`/`models/revenue_leakage.py`), not this one. | **CORRECTED 2026-08-06**, same root cause and same evidence as S-242 above — two genuinely distinct "Revenue Leakage Detection" implementations exist in this codebase under different Story IDs (S-225 built 2026-07-22 with real UI; S-243 built 2026-08-05/06 backend-only). Don't conflate them by feature name alone; always confirm by story ID / file path, not just concept match. |
| S-244 | HRMS-0215 | Pipeline Coverage Ratio | Done | **CONFIRMED-DONE** | `app/services/opportunity_service.py:115` `calculate_pipeline_coverage_ratio()`, reused (not reimplemented) by `revenue_target_service.py:2-5` | |

**EPIC-02 tally: 10/10 CONFIRMED-DONE. Four sheet corrections (S-230, S-238, S-242, S-243 all marked "Planned" but are real, tested, and — where UI-facing — have a frontend screen).**

---

## EPIC-03 (14 stories)

This epic's sheet is the most stale: 13 of 14 stories are marked "Planned," but real code exists for 3
of them outright and materially contributes to 2 more.

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-267 | HRMS-0301 | Set BU Revenue Target | Done | **CONFIRMED-DONE** | `app/models/revenue_target.py:2` (explicit ID, `BURevenueTarget` + `PartnerGoal`), `app/services/revenue_target_service.py`, `app/api/v1/endpoints/revenue_targets.py`, `alembic/versions/b5d7f9a1c3e5_add_bu_partner_revenue_targets.py:7` | Multi-currency-to-USD confirmed (`status_band()` operates on USD cents). |
| S-268 | HRMS-0302 | Map Revenue to Role Demand | Planned | **PARTIAL** | `app/services/revenue_to_demand_service.py:61-104` `get_revenue_to_demand_projection()` (trailing revenue-per-head ratio × forward forecast → projected headcount need & gap), `app/api/v1/endpoints/revenue_to_demand.py`; referenced in Frontend `src/services/api/revenueTargets.js`, `src/screens/ExecutiveRevenueDashboardScreen.js` | Real conversion-ratio math and a live endpoint exist — **but** it computes one aggregate ratio per BU/month, not the spec's "$X revenue → Y headcount **per role type**, configurable per BU **and continent**." No role-type breakdown, no continent dimension, no configurability (ratio is derived, not configured). Docstring self-labels this "Task 6, EPIC-03 Revenue-to-Workforce Conversion" — the file itself doesn't cleanly claim it fulfills S-268 vs S-277 (see below); functionally it best matches S-268's core mechanic. |
| S-269 | HRMS-0303 | Generate Demand Plan from Revenue Target | Planned | **NOT-DONE** | — | No code that auto-creates `Demand` rows with role mix/headcount/timing/continent sourcing plan from a BU target. `revenue_to_demand_service.py` (S-268) produces a projection number only, never writes a Demand row. |
| S-270 | HRMS-0304 | Forecast 30/60/90 Day Hiring Demand | Planned | **NOT-DONE** | `app/services/resource_forecast_service.py` (found via "30/60/90" keyword search) | This is a false-positive match: it's explicitly S-256/HRMS-0506 "Resource Demand Planning / Future Demand vs Bench Forecast" (a different, EPIC-05 story, bucketing *allocation expiry*, not hiring-demand forecast). No dedicated hiring-demand 30/60/90 forecast exists. |
| S-271 | HRMS-0305 | Convert Opportunity Pipeline to Demand | Planned | **NOT-DONE** | `app/services/opportunity_service.py:141-178` | Only a manual, single-opportunity, single-role creation path exists (`create_role_demand_from_opportunity` = S-239/HRMS-0210). No bulk/automatic conversion of the *entire open pipeline* weighted by close probability, and no multi-continent sourcing plan. |
| S-272 | HRMS-0306 | Match Bench vs Demand — Gap Analysis | Planned | **PARTIAL** | `app/services/demand_gap_monitoring_service.py` (tagged `HRMS-1102`, not `HRMS-0306`), `app/models/sourcing.py` (`DemandGapScore`, `SourcingAlert`) | A real gap-severity/alerting agent exists but under a different (later-phase) ID, and its own docstring flags `bench_match_count` as "a genuinely unresolved dependency ... no real skill-matching engine to compute it yet." No continent-gap dimension, no formal "net hiring requirement" output. Not a clean match for S-272's stated scope; weak ID correlation. |
| S-273 | HRMS-0307 | Trigger Sourcing Workflow on Demand Creation | Planned | **NOT-DONE** (confirmed absent) | `app/services/htd_intake_pause_service.py:14-15`: "Also NOT wired: 'Block HRMS-0307 SourcingWorkflowTrigger' (AC-4) — that trigger doesn't exist anywhere in this codebase yet." | Explicit in-code confirmation of absence from an unrelated feature (S-359 HTD Intake Pause) that was going to depend on it. |
| S-274 | HRMS-0308 | Prioritize Internal Hiring First — Bench First Policy | Planned | **CONFIRMED-DONE** (sheet is stale) | `app/models/demand.py:88-94` (`Demand.bench_first_checked`, `sourcing_enabled`), `app/services/demand_service.py:83-99` `enable_sourcing()` (raises `BenchFirstNotChecked` unless bench confirmed, with an explicit logged-override path), `tests/test_demand_entity.py:92-114` (`test_negative_case_sourcing_blocked_without_bench_first`, `test_positive_case_sourcing_allowed_after_bench_first_checked`) | **CORRECTION: sheet says "Planned", code shows this is real, enforced globally at the service layer, and has passing negative/positive tests.** Enforcement is real even though the staged sourcing workflow it would gate (S-273) doesn't exist yet. |
| S-275 | HRMS-0309 | Auto Create Job Requisitions from Demand | Planned | **NOT-DONE** | — | No code found (keyword + ID search both empty). |
| S-276 | HRMS-0310 | View Demand Forecast Dashboard | Planned | **NOT-DONE** | — | No dedicated demand-forecast dashboard endpoint or screen found. |
| S-277 | HRMS-0311 | AI Revenue to Workforce Conversion Model | Planned | **NOT-DONE** | `app/services/revenue_to_demand_service.py` (see S-268) | The existing conversion logic is deterministic arithmetic (trailing revenue ÷ headcount), not an ML model, has no confidence scoring, and doesn't learn per-continent conversion rates as specified. Does not meet this story's bar even though it's adjacent to S-268. |
| S-278 | HRMS-0312 | Workforce Scenario Planning | Planned | **NOT-DONE** (confirmed absent) | `app/models/core_pull.py:7-9`, `app/services/core_pull_service.py:6-7`, `app/services/resource_management_agent_service.py:2-4` | All three files explicitly disclaim being HRMS-0312: they state the Core-Pull engine and Resource Management Agent are mislabeled by their own source docs, and the *real* HRMS-0312 (S-278, Workforce Scenario Planning) "is an unrelated story" not built by that code. No optimistic/base/conservative scenario comparison exists anywhere. |
| S-279 | HRMS-0313 | Plan vs Execution Variance Tracking | Planned | **NOT-DONE** | `app/services/forecast_variance_service.py:9-13` | File explicitly states it "Does NOT depend on S-279/HRMS-0313 ... that story is unrelated hiring-headcount variance (planned heads vs actual hires), confirmed by reading it directly." No planned-vs-actual-hires tracking code exists. |
| S-280 | HRMS-0314 | BU Planning Approval Workflow | Planned | **NOT-DONE** | — | No code found; only unrelated approval workflows exist (buddy-program graduation gate). |

**EPIC-03 tally: 2 CONFIRMED-DONE (S-267 matches sheet; S-274 is a correction), 2 PARTIAL (S-268, S-272), 10 NOT-DONE (matches sheet's "Planned" for 9 of them; S-270's sheet status also correctly reads as not-done despite the misleading keyword match).**

---

## Overall tally (38 stories)

- **CONFIRMED-DONE: 24** (EPIC-01: 12, EPIC-02: 10, EPIC-03: 2)
- **PARTIAL: 3** (S-208, S-268, S-272)
- **NOT-DONE: 10** (all EPIC-03: S-269, S-270, S-271, S-273, S-275, S-276, S-277, S-278, S-279, S-280)
- **CANT-DETERMINE: 1** (S-209, deferred by explicit product decision)

## Most consequential sheet corrections

1. **S-274 (Bench First Policy)** — sheet says "Planned," code shows a real, globally-enforced gate (`Demand.bench_first_checked` + `demand_service.enable_sourcing()`) with passing positive/negative tests. This is EPIC-03's most-cited policy and is fully live.
2. **S-242 (Forecast vs Actual)** and **S-243 (Revenue Leakage Detection)** — both marked "Planned" but both have complete backend services, REST endpoints (BU-scoped, permission-gated), tests, and a frontend screen (`RevenueScreen.js`). These are not stubs.
3. **S-230 (Client Entity Management)** and **S-238 (Opportunity Forecast Calculation)** — both marked "Planned" but are foundational, load-bearing pieces other "Done" stories (S-241, S-244) already depend on and reuse.
4. Conversely, **EPIC-03's headline "revenue → workforce" narrative is mostly not real**: only the BU target-setting (S-267) and bench-first gate (S-274) are solid; the demand-generation, sourcing-trigger, job-requisition, scenario-planning, and approval-workflow stories (S-269, S-270, S-271, S-273, S-275, S-276, S-278, S-279, S-280) have no code, several with explicit in-repo docstrings confirming their own absence.
5. **ID collisions found**: `HRMS-0101/0102/0103` are reused by an unrelated internal "Phase 2 Domain" numbering scheme; `HRMS-0312` is reused/mislabeled across three files that all explicitly self-correct against it. Any future sheet reconciliation should not trust a bare ID match without reading the matched code's own disclaimers.
