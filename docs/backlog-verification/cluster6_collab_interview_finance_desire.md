# Backlog verification: EPIC-14 / EPIC-15 / EPIC-16 / DESIRE

Verified directly against code in `OnboardingModule-Backend` and `OnboardingModule-Frontend-main`. Sheet "Status" column and prior summary claims were not trusted — every row below is grounded in a specific file.

## EPIC-14 — Internal Collaboration Hub (6 stories)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence | Notes |
|---|---|---|---|---|---|---|
| S-379 | HRMS-1401 | M365 SSO bridge + embedded tab/dock shell for Outlook/Teams | Ready for Build | **PARTIAL** | `app/api/v1/endpoints/msgraph.py:91-134` (`/link/start`, `/link-status`, `/unlink`), `tests/test_msgraph_account_linking.py` (9 passing-shaped tests) | Backend account-linking logic (link WITHOUT swapping WROS session) is real and tested. But it has **zero frontend consumer** — grepped frontend for `link/start`/`link-status`/`unlink` calls: none exist. `InterviewSchedule.js`, `InterviewStatus.js`, `CandidateCreate.js` all call the *older*, pre-existing `/auth/signin` flow instead (mints a fresh WROS session — the exact behavior S-379 was built to avoid). No "embedded tab/dock framework" (Launchpad shell) exists anywhere in the frontend. |
| S-380 | HRMS-1402 | Embedded Outlook Mail/Calendar tab, WROS-context deep linking | Ready for Build | **NOT-DONE** | `app/api/v1/endpoints/msgraph.py` has `mail/send`, `calendar/schedule`, `calendar/meetings` as discrete API actions | These are Graph API wrapper *actions* invoked from existing scheduling forms (e.g. `InterviewSchedule.js`), not an embedded Outlook Mail/Calendar tab surface with deep linking. No embedded-tab UI exists. |
| S-381 | HRMS-1403 | Teams chat dock + unified notification center | Ready for Build | **NOT-DONE** | grep for `teams_chat`, `teams.*dock`, `notification_center` across `app/`: no matches | Only surface: `"Microsoft Teams"` as a meeting-platform dropdown value (`CandidateDetailsScreen.js:88`) and `outlook_event_id` storage — cosmetic, not a chat dock. Matches memory note that S-381 was explicitly deferred. |
| S-382 | HRMS-1404 | Dynamic Reporting Hierarchy Engine (configurable org-level chain walk) | Ready for Build | **NOT-DONE** | `app/models/employee.py:159` has only a flat `reporting_manager_user_id` FK | No org-level list, no chain-walking/level-resolution service exists anywhere (`grep -rliE "org_level|reporting_hierarchy"` → no service hits). |
| S-383 | HRMS-1405 | Admin-editable check-in cadence by org level | Ready for Build | **NOT-DONE** | No matches for `check_in`/`checkin`/`cadence` config anywhere in `app/models` or `app/services` | Depends on S-382, which doesn't exist. |
| S-384 | HRMS-1406 | Auto-scheduler batch job for recurring check-ins via Outlook Calendar | Ready for Build | **NOT-DONE** | No `auto_scheduler` service, no scheduler.py job for check-ins | Depends on S-382/S-383/S-380, none of which exist. |

**EPIC-14 real count: 0/6 fully done, 1/6 partial (S-379 backend-only), 5/6 not done.** (Note: S-435, the M365 mail-sync/high-water-mark story confirmed built 2026-08-05, is a real and separate story — it is not one of this cluster's 6 in-scope EPIC-14 items and does not change this count.)

## EPIC-15 — Interview Integrity Layer (2 stories)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence | Notes |
|---|---|---|---|---|---|---|
| S-385 | HRMS-1501 | Interview Integrity Analysis Engine — analyze consented recording/transcript for rehearsed-answer, outside-assistance, identity-mismatch signals | Ready for Build | **NOT-DONE** | `app/core/llm_prompt_safety.py:4` and `app/models/consent.py:9` only *mention* HRMS-1501 as a **future** consumer of shared infra ("so every later agent... reuses it", "e.g. HRMS-1501's... consent gate builds on this"). Grepped `rehearsed|identity.mismatch|outside.assistance|transcript.*analy|interview_integrity` across the whole backend: zero implementation hits. | No service, model, or endpoint implements this. |
| S-386 | HRMS-1502 | Panel Feedback Cross-Validation & Clarification Routing | Ready for Build | **NOT-DONE** | Grepped `clarification_routing|cross.validation`: zero hits | Depends on S-385, which doesn't exist. |

**Important correction to this session's earlier working assumption:** `app/services/interview_rehire_guard_service.py` and `app/services/interview_sequencing_service.py` (both real, well-tested modules) do **NOT** map to EPIC-15. Read in full — both modules' own docstrings make this explicit:
- `interview_rehire_guard_service.py` is "Part 2 of the interview regrouping + rehire guard priority Avinash flagged... after epic-01 is complete" — a justification/approval gate before **re-scheduling a past no-hire candidate**.
- `interview_sequencing_service.py` implements **R-05** ("L1 must pass before L2 can be scheduled") for the legacy interview-panel system.

Neither touches recording/transcript analysis, rehearsed-answer detection, identity mismatch, or panel-decision-vs-AI-assessment cross-validation — the actual substance of S-385/S-386. They are a distinct, unrelated backlog item (the "interview regrouping + rehire guard" priority from the 2026-08-05 memory note), not EPIC-15 work.

**EPIC-15 real count: 0/2. The rehire-guard/sequencing services do not count toward it.**

## EPIC-16 — Finance & Accounting Operations (15 stories)

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence | Notes |
|---|---|---|---|---|---|---|
| S-387 | HRMS-1601 | Timesheet Submission Nag Agent — 7-step email/Teams/WhatsApp cascade to employee then RM/RM's-manager/BU-Head, timezone-aware | Ready for Build | **PARTIAL** | `app/services/timesheet_nag_service.py` (136 lines), registered job at `app/core/scheduler.py:798-816` | Real, scheduled, tested 2-level escalation (employee → reporting manager only) via generic `send_notification()`. Not the specified 7-step cascade (no explicit Teams/WhatsApp channel routing visible in this module, no RM's-manager/BU-Head tier, no explicit business-hours/timezone gate in this module). |
| S-388 | HRMS-1602 | Monthly Invoice Generation Cycle — scheduled trigger on the 2nd of month | Ready for Build | **PARTIAL** | `app/api/v1/endpoints/invoices.py:88` `/invoices/generate` (manual endpoint, reuses S-226 logic); `app/core/scheduler.py` has no invoice-generation job | The underlying generation logic is real (reused from S-226) and frontend-wired (`InvoicesScreen.js`), but it's manually triggered per-project only — no scheduled "2nd-of-month" batch trigger exists anywhere in `scheduler.py`. |
| S-389 | HRMS-1603 | Manual Invoice Mark-as-Paid, captures payment_date | Ready for Build | **CONFIRMED-DONE** | `app/services/invoice_service.py:116-122` (`mark_invoice_paid`, sets `paid_at`), `app/api/v1/endpoints/invoices.py:144` (`/invoices/{id}/mark-paid`), `InvoicesScreen.js:137` (real "Mark Paid" button, gated on SENT status) | Full stack real. Note: not wired as an automatic trigger into `partner_incentive_service.py` (see S-399) — that linkage is explicitly deferred per that module's own docstring. |
| S-390 | HRMS-1604 | AR Follow-Up Agent — 15/30/45-day escalating reminders, LLM-drafted messages queued for review | Ready for Build | **PARTIAL** | `app/services/ar_followup_service.py` (113 lines): `scan_overdue_invoices`, `trigger_ar_follow_up`, scheduled at `scheduler.py:827` | Real single-tier aging scan (one grace-day threshold, default 30d) + Task creation + notification. No 15/30/45-day escalation ladder, and no LLM-drafted client message generation/review-queue — it creates an internal Task, not an outbound client-facing draft. No frontend AR-aging panel found anywhere (`FinanceOperationsScreen.js` doesn't surface it; `InvoicesScreen.js` doesn't either). |
| S-391 | HRMS-1605 | Bank Statement Reconciliation — PDF/Excel upload, LLM extraction, deterministic matching, Finance-confirmed only | Ready for Build | **PARTIAL** | `app/services/bank_reconciliation_service.py` (67 lines): `record_bank_transaction`, `match_transaction_to_invoice` (exact-amount match only), `get_unreconciled_transactions` | Real manual-entry + deterministic exact-amount matching engine, matching its own docstring ("manual-entry, not a real bank feed"). No PDF/Excel upload, no LLM extraction step. No frontend UI panel exists (checked `FinanceOperationsScreen.js` — not present). Backend-only, and even the backend implements a narrower slice than the story (no document upload/extraction). |
| S-392 | HRMS-1606 | Intercompany Settlement Ledger (BXIN/BXUS) — manual entry, NPBR reference only | Ready for Build | **CONFIRMED-DONE** | `app/services/intercompany_ledger_service.py` (real, manual-only, matches its own docstring); `app/api/v1/endpoints/cost_rate.py:154-186`; `FinanceOperationsScreen.js:283-303` (real "Intercompany Settlement" panel, BXUS/BXIN entity fields, settlement list) | Full stack real and matches spec (manual-entry only, net-position shown as reference, never auto-posted). |
| S-393 | HRMS-1608 | Fully Loaded Cost Engine — locked India/US formulas, ESI cap, 4 flagged placeholder constants | Ready for Build | **PARTIAL** | `app/services/cost_rate_service.py` (real, tested), `app/models/cost_rate_config.py` (config-driven statutory_pct/overhead_pct), `FinanceOperationsScreen.js:250-257` ("Set Cost & Rate Config" panel) | Real, full-stack, deliberately documented deviation: implements a **generic, admin-configurable** statutory%/overhead% model rather than the story's specific **locked India/US formulas with ESI-cap logic and 4 flagged placeholder constants**. Model docstring explicitly says "exact percentages are Avinash's to configure, never invented here" — an honest simplification, but not what S-393 specifies. |
| S-394 | HRMS-1609 | RM Burden Allocation Engine — flat per-hour RM overhead across billable employees | Ready for Build | **NOT-DONE** | Grepped `rm_burden`, `RM Burden`, `burden_allocation` across all of `app/`: zero hits | No such engine exists. `cost_rate_service.py` has a *different* concept, `calculate_blended_delivery_rate`, but it is BU-level revenue÷hours, not an RM-overhead allocation. |
| S-395 | HRMS-1610 | Minimum Bill Rate Engine — (FLC + RM Burden) × multiplier | Ready for Build | **NOT-DONE** | Grepped `minimum_bill_rate`, `min_bill_rate`: zero hits | Depends on S-394, which doesn't exist. |
| S-396 | HRMS-1607 | BXIN/BXUS Separate P&L Engine — Revenue/Salary/EBITDA/Net Profit per legal entity | Ready for Build | **NOT-DONE** | `app/services/pnl_service.py:1-11` (module docstring, verbatim) | The module's own docstring states this explicitly: "Location P&L is NOT built: no field anywhere on Employee identifies which legal entity (India vs US) an employee sits in... Building Location P&L against either would be a guess dressed up as data." What **is** built is BU P&L (Axion vs Prism) — a different dimension, real and good (`get_bu_pnl`/`get_org_pnl_summary`, frontend panel in `FinanceOperationsScreen.js:209-227`), but it is not S-396. |
| S-397 | HRMS-1612 | Reserve Fund Engine — 25% of EBITDA auto-calculated (contribution or drawdown), 12-month buffer check | Ready for Build | **PARTIAL** | `app/services/reserve_fund_service.py` (real), `FinanceOperationsScreen.js:259-266,238-246` (real UI panel) | Real ledger + a genuine 12-month-target health check (`get_reserve_fund_target_usd_cents` = 12x trailing avg BU cost, matches the buffer-health-check half of the spec). But contributions/drawdowns are **manual Finance entries**, not an automatic "25% of EBITDA if positive, drawdown if negative" calculation — module docstring says this is deliberate ("a real transaction Avinash/Finance records, never invented here"). |
| S-398 | HRMS-1613 | Hiring Affordability Gate — TWO hard gates (Reserve check + Run-rate check), zero override | Ready for Build | **PARTIAL** | `app/services/hiring_affordability_service.py` (59 lines, real), `FinanceOperationsScreen.js:268-281` (real UI panel) | Real, tested single-gate check: projected BU gross margin stays ≥0% after adding the hire's fully-loaded cost. This is only **one** gate (a margin check), not the specified **two** hard gates (Reserve Fund check + Run-rate check). Module docstring flags the margin floor itself as "the one real assumption... easy to make configurable later once Avinash gives an actual minimum-margin policy" — an honest gap, not silently invented, but not what the story specifies. |
| S-399 | HRMS-1611 | Partner Incentive Calculator — paid-invoices-only trigger, rolling calc, Troy/Curtis/Avinash revenue share + bonuses | Ready for Build | **CONFIRMED-DONE** (with one gap) | `app/services/partner_incentive_service.py` (205 lines, real, idempotent, tested), `app/api/v1/endpoints/partner_incentives.py`, `FinanceOperationsScreen.js:305-326` (real UI panel) | Real new-logo-bonus + revenue-share engines, 1:1 Partner-per-BU resolution (Troy=Axion, Curtis=Prism), idempotent double-pay protection. Gap: the revenue-share calc is **not gated on paid invoices specifically** (uses `get_monthly_actual_revenue`, not invoice `PAID` status filtering), and it is **not automatically wired** to fire on invoice mark-paid — module docstring: "automatic wiring into the Client/Invoice write paths is deferred." No distinct "Avinash" incentive path found — only the generic Partner-role mechanism. |
| S-400 | HRMS-1614 | Executive Finance Dashboard — 7-block CEO dashboard, pure aggregation over the 5 engines | Ready for Build | **PARTIAL** | `app/services/pnl_service.py:74-118` (`get_org_pnl_summary`, real org-wide rollup), `app/api/v1/endpoints/cost_rate.py:107` (`/pnl/org-summary`) | Backend aggregation exists and is real (sums every BU's own `get_bu_pnl()`, honestly flags incomplete data). But there is no dedicated Executive Dashboard screen/component anywhere in the frontend — `FinanceOperationsScreen.js` is a per-BU operational screen, not a 7-block CEO rollup view, and doesn't call `/pnl/org-summary` at all. |
| S-401 | HRMS-1615 | GST & Statutory Compliance — UAT-gated placeholder scaffold | BLOCKED — Awaiting Finance/CA Review | **NOT-DONE** (correctly, matches sheet) | Grepped `GST`, `statutory.*complian`, `filing_ready`: zero hits | This is the one EPIC-16 story where sheet status is accurate — genuinely not built, and correctly blocked pending Finance/CA signoff per the sheet. |

**EPIC-16 real count: 2/15 fully confirmed-done (S-389, S-392), 1 more effectively done (S-399, minor gaps), 9/15 partial, 3/15 not done (S-394, S-395, S-401 — S-401 correctly marked blocked on the sheet).**

**Frontend coverage for EPIC-16, checked against `FinanceOperationsScreen.js` (333 lines) specifically as asked:**
- **Has a real UI panel:** Fully Loaded Cost (S-393), BU P&L display (part of S-396's scope but not the BXIN/BXUS legal-entity split S-396 actually asks for), Reserve Fund (S-397), Hiring Affordability (S-398), Intercompany Settlement (S-392), Partner Incentives/Revenue Share (S-399). Invoice mark-as-paid (S-389) has its own UI in `InvoicesScreen.js`.
- **Confirmed NO frontend UI anywhere:** AR Follow-Up / aging (S-390), Bank Reconciliation (S-391), RM Burden Allocation (S-394 — doesn't exist), Minimum Bill Rate (S-395 — doesn't exist), BXIN/BXUS Location P&L (S-396 — doesn't exist), Executive Finance Dashboard 7-block view (S-400), GST (S-401 — doesn't exist). Timesheet Nag (S-387) is a background-only agent by design, no UI expected.

## DESIRE — Desire Intelligence System (5 stories)

Per instructions, spent minimal effort re-confirming prior findings rather than re-deriving from scratch. One correction found.

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence | Notes |
|---|---|---|---|---|---|---|
| S-346 | HRMS-P116 | Portal Real-Time Chat Widget | NEW — Ready for Build | **CONFIRMED-DONE** (upgraded from prior finding) | `src/screens/CandidatePortalScreen.js:225-257` | **This session's earlier "missing real-time polling" gap has since been fixed.** Code comment at line 225: `"S-346 real-time fix, 2026-08-06: the backend has always supported this poll endpoint (after_id) -- nothing on this screen ever called it... Skipped entirely until conversationId is known."` A real `setInterval`-driven poll loop (`pollPortalMessages`) is now wired in, dedupes by message id, and the send handler was also fixed to stop using fake local ids that would double-render against real polled ids. **Correction to earlier session note: DESIRE is now 5/5, not 4.5/5.** |
| S-347 | HRMS-P117 | Candidate Desire Intelligence Engine | NEW — Ready for Build | **CONFIRMED-DONE** | `app/services/desire_signal_service.py` (248 lines), scheduled every 30 min (`scheduler.py:683-703`) | Prior finding holds. |
| S-348 | HRMS-P118 | Desire Profile Builder | NEW — Ready for Build | **CONFIRMED-DONE** | `app/services/desire_profile_service.py` (353 lines), scheduled every 4 hours (`scheduler.py:710-730`), `IntelligenceTab.js` (frontend, real panel visible on candidate detail) | Prior finding holds. |
| S-349 | HRMS-P119 | Proactive Motivation Engine | NEW — Ready for Build | **CONFIRMED-DONE** | `app/services/motivation_engine_service.py` (308 lines): `detect_trigger`, `generate_motivation_message`, `send_motivation_message`, `run_motivation_job` | Prior finding holds; real threshold-triggered strategy-shift logic. |
| S-350 | HRMS-P120 | HR Intelligence Briefing — daily 08:00 digest per recruiter across all their candidates, portal + email, `hr_intelligence_briefings` table | NEW — Ready for Build | **PARTIAL** (downgrade from prior "confirmed real") | `src/screens/tabs/IntelligenceTab.js:1` (self-labeled "S-350/HRMS-P120"), `app/services/desire_profile_service.py:230-269` (LLM briefing text) | What exists is a real **per-candidate, on-demand** motivation briefing (viewed one candidate at a time on the candidate detail screen, regenerated via a Refresh button gated to Partner/BU Head/HR Manager/Super User). This is NOT the story's actual spec: a **daily 08:00 push digest per recruiter, aggregating desire_score/gap/next-action across every candidate they're assigned**, delivered via portal notification + email, persisted in an `hr_intelligence_briefings` table. Grepped for `hr_intelligence_briefings`/`HrIntelligenceBriefing`: zero hits — no such table exists. `app/services/daily_digest_service.py` is a real, separate, pre-existing recruiter daily digest (candidate activity/risk/interviews-today) with no desire_score content in it at all, and no 08:00 scheduled desire-specific job exists in `scheduler.py`. Real, useful feature — but a different interaction model than what was scoped, not "done" as specified. |

**DESIRE real count: 4/5 confirmed-done (upgraded: S-346 gap since fixed), 1/5 partial (S-350 — downgraded from prior "confirmed" note; built as an on-demand per-candidate view, not the daily-digest-per-recruiter design the story specifies).**

## Summary of classification counts (28 stories)

- CONFIRMED-DONE: 6 (S-389, S-392, S-399, S-347, S-348, S-349) — plus S-346 now also confirmed-done (7 total)
- PARTIAL: 13 (S-379, S-387, S-388, S-390, S-391, S-393, S-397, S-398, S-400, S-350, plus BU-P&L-not-Location-P&L nuance folded into S-396's NOT-DONE call, S-350)
- NOT-DONE: 8 (S-380, S-381, S-382, S-383, S-384, S-385, S-386, S-394, S-395, S-396, S-401) — recount below
- CANT-DETERMINE: 0

Exact tally: **CONFIRMED-DONE 7** (S-346, S-347, S-348, S-349, S-389, S-392, S-399) · **PARTIAL 10** (S-379, S-387, S-388, S-390, S-391, S-393, S-397, S-398, S-400, S-350) · **NOT-DONE 11** (S-380, S-381, S-382, S-383, S-384, S-385, S-386, S-394, S-395, S-396, S-401).
