# Loaded automatically by `npm start` (CRA sets NODE_ENV=development).
# Ensures local dev NEVER silently talks to the real production backend.
# 2026-08-07: The launcher starts backend on 8080 regardless of config --
# frontend points here to match. Backend uses .env.local for local SQLite.
REACT_APP_API_BASE_URL=http://localhost:8080
