# CLAUDE.md — WROS Project Context

This file is read automatically at the start of every Claude Code session in this repo. If you are reading this, you are working on WROS (Workforce Revenue Operating System) for BlitzenX, a Guidewire specialist staffing firm.

## Read this, in this order, before writing any code

1. `docs/build-package/00-MASTER-INDEX.md` — the build order and what already exists
2. `docs/build-package/PHASE-1-GATE-STATUS.md` — **current, honest status of Phase 1** (see summary below)
3. `docs/build-package/REQUIREMENTS-CATALOG.md` — every requirement doc mapped to its epic/phase
4. `docs/build-package/01-SECURITY-FOUNDATION.md` through `04-RESOURCE-MANAGEMENT.md` — the four sequenced phases
5. `docs/build-package/WROS_Development_Review_Standard.md` — the hard-rule compliance checklist and build-brief template every story build must follow
6. The specific story's `.docx` for whatever you're currently building — its Business Rules and Acceptance Criteria sections are the authoritative behavior spec. **Location: `C:\Users\AvinashMukund\Documents\Claude\Requirements\`** (a separate folder outside this repo, not `docs/requirements/` — that path doesn't exist in-repo). Filenames are `S-XXX_HRMS-YYYY.docx`; the HRMS-ID is the stable identifier, S-numbers have real renumbering churn (some point at two different HRMS-IDs).
7. `C:\Users\AvinashMukund\Documents\Claude\WROS_Canonical_Backlog_S001-401.xlsx` — the authoritative Story-ID/WROS-ID/Status tracker (401 stories). Resolves the renumbering churn noted above: when a `.docx` filename and this sheet disagree on a story's real WROS ID, trust the sheet, verified by content (Summary/Description), not ID string alone — real, confirmed drift exists (e.g. Core-Pull is genuinely HRMS-0514/S-353, not HRMS-0312 as this file used to say and `04-RESOURCE-MANAGEMENT.md` still does). **Standing convention**: mark a row `Done` in this sheet (with a `Change Log` tab entry) only once the Definition of Done above is actually met (UI + API/integration + business rules + tests) — don't batch it, don't do a full historical reconciliation, just keep the going-forward stream current. A full regression pass over the remaining open rows happens once, at the end of the build.
8. `docs/build-package/PHASE-4-UI-INTEGRATION-COMPLETION-PROMPT.md` — **current active task**: Phase 4's backend (Core-Pull Engine, Specialty Pool Guard, Resource Management Agent, Confirmed vs Potential Demand Workflow) is built and tested but has no API or UI yet, so none of it is actually Done per the rule above. This is the story-by-story, top-down completion checklist — read it before starting any Phase 4 UI/API work.

## BlitzenX Operating Model (v1.0) — read this before touching Partner/BU Head/revenue-visibility/resource-ownership work

Given directly by Avinash, 2026-08-05, as the formal baseline: "Based on all of our discussions over the past several months, below is the current operating model as I understand it. This should become the baseline for your Python project." Written here close to verbatim — this is architecture, not prose, and precision matters more than paraphrase for a spec like this. Treat every rule below as load-bearing for EPIC-02 (Revenue Visibility), EPIC-03 (Workforce Planning), EPIC-05 (Resource & Bench Management), and the Partner ROI Agent — not background color.

**Purpose**: BlitzenX is not a traditional consulting company — it's multiple independent business units (P&Ls) sharing one corporate platform. Goal: independent growth engines, independent delivery orgs, zero unnecessary overhead, repeatable scaling, minimal founder dependency.

**Enterprise structure**:
```
CEO
│
├── AXION (Guidewire Services Practice)
│     Partner: Troy
│     Offshore BU Head: Hemant
│
├── PRISM (Guidewire Innovation / Accelerator Practice)
│     Partner: Curtis
│     Offshore BU Head: Manian
│
├── Corporate — Finance, HR, Marketing, Recruiting
│
└── Future Business Units
```

**CEO owns**: enterprise vision, capital allocation, executive hiring, strategic partnerships, M&A, product strategy, AI, enterprise governance, corporate marketing, enterprise sales assistance when required.
**CEO does NOT own**: daily delivery, resource allocation, sprint execution, practice operations.

**Every BU operates like an independent company** — owns its own revenue, gross margin, delivery, hiring, bench, talent development, client success, P&L. The only shared layer is corporate services.

**AXION** ("Adaptive eXecution & Integrated Operations") — Partner: Troy. Guidewire System Integration, Guidewire Managed Services, net-new carriers, practice growth, AI-enabled managed services. Supported by Hemant.
**PRISM** ("Product Integration & Systems Management") — Partner: Curtis. Net-new business ($5M ARR target), existing customer/account expansion, product integration, production support. Supported by Manian.

**Each Partner has exactly one offshore BU Head** — this is a real 1:1 structural pairing (Troy↔Hemant, Curtis↔Manian), not a many-to-many relationship. Matters directly for the `PartnerGoal`/scoring design below: there's one Partner and one BU Head per BU, each answering for a different half of the same number, not multiple Partners splitting one BU's target.

**Partner responsibilities**: commercial, customers, revenue, P&L, executive relationships, growth, practice strategy.
**BU Head responsibilities**: delivery, resource planning, hiring, quality, training, forecasting, bench, utilization, employee development, escalations.

**Delivery ownership rule — one of the most important rules in this spec**:
- **US work** (US onsite, US client-facing, executive consulting, architecture, sales, executive relationship) → **Partner owns delivery directly**. Does NOT route through offshore BU Head. Example: a Carrier CIO meeting goes to Troy, never to Hemant.
- **India/offshore work** → **Partner depends on BU Head**. Example: customer asks for 10 developers → Troy → Hemant → team. Partner does not manage daily offshore delivery.
- One-line rule: **US = Partner. India = BU Head.**

**Resource ownership**: resources belong to their Business Unit, not a global BlitzenX pool. Example: a developer belongs to AXION → Hemant, not to a shared pool. *Flag for whoever next touches Resource Management (EPIC-05)*: the existing `EmployeeAllocation`/bench_pool build should be checked against this — confirm employees are BU-scoped as this model requires, not treated as globally poolable, before extending that system further.

**Talent Engine — each BU owns its own SPECIALITY, TIP, and HTD engine. No sharing across BUs.**
- **SPECIALITY**: generates billable talent, protects economics, ensures zero idle cost. Acts as the gateway until the company reaches 150 employees.
- **HTD** (Hire → Train → Deploy): Java Developer → Training → Guidewire → Billable → Graduate.
- **TIP** (Talent Incubation Program): ~60-day training period, then one year billable inside Speciality, then graduates to eligible for carrier projects.
- *Flag*: HTD/Speciality/Delivery-Engine concepts already exist in this codebase (S-353 Core-Pull, S-373 Specialty Pool Guard, Buddy Program, `delivery_engine` field) — worth an explicit reconciliation pass against this spec's BU-ownership model next time that area is touched, not assumed compatible by default.

**Hiring pipeline**: every hire → assigned to a Business Unit → assigned to Speciality → billable → available → deployed.

**Deployment logic — no hiring just because a customer needs a resource; resources already exist.** Customer demand → Partner → BU Head → resource selection → deployment. Example: Builders Insurance needs 10 developers — instead of hiring, move someone off PwC onto Builders, 2-week notice, immediate deployment.

**Bench philosophy**: bench is NOT idle — it's already billable, protected by Speciality. Zero economic drag by design.

**Revenue ownership**: every client belongs to exactly one Partner. Example: Builders → AXION → Troy. Never shared.

**Client attribution locking — "one more major law," given directly 2026-08-05, real enforcement not just a convention**: a client's BU is permanently locked to whichever Partner's own organizational tree sourced it — **never manually reassignable across that boundary**. A client coming from Curtis goes to PRISM, never AXION. A client from Troy goes to AXION, never PRISM. A client sourced by Avinash himself goes to **Corporate**, not to either BU. If Troy later hires his own sales org (Sales Director → BDRs → AEs, per the Sales Phases section above), every client that org sources still rolls up to AXION — **the tree never crosses group**, no matter how many layers get added under a Partner. This is the authoritative rule for `Client.business_unit_id` assignment at creation time: derived from the sourcing user's own position in the org (their Partner, or Corporate if the CEO sources it directly), not a freely-editable dropdown a user can pick.

**Resource requests**: Partner receives the work. Offshore → BU Head fulfills. Onsite → Partner fulfills directly. Example: carrier needs a developer → Troy → Hemant → developer. Carrier needs an executive architect → Troy → Troy (stays with Troy, no handoff).

**Sales phases**: Phase 1 — Founder sells, Partner assists. Phase 2 — Partner sells, Founder supports. Phase 3 — Partner's own organization sells (Troy → Sales Director → BDRs → AEs), Founder runs the enterprise.

**Executive philosophy**: every executive owns an economic engine, never overhead. Hemant owns delivery economics. Troy owns AXION practice economics. Curtis owns PRISM economics.

**Corporate functions remain centralized**: Finance, HR, Legal, Marketing, Recruiting, Payroll, Accounting, IT, corporate brand.

**Partner KPIs**: Revenue, Gross Margin, Net New Logos, Customer Satisfaction, Practice Utilization, Practice Growth, Thought Leadership, P&L.
**BU Head KPIs**: Utilization, Hiring Velocity, Delivery Quality, Bench Health, Attrition, Training, Forecast Accuracy, Delivery Margin.

*These two KPI lists are the real spec for what `get_revenue_position()`/the Partner ROI Agent's weekly scoring needs to gather from each side — Partner-side inputs map to the Partner KPI list (revenue/logos/P&L-facing), BU Head-side inputs map to the BU Head KPI list (headcount/delivery/hiring-facing). See the Revenue Visibility Agent design entry below for how these get combined into one weekly score.*

**Scaling strategy**: 75 → 150 → 300 → 500 → 1,000 → 2,000 employees. Each growth stage adds new Business Units, not a larger centralized organization.

**Long-term vision**: a federated enterprise, not a traditional consulting firm. Each BU behaves like a self-contained company with its own Partner, offshore delivery org, talent engine, customers, and P&L; corporate provides governance, capital, brand, finance, legal, HR, and strategic direction. End state: the CEO is no longer the operational bottleneck — Partners build and scale their own businesses within the BlitzenX platform, growing from ~75 employees today toward 2,000+ by 2030 while keeping accountability, financial discipline, and entrepreneurial ownership intact.

## Where things actually stand (updated 2026-07-21 — read this before assuming anything is/isn't done)

**Phase 1**: 6 of 10 acceptance items fully closed and tested. 4 partial, each blocked on something a coding session can't provide (real DB/VPS access, a frontend build + product sign-off for MFA, or a deliberate git-history-rewrite decision) — full detail and exact next step for each in `docs/build-package/PHASE-1-GATE-STATUS.md`. Do not re-do this work; read that file first.

**Phase 2**: `Employee` (HRMS-0101+REV), `Client` (HRMS-0102), `Demand` (HRMS-0103) are built, tested, migrated. **Submission + Interview pipeline now built too** (HRMS-0711 + HRMS-0706, migration `f8a9b0c1d2e3`, 27 new tests, 140/140 suite passing) — `app/models/submission.py`, `app/models/interview_pipeline.py`, `app/services/submission_service.py`, `app/services/interview_service.py`. Built against the EPIC-01/Phase A doc track (same sprint sequence as Employee/Client/Demand), **not** the structurally different EPIC-07 "Talent Engine ATS Layer" docs that reuse the same HRMS-0703/0705/0706 numbers for a later-phase Kanban design — flag that fork again if anyone later touches this area from the EPIC-07 docs.

Compliance gates (R-01 experience, R-02/HRMS-P605 market profile, R-03/HRMS-P606 employment type) are enforced as **real hard blocks** in `submission_service.py` — but only the block. Explicitly deferred, by confirmed decision, not oversight:
- HRMS-P601's Director+BU Head sequential experience-override workflow (no Director role exists in this codebase's RBAC yet).
- HRMS-P605/P606's 3-strike/5-strike violation-escalation cascade (compliance_flags, account suspension) — `SubmissionViolation` rows are logged for every block, nothing reads them yet.
- HRMS-0440's full Thunder AI candidate ranking (HRMS-0710) — `submission_rank` is caller-supplied (e.g. from the existing `ATSScore.overall_score`), not recomputed.
- HRMS-0448 Outlook calendar matching — interview scheduling is manual (`scheduled_at` set directly), `scheduled_via_graph_event_id` stays null.
- HRMS-P608's L1-pass/L2-fail auto-reject + BU Head reversal workflow — R-05 itself (L1 must pass before L2) is enforced in `interview_service.create_interview()`, but a FAILed interview does not auto-cascade the submission's status.

Two new nullable-safe columns added to the legacy `candidates` table for this: `total_experience_months` (Integer, null = not-yet-verified = ineligible) and `employment_type` (fails closed to `UNKNOWN` for every existing row, same as C2C/1099). New tables `demand_interview_panels` / `submission_interviews` are deliberately NOT named `interview_panels`/`interviews` — this codebase already has legacy tables with those exact names (`app/models/user.py`, tied to the old Jobs/Candidate flow) — same non-retrofit precedent as Demand vs. Jobs.

**Also built**: HRMS-0709 (Client Record Management's non-CRUD half) — `assign_account_manager()` and `get_client_activity_timeline()` in `app/services/client_service.py`. This closes out the EPIC-01/Phase A sprint that Employee/Client/Demand/Submission/Interview all belong to.

**HRMS-0113 Notification Engine — now actually built** (`app/models/notification.py`, `app/services/notification_service.py`, migration `b2c3d4e5f6a8`, 15 new tests). Previously flagged as cited-but-not-built; fixed properly per Avinash's explicit instruction, not left as a TODO. Real, tested enforcement of all 3 business rules: BR-0113-01 (P0 fallback to a secondary channel within 60s, `FALLBACK_SENT` tracked), BR-0113-02 (cross-tenant dispatch rejected before send), BR-0113-03 (P1/P2 held outside the recipient's local business hours — default 08:00–20:00 — via real `zoneinfo` timezone math; P0 always bypasses this). `assign_account_manager()` in `client_service.py` has been retrofitted to call `send_notification()` instead of `EmailService` directly — the one call site that was bypassing it is now fixed, not just flagged again.

**Per-staff WhatsApp number routing for candidate conversations — new capability, not from any existing story** (`app/services/whatsapp_routing_service.py`, `Users.whatsapp_number`, migration `c3d4e5f6a7b9`, 18 new tests). Avinash asked for Recruiter/HR to each coordinate with candidates through their own WhatsApp number rather than the single shared tenant number every one of the 357 requirements docs describes (confirmed via full-corpus search — genuinely not covered by HRMS-0401/0402/0409/0410). Extends HRMS-0410's existing `CandidateConversation.owner_type`/`owner_id` ownership model rather than building a parallel system: when a specific recruiter owns a conversation, outbound sends resolve to *that* recruiter's own number (`Users.whatsapp_number`); falls back to `THUNDER_WHATSAPP_NUMBER` (env var, since this codebase's `candidate_ai.py` `tenant_id` is actually `users.UserID`, not the real multi-tenant `Tenant` table, so a DB column there would compound that pre-existing inconsistency rather than fix it). Unification is via the existing `candidate_id`/`conversation_id` pairing (this legacy `Candidate` table uses its own string ID scheme, not a literal UUID column — functionally equivalent for this purpose, flagged rather than silently pretended otherwise).

Building this surfaced and fixed a real pre-existing gap: **HRMS-0410 BR-01/R-08 ("Thunder must never send while a recruiter owns the conversation") was never actually enforced anywhere** in `app/services/ai_conversation_service.py` — the `owner_type`/`owner_id` fields existed but no `isAIOwner()`-style gate ever checked them, because no human manual-send path existed yet to conflict with Thunder's sends. `is_ai_owner()` and the gate inside `send_whatsapp_message()` are that enforcement, built for real now that a competing send path exists — not layered on top of a rule that still does nothing.

Per Avinash's explicit scope choice: this is the **routing/ownership layer only**, not a WhatsApp send/receive pipeline (the existing Thunder implementation in this codebase is actually email-only via Microsoft Graph today — `ai_conversation_service.py` has no WhatsApp code at all). The outbound WhatsApp call itself is an injectable stub (`ChannelNotConfigured` by default, reusing the same exception from `notification_service.py`) since no WhatsApp Business API credentials are provisioned — same posture as the Notification Engine's WhatsApp channel. A caller can inject a real sender once that integration exists; the routing/gating logic doesn't need to change.

**Conversation inactivity safety net — new capability, built on top of the WhatsApp routing work above** (`app/services/conversation_inactivity_service.py`, `Candidate.timezone`, migration `d4e5f6a7b8c0`, 12 new tests). Worked through with Avinash conversationally before building — see session notes for the reasoning, summarized here:

One shared 30-hour clock (`INACTIVITY_THRESHOLD_HOURS`), continuous wall-clock time paused only Friday 21:00→Monday 09:00 in the candidate's local timezone (explicit choice, optimizing for "don't lose the candidate" over a stricter business-hours-only count). Whichever side's message is most recent decides the effect:
- **Candidate's last message unanswered by the owning human** → Thunder reclaims ownership (logged as `ai_auto_reclaimed_ownership`, deliberately distinct from a manual hand-back so HRMS-P612's human-dependency tracking can tell them apart), notifies the previous owner via the Notification Engine, and immediately sends the candidate a check-in.
- **Staff's last message unanswered by the candidate** → an automated nudge fires, attributed as the current owner's own message (their explicit call: "it goes as the recruiter is sending it") — but tagged `auto_generated=True` on the event internally, so R-06/HRMS-P612 metrics don't get inflated by sends that only *look* manual. If Thunder already owns, the nudge goes out as Thunder directly.
- The resulting action only actually fires within a 09:00–21:00 candidate-local window (reuses `notification_service`'s timezone-conversion helpers, now generalized to accept a configurable window instead of only its own 08:00–20:00).

Not wired to a scheduler — `evaluate_conversation_inactivity()` is the per-conversation function a cron would call across every open conversation, same posture as every other scheduled-job story this session.

Two real, deliberate gaps, not oversights:
- **WhatsApp Business API and SMS gateway are not provisioned in this codebase** (S-211's own prerequisites list this as required-but-missing) — their channel-sender functions raise `ChannelNotConfigured` by default. The dispatch/fallback *state machine* around them is fully real and tested (a caller can inject real senders via `channel_senders=` once those integrations exist — no logic changes needed then).
- **`Users.timezone`** (new nullable-safe column, default `"Asia/Kolkata"`) stands in for HRMS-0121 (Locale & Currency Config), which doesn't exist in this codebase — same pattern as other flagged doc-vs-reality gaps this session.
- The in-app notification bell/nav-shell UI (depends on HRMS-0111, not built) and the actual cron wiring for `release_pending_notifications()` are not built — the backend functions a future UI/cron would call (`get_unread_count()`, `mark_as_read()`, `release_pending_notifications()`) are real and tested; only the UI and the scheduler registration are deferred, consistent with every other Phase 2 entity this session (model/service/test layers only, no REST endpoints anywhere yet).

**Also built (this round)**: `EmployeeAllocation` (HRMS-0507 — deliberately just the "allocate/end-allocation" data primitive, not Phase 4's Core-Pull/bench-agent machinery, which `docs/build-package/04-RESOURCE-MANAGEMENT.md` confirms really is Phase 4, not a Phase 2/Domain 3 mislabel) and `Timesheet`/`TimesheetEntry` (HRMS-0901 Submission + HRMS-0902 Approval, Phase 2 Domain 4) — `app/models/employee_allocation.py`, `app/models/timesheet.py`, `app/services/employee_allocation_service.py`, `app/services/timesheet_service.py`, migration `a1b2c3d4e5f7`. 20 new tests, 167/167 suite passing.

Real enforcement: BR-01 (60h/week hard cap), BR-02 (no submission >4 weeks stale), BR-03 (one timesheet per employee+allocation+week, UNIQUE), BR-04 (bench employees — no active allocation — never get a timesheet), plus HRMS-0902's approved-is-immutable and rejected-returns-to-draft-on-explicit-reopen. `EmployeeAllocation.demand_id` (not `project_id` as 02-DATA-MODEL.md's sketch names it) — this codebase has no `projects` table yet (Domain 4/EPIC-08, not built), same flagged-not-retrofitted posture as the existing Demand-vs-Jobs note.

**Domain 4 continued — Opportunities (HRMS-0207/0209/0210/0211/0215) built** (`app/models/opportunity.py`, `app/services/opportunity_service.py`, migration `e5f6a7b8c9d1`, 17 new tests). Same session, working toward "finish Phase 2 before Phase 3" per Avinash's direction (2026-07-21). Key finding along the way: **HRMS-0201 ("Client Entity Management v2") is a genuine fork of the already-built HRMS-0102 Client model** — different fields, different contact-role vocabulary, notes routed through a nonexistent Activity Timeline table instead of the existing `client_history`. Since Client is already load-bearing (Demand/Submission/EmployeeAllocation all FK to it), extended it rather than forking: added the one genuinely new field (`country`), reused `billing_currency` for HRMS-0201's "default_currency", and added the one real new business rule — `set_primary_contact()` in `client_service.py`, exactly-one-primary-contact enforced transactionally. The contact-role vocabulary mismatch (existing HIRING_MANAGER/TECHNICAL_PANEL/etc. vs. the new doc's Decision Maker/Day-to-Day/Finance) was left as-is — a product naming call, not resolved silently, not asked about again since it's not hard-rule-adjacent.

`Demand` extended (not forked) with `opportunity_id`, `source_type` (tag, not a schema branch — same pattern as everywhere else), `duration_hours`, `revenue_potential_usd_cents` — `create_role_demand_from_opportunity()` reuses the existing `demand_service.create_demand()` path rather than a new insert. `calculate_weighted_forecast()` is built as the one shared function HRMS-0209 specifies every dashboard should call (none of those dashboards/UI exist yet, consistent with the no-REST-endpoints-anywhere state of this whole build).

**HRMS-0904 (Timesheet Dispute Resolution) + HRMS-0905 (Timesheet Analytics & Compliance) built** (`app/models/timesheet_dispute.py`, `app/services/timesheet_dispute_service.py`, `app/services/timesheet_analytics_service.py`, migration `a7b8c9d0e1f3`, 17 new tests). BR-01 honored literally: resolving a dispute (even ADJUSTED) never mutates the original Timesheet/TimesheetEntry rows — the adjustment lives entirely on the dispute record, since `revenue_records`/`invoices` don't exist yet for the fuller propagation the doc describes. `calculate_utilization_pct_phase2()` returns `None` (not a fake 0%) when no approved timesheets exist in the period, since HRMS-0510's actual Phase 1 fallback function doesn't exist in this codebase either.

**Projects built** (`app/models/project.py`, `app/services/project_service.py`, migration `c9d0e1f2a3b5`, 19 new tests): HRMS-0801 (lifecycle, incl. auto-creation when an Opportunity transitions to WON — completes the hook `opportunity_service.transition_stage()` deferred until Project existed), HRMS-0804 (milestones, `delay_days` always computed at completion time, never a caller-supplied value), HRMS-0805 (unfilled-role gap detection, OPEN/URGENT/OVERDUE, derived fresh on every call per the doc's own "never stored" note), HRMS-0806 (revenue/margin estimate — always labeled "Estimate only, not a finance figure," returns `INSUFFICIENT_DATA` rather than a misleading number when billing rate/end date/employee cost data is missing). HRMS-0803 (resource assignment) reused the already-built `EmployeeAllocation` (`utilization_pct`/`client_reporting_manager_contact_id` already meant exactly what HRMS-0803 calls `allocation_pct`/reporting-manager — no new fields needed there, just `project_id`/`role`) — but its overlapping-allocation rule required **relaxing a stricter constraint this session had built earlier**: `allocate_employee_to_project()` originally hard-blocked *any* second concurrent allocation outright. HRMS-0803 wants partial concurrent allocations (e.g. 60%+40% across two projects) to be allowed as long as they sum to ≤100%. Resolved via an explicit `allow_concurrent` opt-in flag (default `False`, preserving every existing call site's behavior unchanged and byte-for-byte passing the original tests) rather than silently changing established behavior — real partial-allocation capacity checking (`AllocationOverCapacity`) only activates when a caller opts in.

**Not built**: HRMS-0807 (Project Risk Flagging) — it's specified to only ever *display* HRMS-1107's health score (a Phase 3 EPIC-11 agent that doesn't exist), never recompute one; there's nothing to display yet, and building a second scoring formula would contradict that story's own anti-duplication rule.

**Invoice + Revenue Leakage + Reconciliation built** (`app/models/invoice.py`, `app/models/revenue_leakage.py`, `app/services/invoice_service.py`, `app/services/revenue_leakage_service.py`, migration `e2f3a4b5c6d7`, 16 new tests). HRMS-0907 (`generate_invoice()`): R-10 ("unapproved timesheet blocks invoice generation") enforced directly — every `Timesheet` whose allocation is scoped to the project and whose week falls in the billing period must be `APPROVED`, no partial invoicing around a stuck one; also extends HRMS-0904's BR-02 by reusing `has_open_dispute()` as a second hard gate, rather than a parallel check. Status lifecycle DRAFT→APPROVED→SENT→PAID, each transition explicit — "Sent" is never automatic even after Finance approval, per that story's own "Not In Scope" note. HRMS-0906 (`scan_project_revenue_leakage()`): compares approved-vs-invoiced hours for a project+period, only flags once a grace period has elapsed (never mid-period); BR-0906-02's `partial_billing_reason` suppresses a flag from `get_active_leakage_flags()` while the row persists for audit. HRMS-0903 (`find_reconciliation_gaps()`): every `APPROVED` timesheet past a grace period with no `InvoiceLineItem` is a reconciliation gap — HRMS-0605's `TimesheetRevenueProcessor` re-processing step doesn't exist in this codebase, so gaps go straight to `ReconciliationAlert` rather than attempting an automatic re-process first. PDF export and automatic invoice sending are explicitly out of scope (reporting/frontend concerns, consistent with the no-REST-endpoints-anywhere state of this build). 325/325 suite passing (1 xfailed) after this round.

**Anomaly Detection + Client Revenue Dashboard built** (`app/models/timesheet_anomaly.py`, `app/services/timesheet_anomaly_service.py`, `app/services/client_revenue_dashboard_service.py`, `Project.allow_weekend_billing`, migration `f3a4b5c6d7e8`, 12 new tests) — **this closes out Domain 4, and Phase 2 in full except for the explicitly-skipped Domain 6** (see below). Read straight from the real source docs (S-229/S-228), not from prior summarized notes, since both turned out thinner/different than expected: HRMS-0910 ("AI Time Entry Anomaly Detection") only formally numbers 2 of its 4 checks as BRs — BR-0910-01 (flags are advisory, never block approval — `scan_timesheet_anomalies()` is deliberately never called from `timesheet_service.submit_timesheet()`/`approve_timesheet()`, so a flag can't block anything by construction) and BR-0910-02 (weekend flagging respects the new `Project.allow_weekend_billing`); the >12h/day, COMPLETED-project, and duplicate-entry (same employee+project+date, across different timesheets) checks have no numbered rule in the doc itself — implemented per its plain-language description, not invented further. Its own title says "AI" while its "Not in Scope" section explicitly disowns any AI/behavioral-risk model — a contradiction in the doc, not resolved silently, flagged here. HRMS-0909 ("Client Revenue Realization Dashboard") is pure aggregation, no new schema — `get_client_revenue_dashboard()` computes earned-vs-planned (invoiced revenue vs. WON-opportunity `revenue_value_usd_cents` for opportunity-linked projects), billable ratio (`billable_hours/total_hours` across approved timesheets, `None` on zero-hours rather than a divide-by-zero or fake number — the doc itself has no BR covering this gap), and burn rate (FIXED_BID projects only, per the doc's one line about it — actual invoiced revenue against what the elapsed-timeline fraction alone would predict, `None` for non-fixed-bid or missing dates, since the doc gives no formula at all beyond that one scoping sentence). 337/337 suite passing (1 xfailed) after this round.

**Domain 5 (Sub-Vendor Portal) — core pipeline built** (`app/models/sub_vendor.py`, `app/models/sub_vendor_submission.py`, `app/services/sub_vendor_service.py`, `app/services/sub_vendor_submission_service.py`, migration `b8c9d0e1f2a4`, 20 new tests). Doc conflicts resolved with engineering judgment (greenfield, nothing existing to defer to, unlike the Client fork): `sub_vendor_accounts` chosen over the competing `sub_vendors` name; registration-approval `status` and ongoing `compliance_status` modeled as two separate fields (genuinely different concerns, not one to pick between); `sub_vendor_requests` supersedes P804's thinner `demand_subvendor_assignments` sketch (needs deadline/max_candidates for the governance-agent and clarification-Q&A stories that depend on it); P806→P807→P808's staged gate flow (submission → FT-only gate → dedup → recruiter review → only then `create_candidate_safe()`) was built as authoritative over P802's conflicting "create immediately" language.

Real enforcement, reusing existing logic rather than reimplementing: the FT-only gate reads the same allowed-value convention as `candidates.employment_type`; dedup calls the actual `find_duplicate_candidate()` R-07 function (per BR-0807-01's explicit "one dedup path" requirement) — not a parallel matcher; Accept always goes through `create_candidate_safe()`, never a direct insert; dedup rejections are tracked separately from FT-only violations (BR-0807-03) so a vendor is never penalized for something outside their control; the 3-strike/5-strike compliance escalation sets `SUSPENSION_PENDING`, never auto-jumping to `SUSPENDED` (BR-0811-02 — the one deliberately non-automatic step, requiring `confirm_suspension()`). Added `Candidate.source_channel`/`vendor_id` (HRMS-P816, immutable sourcing attribution) and completed the previously-deferred `Submission.subvendor_id` FK now that `sub_vendor_accounts` exists.

**Domain 5 continued — vendor tracking, scorecard/analytics, and clarification Q&A built** (`app/services/sub_vendor_tracking_service.py`, `app/services/sub_vendor_qa_service.py`, `ClarificationQA` model, migration `d0e1f2a3b4c6`, 9 new tests). HRMS-P803/P810 (`get_submissions_for_vendor()`) and HRMS-P805/P812 (`get_sub_vendor_scorecard()`/`get_sub_vendor_portfolio_analytics()`) needed zero new schema — pure read-only queries over what HRMS-P801/P806/P807/P808 already built, confirming `candidates.source_channel=SUBVENDOR` (HRMS-P816) is exactly what P812's contribution metric reads. HRMS-P814's `get_qa_for_request()` deliberately has no `sub_vendor_id` filter (unlike `get_submissions_for_vendor()`'s isolation boundary) — shared visibility across every vendor on the same request is the actual point of BR-0814-01, not an oversight.

**Domain 5 now complete.** HRMS-P809/P818 (Thunder onboarding + ATS integration confirmation) were built as exactly what their own "Not In Scope" sections call for — verification, not new capability: `tests/test_sub_vendor_ats_integration.py` proves functionally (not just by grep) that R-01 (experience gate) and R-05 (L1-before-L2) apply identically to a `source_channel=SUBVENDOR` candidate as a `DIRECT` one — confirmed no branching exists anywhere in `submission_service.py`/`interview_service.py`/`candidate_service.py` (the former only carries `source`/`subvendor_id` as pass-through data on the `Submission` row, never as a conditional). Found a significant cross-cutting gap while researching it: **`createCandidateSafe()` — the one sanctioned candidate-creation path R-07 and dozens of stories across the entire 357-doc corpus depend on — does not exist anywhere in this codebase.** This needs to be built as its own piece (real dedup matching on email+phone, the actual creation path) before Sub-Vendor Portal (or anything else that assumes it exists) can be built against something real rather than a fiction. Also found: real internal inconsistencies in the Sub-Vendor doc cluster itself (stale S-number cross-references drifting by one ID around P803/P804/P805; two conflicting table names `sub_vendors` vs `sub_vendor_accounts` for the same entity; two conflicting `status` fields for registration-approval vs. compliance-standing) that need explicit decisions before building, not silent reconciliation.

**Domain 6 (Internal Collaboration, EPIC-14/15, S-379–386) does not exist in the requirements corpus at all** — verified four independent ways (no matching files, no EPIC-14/15 tag anywhere in any doc, no matching keyword cluster, corpus literally stops at S-378). `02-DATA-MODEL.md`'s claim that these stories are "fully specified" does not hold up. This isn't a search failure to retry — there is nothing to build against. Options if this needs to happen anyway: confirm with whoever maintains the Requirements folder whether these docs exist in an unreceived drop, or accept building from `02-DATA-MODEL.md`'s own schema sketch alone (which it explicitly says to treat as non-authoritative) with hand-written business rules.

**Not built, by design**: the actual Monday-6am/daily-reminder cron jobs both stories describe (the idempotent functions they'd call exist; scheduling them is follow-up work), the `timesheet.approved` event publish (no event bus in this codebase), and role-gating on who may approve (API-layer concern, no REST endpoints exist for this domain yet).

**Phase 2 is now complete except for Domain 6** (explicitly skipped, doesn't exist in the requirements corpus, see below). **Still not built anywhere in Phase 2**: REST endpoints + UI for any entity built this session (model/service/test layers only, a consistent gap across literally everything), and Convert-Candidate-to-Employee (HRMS-0708, depends on `offers`/`preboarding_documents` tables that don't exist yet).

**Security incident (from earlier this session, mostly resolved)**: `.env` was committed to this repo's history since Jan 2026. Credentials rotated (DB password, Azure AD secret, Gemini key handed off to VPS admin), `.env` untracked going forward, secret redaction added to all logging, full git-history grep performed. Two new secrets generated locally this session that still need deploying to the VPS: `WEBHOOK_SHARED_SECRET` and `FIELD_ENCRYPTION_KEY` (32 raw bytes, base64-encoded, for AES-256 employee bank-field encryption) — check with Avinash whether these have been added to the real `.env` yet.

## Architectural decisions made this session — follow these, don't re-litigate them

- **Production database is SQL Server** (`mssql+pyodbc`), not PostgreSQL — despite every requirements doc being written in Postgres syntax (native `ENUM`, `JSONB`, `GIN` indexes). Translate every spec: `Enum(*values, native_enum=False, create_constraint=True)` for a portable VARCHAR+CHECK (note: `create_constraint=True` is required explicitly — SQLAlchemy 2.0 does NOT add the CHECK constraint by default even with `native_enum=False`), `Text` instead of `JSONB` (JSON-encoded strings), no GIN index (use a standard index on the realistic query column instead).
- **New Phase 2 entities use UUID primary keys as `String(36)`** (`str(uuid.uuid4())`), not SQL Server's native `UNIQUEIDENTIFIER` or Postgres's UUID type — a plain string round-trips identically on SQLite (every test in this repo runs against throwaway SQLite) and SQL Server. **Existing tables (`Candidate`, `Users`, `Jobs`) keep their original custom string IDs** — do not retrofit them to UUID, that was an explicit decision, not an oversight.
- **New tables (`employees`, `clients`, `demands`) are genuinely new**, linked to existing tables via FK (e.g. `employees.candidate_id` → `candidates.candidateID`), not retrofitted into `Candidate`/`Jobs`. Note: `demands` vs. the existing `Jobs` table is an **unresolved open question** — they serve overlapping purposes and haven't been reconciled. Don't silently pick a direction; flag it.
- **Every table gets `tenant_id`, nullable-for-now** (existing rows backfilled to a single "BlitzenX" tenant via migration `d6e7f8a9b0c1` — every *new* tenant-scoped table should follow the same nullable pattern, matching HRMS-0109's safe-upgrade precedent). Real tenant scoping is only wired into 6 high-risk list/search routes so far — see `docs/build-package/HRMS-0109-tenant-scoping-gap.md` for the ~180 remaining lower-priority (IDOR-shaped) call sites.
- **Testing convention, always**: throwaway SQLite (`tempfile.mkstemp`), never the real database. For anything touching JWT, generate a throwaway RSA key pair per-test and monkeypatch `app.core.security.PRIVATE_KEY`/`PUBLIC_KEY` — never rely on real signing keys in a test. For anything touching a real LLM (Gemini), mock the client — never make a real API call from a test.
- **MFA is fully built (TOTP, backup codes, enrollment/verify, pending-token isolation) but enforcement is OFF by default** (`MFA_ENFORCEMENT_ENABLED` env var). Do not turn it on until the frontend has a screen for it — there's no UI to complete a TOTP challenge yet, and flipping it on would lock out every `Super User`/`BU Head` account. Also: that role mapping (`Super User`/`BU Head` standing in for the docs' "Admin and Director") is this session's best-guess analog, not a confirmed decision — the real RBAC role list has neither name.
- **`AuthenticationMiddleware` and `RateLimitMiddleware`**: rate limiting IS enabled (`app/main.py`). Full JWT-enforcement middleware is NOT enabled — still per-route `Depends()` only. Two real bugs were found and fixed in `AuthenticationMiddleware` (a prefix-matching bug that would have made every route public if ever enabled, and a stale public-routes list) but it's still not wired into `app.main` — needs someone who can watch real production traffic after enabling it.

## R-07 — createCandidateSafe() now actually exists (2026-07-21)

CLAUDE.md's own non-negotiables list has said "createCandidateSafe() is the only path to create a candidate" all session — but the function itself was never built anywhere in this codebase until now (confirmed by grep while researching Sub-Vendor Portal: zero hits). `app/services/candidate_service.py` builds it for real: `find_duplicate_candidate()` checks email, phone, and LinkedIn **each independently** (added `Candidate.linkedin_url`, a genuinely new field — the Dev Review Standard's own worked example of this exact gap is "missing phone/LinkedIn"), and `create_candidate_safe()` is the one creation path, raising `DuplicateCandidateError` rather than inserting a second row.

**The two pre-existing direct-insert call sites were retrofitted, not left bypassing it**: `app/api/v1/endpoints/onboarding.py`'s HR candidate-creation endpoint and `app/api/v1/endpoints/create_job.py`'s public job-application endpoint both previously built `Candidate(...)` directly and deduped on email only via `app.core.database.check_candidate()` (exactly the gap the Dev Review Standard describes as its own example). Both now call `create_candidate_safe()`; external behavior preserved exactly (create_job.py still returns a 200 "Already Applied" response, onboarding.py still raises 400) — only the creation/dedup path changed. Full test suite (241/241, including the real-app-import check) confirms no regression.

**Still open**: `sub_vendor_dedup_rejections`-style matching (fuzzy/AI matching, cross-referencing sub-vendor submissions) is explicitly NOT what R-07's dedup does — R-07 is exact-match only on email/phone/LinkedIn, by design, matching the Sub-Vendor Portal docs' own "no fuzzy/AI-based name matching" note. `createCandidateSafe()` is now real enough to build Sub-Vendor Portal (HRMS-P801-P818) against, which was blocked on this.

## Environment setup (so you don't have to rediscover this)

- Python 3.11 is installed at `C:\Users\AvinashMukund\AppData\Local\Programs\Python\Python311` (was NOT there before this session — installed via winget). Not on PATH by default in a fresh shell; prepend it, or use the venv's python directly.
- A working venv with all dependencies (including `requirements-dev.txt`'s `pytest`, plus `pyodbc`, `cryptography`, `pyotp`, `python-docx`) already exists at `C:\Users\AvinashMukund\Documents\Claude\OnboardingModule-Backend-main\.venv` — **note the `-main` suffix**, it's a sibling folder to this repo, not inside it (an artifact of an earlier zip-extracted copy; the actual git repo is `OnboardingModule-Backend` without the suffix). Run tests with that venv's `python.exe -m pytest tests/ -v` from within this repo's directory.
- Do not install `pyodbc`'s underlying ODBC system driver or attempt a real DB connection from a dev/test session — the pip package is only needed so `app.core.database` *imports* cleanly (SQLAlchemy needs the DBAPI module to construct the dialect object, even though `create_engine()` itself is lazy and never actually connects until a query runs).

## The one-sentence version of every rule that matters

Ten hard business rules (R-01 through R-10) plus one financial rule (R-11) are absolute and enforced in code, not policy — see the Development & Review Standard for the full checklist and the specific "how it silently fails" pattern to test against for each. If you are ever unsure whether something you're building touches one of these, assume it does and check.

## Non-negotiables, regardless of which story you're building

- **`createCandidateSafe()` is the only path to create a candidate.** No direct inserts, anywhere, ever.
- **`sendThunderMessage()` is the only path to send a candidate message.** No raw WhatsApp/email/LinkedIn API calls from any story.
- **`sendNotification()` (HRMS-0113) is the only path to send any internal notification.** It handles business-hours gating, tenant scoping, and channel fallback — do not reimplement any of that in an individual story.
- **Core-Pull decision logic lives in exactly one place: HRMS-0312.** Every story that touches Core-vs-Speciality staffing calls it or checks its output. None reimplement it.
- **Every table has `tenant_id`, NOT NULL, indexed. No exceptions.** Middleware resolves it from session, never from client input. (In practice today: nullable-for-safe-upgrade until backfilled, per the architectural note above — the end state is still NOT NULL.)
- **Every monetary value is `BIGINT`, USD cents, named `*_usd_cents`.** No second-currency column, anywhere, in any story, for any reason.
- **LLM output is advisory for anything irreversible.** An agent proposing an action and a human confirming it are two different code paths — do not collapse them because it seems more efficient.
- **Any LLM prompt assembled from user-supplied content goes through `app.core.llm_prompt_safety.build_safe_prompt()`.** Established this session after finding the actual vulnerable pattern (raw f-string concatenation) in `ai_conversation_service.py`'s real Gemini call sites — both retrofitted. Every future LLM-calling story reuses this, does not hand-roll its own prompt concatenation.

## What already exists — check before building anything that sounds new

- **357 requirement docs** (`Requirements/` folder, see catalog above) — most already have complete, audited content. Don't assume something needs a `[GAP-SPEC]` stub without checking; two areas the phase docs call "no doc yet" (Nurture Engine / P3xx, Analytics / 12xx) already have full docs in practice.
- **15 EPIC-16 Finance & Accounting stories** (S-387 onward) — timesheet nag cascade, invoice cycle, AR follow-up, bank reconciliation, cost/rate engines. Some still pending (P&L Engine, Reserve Fund, Hiring Affordability, Partner Incentive Calculator, Executive Dashboard, GST placeholder).
- **This codebase (`OnboardingModule-Backend`)** already implements rough versions of Add Candidate, Schedule Interview, Start Pre-Onboarding, Collect Document, plus (as of this session) tenant isolation, audit logging, MFA, rate limiting, prompt-injection protection, and the Employee/Client/Demand entities. Extend it, don't discard it.
- **94 stories have no written requirements yet.** If you're asked to build something in Client Portal legacy screens, Resource & Bench Management basics, Boolean Search, Interview Decision Engine, LinkedIn Sourcing, Talent Engine ATS, Analytics, or Nurture Engine — check the requirements catalog and `Requirements/` folder first, most of these areas turned out to already have docs. If genuinely not, flag it rather than inventing scope, unless a `[GAP-SPEC]` minimum-viable stub is given in the relevant phase document.

## Build order

Phase 1 (Security) → Phase 2 (Data Model) → Phase 3 (Thunder + Agentic Layer, Part A single-threaded then Part B's four parallel workstreams) → Phase 4 (Resource Management, in parallel with Phase 3's other workstreams). EPIC-16 (Finance) can build in parallel with Phase 3/4 once Phase 1 and Phase 2 are complete, since it depends on Payroll Sync (Phase 2/Integration Hub) and the Notification Engine (Phase 1) but not on Thunder or Resource Management directly.

Do not skip ahead. A phase assumes the prior phase's acceptance gate has actually passed, not just that the code exists. **Phase 1's gate has NOT fully passed** (see status above) — Phase 2 work is proceeding anyway with Avinash's explicit sign-off given the 4 remaining Phase 1 items are infrastructure/product-decision-blocked, not code-blocked. This is a deliberate, confirmed exception, not a precedent for skipping gates generally.

## When something is ambiguous

Check the specific story's requirements doc first (`Requirements/` folder). If the story doesn't exist yet, check the phase document for a `[GAP-SPEC]` stub. If neither exists, stop and ask — do not invent a plausible-sounding design for a hard-rule-adjacent story (anything touching R-01 through R-11, tenant isolation, or Core-Pull) without confirmation. This session found real value in stopping to ask: the SQL Server vs. Postgres mismatch, the UUID-vs-string-ID mismatch, and the MFA role-mapping question were all surfaced and resolved this way rather than guessed at. For lower-stakes ambiguity (a UI label, a non-critical config default), make a reasonable choice and note the assumption.

## Session log — 2026-07-22: Phase 2 closed out, Phase 3 Part A built, two legacy hard-rule gaps closed

**Phase 2 formally closed** (`docs/build-package/PHASE-2-GATE-STATUS.md`) — Domain 1-5 done and tested, Domain 6 confirmed not buildable (doesn't exist in the requirements corpus), two carry-forward blockers from Phase 1 (tenant_id NOT NULL, audit_log grant-level append-only) explicitly not re-solved here, same as Phase 1's own honest gate status.

**Phase 3 Part A built** — `app/services/thunder_service.py` (`build_candidate_context()`, `send_thunder_message()` wrapping the pre-existing R-08 gate in `whatsapp_routing_service` with consent + debounce added) and `app/services/orchestration_router_service.py` + `app/models/orchestration.py` (HRMS-1101's deterministic conflict router — 2 concrete seeded rules per the actual story doc, not the phase-doc's rounded "six," fail-open on internal error per BR-1101-06). `conversation_inactivity_service.py` retrofitted to call `send_thunder_message()` instead of bypassing it. Full status: `docs/build-package/PHASE-3-PART-A-GATE-STATUS.md` — 3 of 4 gate items pass; `buildCandidateContext()`-as-mandatory-precall can't be verified yet because no Thunder response-generation path exists (that's Part B's job).

**Two of the three real, pre-existing hard-rule gaps this session's CLAUDE.md already flagged are now closed:**
- **R-01 at candidate creation** (`app/services/candidate_service.py`: `parse_experience_to_months()`, `enforce_experience_gate_at_creation()`) — reuses `submission_service.check_experience_eligibility()` rather than a second implementation (the exact "LinkedIn sourcing built three times" mistake this project already learned once). Populates `candidates.total_experience_months` from the free-text `candidateExperience` field at creation time — that column previously existed but was **never populated by any code path**, meaning the submission-time R-05... R-01 gate in `submission_service.py` was silently unpassable for every candidate. BU Head override requires a real `Users` row with `UserRole == "BU Head"` (verified server-side) plus a reason, logged via `write_audit_log(action="hard_rule_override")`. Wired into `onboarding.py`'s `create_candidate` endpoint specifically — **deliberately not** inside `create_candidate_safe()` itself, so `create_job.py`'s public application flow and Sub-Vendor Portal's Accept flow aren't silently blocked by a scope decision that wasn't asked for; flagged as an open question whether R-01 should apply there too.
- **R-05 on the legacy interview system** (`app/services/interview_sequencing_service.py`, wired into `interviews.py`'s `create_interview_panel`) — this legacy `InterviewPanel`/`Interview`/`InterviewFeedback` system (the original "Schedule Interview" feature) has no L1/L2 concept in its schema at all, only free-text `round_name`. Generalized per the Dev Review Standard's own wording ("any round 2+") to an ordinal gate: a candidate's next panel is blocked until their most recent prior panel has an interview marked Completed with at least one "Hire" feedback and zero "Reject" (fail-closed on any negative signal). **This aggregation rule is this session's judgment call, not a confirmed product decision** — no existing code anywhere aggregates `InterviewFeedback.recommendation` into a round-level pass/fail, so there was nothing to reuse; flagged for whoever owns the interview process to confirm or override.
- **HRMS-0118 virus-scan gate** also closed this session (`app/services/virus_scan_service.py`) — `is_virus_scanned`/`virus_scan_result` now actually get set on upload (`documents.py`'s `_upload_document_helper`) and gate the `/view` endpoint (fail-closed: unset/error/infected all block, only an explicit "clean" unlocks). No AV vendor is provisioned in this codebase; the injectable `scanner_client` defaults to a stub that raises, recorded as `"error"`, same posture as WhatsApp/SMS's `ChannelNotConfigured`.

**Frontend (`OnboardingModule-Frontend-main`) touched for the first time this session** — it had no git history at all; initialized and baseline-committed before any edits. Wired the two UI surfaces the above gates actually need: `CandidateCreate.js` now handles the R-01 400 (shows a BU Head override panel instead of just failing) and `DocumentsTab.js` now shows scan status and blocks viewing client-side before hitting the backend's 403. Everything else in Phase 3 (Workstream 1 Recruit's agents, Workstream 4 Resource Management) has no REST surface yet, so there's nothing else to wire on the frontend until those exist.

**Still open in Phase 3** (as of this entry, 2026-07-22 morning): Workstream 1 (Recruit — HRMS-1102/1103/1104, HRMS-0527 Curtis Rule), Workstream 3's remaining stories (S-364 Buddy Program, S-365 Graduation Gate, S-351 Delivery Engine Assignment, S-360 HTD gates), Workstream 4 (Resource Management, genuinely Phase 4 scope per `04-RESOURCE-MANAGEMENT.md` even though Phase 3 Part A unlocks it in parallel). Not started this session.
**[CORRECTION, logged in the next session log entry below]: this list went stale immediately after this entry** — commits `c6e4048` through `fed8d7a` closed out Workstream 1, Workstream 2 (HRMS-1501/1502 deferred to Phase 3.5), and Workstream 3 in full, without a matching CLAUDE.md update each round. Only Workstream 4 (Resource Management) is genuinely still open. Don't trust this bullet — check git log or the entry below instead.

**New backlog item, explicitly deferred to the end**: migrating 100K+ candidate records (plus notes, interview history, candidate history) from JobDiva into WROS to sunset JobDiva — see memory file `jobdiva_migration_backlog.md` in the Claude Code memory store for the full context. Blocked on real JobDiva API docs/credentials; the public URL Avinash gave rendered as an empty page with no actual API reference.

411/411 tests passing (1 xfailed) after this session's rounds.

## Session log — 2026-07-22: Thunder reply-generation + "Test Thunder" chat mode (CEO-facing)

**The missing piece from Part A closed**: `app/services/thunder_service.py` gained `generate_thunder_reply()` — the actual reply-generation function that was explicitly out of scope for Part A (`build_candidate_context()`/`send_thunder_message()` only). Reuses Gemini via `langchain_google_genai.ChatGoogleGenerativeAI`, same model family and `GEMINI_API_KEY` env var as `ai_conversation_service.py`'s existing candidate-facing LLM call — not a second LLM client. Persona is Avinash's explicit choice (asked directly rather than guessed, per his standing instruction on ambiguous persona/tone calls): reuses `AI_AGENT_PERSONA = "friendly-hr"`, the same voice already established in the email templates, rather than inventing a separate "Thunder" voice. Every call routes the candidate's inbound message through `build_safe_prompt()` (Phase 1 B5), same discipline as every other LLM call site in this codebase.

**"Test Thunder" mode built** — lets any logged-in internal user (any role, not gated behind a specific permission) chat with Thunder without a live WhatsApp Business API:
- `mock_whatsapp_client()` — an honest mock transport (logs and reports delivered, never calls a real API), injected as `send_thunder_message()`'s `whatsapp_client` so R-08, consent, and the 60s debounce all still run for real; only the wire transport at the bottom is fake.
- `get_or_create_test_candidate()` — bootstraps the one `whatsapp_outreach` `ConsentRecord` `send_thunder_message()` hard-requires (A1's real gate, not bypassed). The candidate ID stays obviously synthetic (`THUNDER-TEST-CANDIDATE`), but per Avinash's explicit request the name/email/mobile are his own real contact info (Mukund A. / avin307@gmail.com / +19499430199) so he's testing as himself, not a placeholder identity.
- `get_or_create_test_conversation()` / `reset_test_chat()` — one test conversation per internal tester (scoped by `tenant_id`), non-destructively closeable so a tester can start fresh without ever deleting history.
- `run_test_chat_turn()` — the orchestration: logs the tester's message as `candidate_reply`, calls `generate_thunder_reply()`, sends the reply through the real `send_thunder_message()` gate. `ConversationOwnedByHuman`/`DuplicateMessageSuppressed`/`ConsentNotGiven` all propagate rather than being swallowed — if a recruiter takes over the test conversation, Thunder is correctly blocked here too, which is the governance working, not a bug.

**New endpoint** `app/api/v1/endpoints/thunder.py` (prefix `/thunder`, gated by `get_current_hr_or_admin` — any internal role): `POST /thunder/test-chat`, `GET /thunder/test-chat/history`, `POST /thunder/test-chat/reset`. Exceptions from the service layer map to 409 (ownership lock), 429 (debounce), 424 (consent), 502 (LLM failure) rather than a generic 500.

**Real gap found and fixed, not silently worked around**: `THUNDER_WHATSAPP_NUMBER` (read by `whatsapp_routing_service.DEFAULT_WHATSAPP_NUMBER`) was never set anywhere — meaning `send_thunder_message()` would `NoWhatsAppNumberAvailable` on **any** Thunder send, real or test, since it was built. Added a placeholder value to `.env` (never actually dialed — no real WhatsApp transport exists) so Test Thunder — and any future real send — has a from-number to record. **`.env` is gitignored, so this only takes effect locally; whoever deploys this needs to set `THUNDER_WHATSAPP_NUMBER` in the real environment too.**

**Frontend** (`OnboardingModule-Frontend-main`): new `ThunderChatScreen.js` (chat bubble UI, real-time send/reply, Reset), wired to `/thunder` via `services/api/thunder.js`. Added as the **first, topmost nav item** (above Dashboard) in `Shell.js` for every role branch — Super User, Admin, HR Manager, Hiring Manager, HR Operations, and the default fallback — since Avinash wants this reachable in one click regardless of role, not buried in a sub-menu. Landing page/index route intentionally left unchanged (still Dashboard/MyWorkspace) — a top nav item was judged prominent enough without disrupting the existing default screen for the rest of the HR team.

**Verified live, not just unit-tested**: the real remote SQL Server DB configured in `.env` isn't reachable from the build sandbox (ODBC Driver 18 not installed there), so full click-through verification used a throwaway-SQLite FastAPI instance mounting the real `thunder` router + real `thunder_service.py` (same code, isolated DB) alongside the real frontend dev server. Confirmed: nav renders first for an Admin-role login, `/thunder` loads cleanly, a typed message round-trips through a **real** (non-mocked) Gemini call and Thunder replies correctly in-persona without fabricating facts, and history survives a full page reload.

18 new tests (`test_thunder_test_chat.py`, `test_thunder_test_chat_api.py`) — 536/536 passing (1 xfailed) after this round.

**Follow-up same day**: Test Thunder's candidate identity was wrong — it was one hardcoded shared identity for every internal tester, so anyone logging in saw Thunder address the same fixed person regardless of who was actually testing. Fixed: `get_or_create_test_candidate()` now derives name/email/WhatsApp per the logged-in `Users` row (candidate ID stays a synthetic `THUNDER-TEST-<UserID>`). 2 more tests, 538/538 passing (1 xfailed).

## Session log — 2026-07-22 (later): Priority pivot to Phase 4, migration-debt fix, Phase 4 kickoff

**Avinash's direction**: pause all further Thunder work (dashboard personalization, candidate-vs-employee differentiation, "prod mode" LLM changes) until the real remaining backlog is done. Confirmed directly (not guessed) that "remaining cards" means the Phase 3/4 backlog already tracked in this file, and that "bring employees/projects/timesheets/KPI into the system" means finishing the Resource Management **features** on the already-built Employee/Project/Timesheet/EmployeeAllocation/performance-store schema — not a real-data migration (that's a separate, later decision).

**Corrected the record**: the "Still open in Phase 3" bullet a few entries up was stale the moment it was written — cross-checking `git log` against this file's own history showed Workstream 1 (HRMS-1102/1103/1104, HRMS-0527 Curtis Rule), Workstream 2 (HRMS-1501/1502, deferred to Phase 3.5), and Workstream 3 (S-351, S-364, S-365, S-360/HRMS-0515) were all already built across commits `c6e4048`..`fed8d7a` — none of those commits updated this file. **Only Workstream 4 (Resource Management) is genuinely still open.** Going forward: trust `git log`, not this file's prose, if the two ever disagree — and update this file every round to stop that disagreement from recurring.

**Real production bug found and fixed while scoping Phase 4**: none of the 9 commits since HRMS-0910 (`452e6b7` through `fed8d7a`) touched an Alembic migration file. Cross-checking every ORM table against `alembic/versions/*.py` turned up one **genuine, deployment-blocking** gap (not just a documentation gap — most new tables self-heal via `app/main.py`'s `Base.metadata.create_all(checkfirst=True)` startup fallback, which only skips tables that already exist): S-365/HRMS-0521 added `SPECIALITY_READY`/`PERFORMANCE_MANAGED` to `employees.status`, but the original migration's `ck_employees_status` CHECK constraint only allows the first 7 values, and `checkfirst=True` never alters an existing table. Every test passed anyway because tests build the constraint fresh from the current ORM model, never through the deployed migration's original text. Fixed in `2179e135a0ab_fix_employee_status_check_constraint.py`, verified in isolation against a throwaway SQLite table seeded with the OLD constraint. No other enum/constraint drift found for tables Workstream 1/2/3 touched — `delivery_engine`, `buddy_program_status`, and `htd_phase` were all "folded in from the start" per the original employees migration and already cover every value the model defines today.

**Requirements-doc drift found while reading Phase 4's actual story docs** (`docs/build-package/04-RESOURCE-MANAGEMENT.md` + the underlying `.docx` files, not the phase-doc summary alone, per this project's standing rule): the phase doc (and HRMS-1105's own story doc, S-274) both cite "**HRMS-0312**" as the Core-Pull decision engine's ID — but the real `S-232_HRMS-0312.docx` in the 357-doc corpus is **Workforce Scenario Planning**, completely unrelated. The actual Core-Pull Conflict Rule Engine — "Core wins, same-day, no debate," `core_pull_events`, Orchestration Router integration, BU Head override — is `S-353_HRMS-0514.docx` (whose own internal title line reads "S-314", yet another off-by-one inconsistency within the corpus itself). Building against **HRMS-0514** for all "HRMS-0312" references in the phase doc and in HRMS-1105's story; flagged here rather than silently building a third ID no story actually uses.

**Phase 4 build order** (Part B is foundational to Part A despite the phase doc listing Part A "build first" — HRMS-1105 can't rank or detect anything without a real bench pool to query, so Part B goes first here):
1. Part B foundation: `bench_pool`, `employee_utilization_metrics`, `allocation_conflict_log`, Bench Duration/Aging, a Staffing Eligibility Engine function.
2. S-353/HRMS-0514 CorePullEngine (`core_pull_events`, `Demand.delivery_engine` — doesn't exist yet, needed here for the first time).
3. S-373/HRMS-0529 Specialty Pool Minimum 40 Guard (calls the CorePullEngine).
4. HRMS-1105 Resource Management Agent (ties bench_pool + CorePullEngine + LLM ranking together).
5. S-372/HRMS-0528 Confirmed vs Potential Demand Workflow.

Already confirmed built and reusable, not rebuilt: `allocate_employee_to_project()`/`end_allocation()` in `employee_allocation_service.py` already implement Part B item 1 (Mark Employee as Bench, via `end_allocation()`'s status transition) and most of item 4 (Allocation Conflict Detection, via the existing `allow_concurrent`/`AllocationOverCapacity` check) — this already **is** S-301/HRMS-0803, so "have S-301 call it" is already satisfied by construction, not a separate refactor. `S-352/HRMS-0513` (Core Eligibility Gate — the full AI-Assessor/90-day-clock approval workflow that actually flips `core_certified` to `TRUE`) is a substantial story of its own with dependencies that don't exist yet (`HRMS-0518` AI Assessor, `HRMS-0522` 90-day clock) — out of scope for Phase 4's Part B, which only needs to *read* the existing `core_certified` boolean as a hard gate, not build the workflow that sets it.

## Session log — 2026-07-22 (continued): S-353 Core-Pull Engine + S-373 Specialty Pool Guard built together

Built as one round, not two, since S-373's guard has no meaningful existence separate from being called by S-353's execute step (04-RESOURCE-MANAGEMENT.md's own "S-373 is called by HRMS-0514 Core-Pull Engine before executing"). `app/models/core_pull.py` (`CorePullEvent`, `SpecialtyPoolReplacementPlan`), `app/services/core_pull_service.py`, migration `35fbb8ebd5c8` (adds `demands.delivery_engine` — didn't exist before this, needed for the very first time here — widens `employee_allocations.status` to include `CORE_PULLED`, creates both new tables).

- **`detect_core_pull_conflict(db, employee, core_demand)`**: the one place "does this employee have a genuine Core-vs-Speciality conflict" gets decided — per-employee-per-demand, called by whoever proposes the Core move (HRMS-1105's future scan, S-372's future workflow, a manual transfer), not a self-scheduled cron this module owns. Idempotent — a second call while a PENDING event exists returns that same event.
- **`execute_core_pull()`**: same-day, no negotiation, per BR. Deliberately does NOT go through `allocate_employee_to_project()`'s normal gate (`BuddyProgramNotGraduated`/`EmployeeAlreadyAllocated` don't apply — the employee is already Core-Certified, and holding the Speciality allocation being transferred isn't a conflict to block on). Ends the Speciality allocation as `CORE_PULLED` (not a plain `ENDED` — kept distinct for reporting), creates the new Core `EmployeeAllocation` directly, flips `employee.delivery_engine`, logs `EmployeeEngineHistory`. Still consults the Orchestration Router first (`action_type="core_pull_transfer"`, `risk_tier=MEDIUM`) — a real governance checkpoint, not skipped for Core-Pull, even though no seeded rule currently targets it.
- **S-373 guard wired in exactly where the doc calls for**: `execute_core_pull()` calls `check_specialty_pool_guard()` before doing anything irreversible; if the move would drop the Specialty Core-Certified pool below 40 and no `SpecialtyPoolReplacementPlan` has been logged for that employee since the conflict was first detected, it raises `SpecialtyPoolBelowMinimum` and nothing happens. `log_replacement_plan()` enforces the 100-char-strategy + required-date BR.
- **BU Head override path** (`override_core_pull()`): Specialty allocation is deliberately never touched on override — it just continues, the Core demand stays open for alternative sourcing. More than 2 overrides in 30 days pages a caller-supplied `director` (same "roster resolution not built, caller-supplied" posture as the Orchestration Router's own `director` param).
- **Real bug caught by the tests, not by inspection**: comparing `CorePullEvent.detected_at` against `SpecialtyPoolReplacementPlan.logged_at` to decide "was a plan logged after this conflict was detected" silently always returned false when both columns relied on their `server_default=func.now()` — a flush() doesn't reliably read a server-generated default back into the Python object (reads as `None`), and even when it does, SQLite's `CURRENT_TIMESTAMP` (no fractional seconds) can string-sort as "earlier than" a microsecond-precision Python timestamp regardless of real order. Fixed by setting both timestamps explicitly with `datetime.utcnow()` in the service layer instead of trusting the column defaults — same "both sides must use the same clock" discipline `thunder_service.py`'s debounce check already established.
- **Doc-ID note carried forward**: built against `S-353_HRMS-0514.docx` and `S-373_HRMS-0529.docx` directly, matching `WROS_Canonical_Backlog_S001-401.xlsx`'s WROS-ID column — not "HRMS-0312" as `04-RESOURCE-MANAGEMENT.md` and HRMS-1105's own story doc both still say. Marked both `Done` in the canonical sheet (`Change Log` tab entry added), per the new tracking convention.

22 new tests, 575/575 passing (1 xfailed).

**Next**: HRMS-1105 Resource Management Agent (S-320 in the canonical sheet, not S-274 as its `.docx` filename says — ties bench_pool + this engine + LLM ranking together), then S-372/HRMS-0528 Confirmed vs Potential Demand Workflow.

## Session log — 2026-07-22 (continued): HRMS-1105 Resource Management Agent (canonical S-320)

`app/models/resource_agent.py` (`BenchAllocationRecommendation`), `app/services/resource_management_agent_service.py`, migration `cf4c36d9b053`. Built from `Requirements/S-274_HRMS-1105.docx` — its filename says S-274, its own prerequisite line says "HRMS-0312", both wrong; canonical is S-320/HRMS-1105, confirmed against `WROS_Canonical_Backlog_S001-401.xlsx`.

- **Scope call, flagged rather than silently narrowed**: the story doc frames Core-vs-Speciality detection purely in terms of the bench pool ("for each bench employee..."), but `detect_core_pull_conflict()` (S-353) only ever fires for an employee who currently *holds* an active Speciality allocation — which a bench employee, by definition, never does. Scanning literally only the bench pool would make the doc's own AC-1 structurally unreachable. `detect_core_pull_triggers()` therefore also checks every Core-Certified employee who **does** hold an active Speciality allocation against open CORE demands — the real Core-Pull scenario — via the exact same `detect_core_pull_conflict()` call S-353 already exposes, never a second "Core wins" conditional. Bench employees still only ever get ranked, never pulled (nothing active to pull them from).
- **`run_bench_scan()`**: one idempotent, directly-callable function (the 30-minute cadence is scheduling wiring this codebase defers everywhere, not built here) that (1) runs `detect_core_pull_triggers()`, (2) for the current bench pool, ranks non-conflicting matches via Gemini (same substitution as Thunder — no Claude API client wired in this codebase, the doc's own "claude-sonnet-4-6" isn't available) and stores `PENDING_RM_REVIEW` rows in `bench_allocation_recommendations`. BR-1105-02 (never creates `employee_allocations` directly) is structural, not just tested: `run_bench_scan()` has no code path that touches that table at all — only `approve_bench_recommendation()` does, and it routes through the existing `allocate_employee_to_project()`, not a direct insert.
- **LLM ranking degrades gracefully**: unlike Thunder's reply generation (no LLM = no reply, since a reply *is* the deliverable), a ranking is advisory — `rank_bench_candidate()` falls back to the raw skill-match score (no rationale) if `GEMINI_API_KEY` is unset or the call fails, rather than blocking the whole scan on an LLM outage.
- **Skill matching**: no requirements doc defines an exact threshold ("skill match score exceeds threshold" is the doc's own phrasing) — 0.5 (at least half the demand's required skills present) is this session's plain default, flagged in code rather than silently baked in.

18 new tests, 593/593 passing (1 xfailed). Marked S-320 Done in the canonical sheet.

**Next**: S-372/HRMS-0528 Confirmed vs Potential Demand Workflow — the last of Phase 4 Part A's four stories.

## Session log — 2026-07-22 (continued): S-372 Confirmed vs Potential Demand Workflow — Phase 4 Part A complete

`app/models/demand_confirmation.py` (`DemandAlignmentCall`), `Demand.confirmation_status`/`sow_reference`/`sow_received_date`, `app/services/demand_confirmation_service.py`, migration `73c1aca4119d`. Built from `Requirements/S-372_HRMS-0528.docx` directly (this one's filename/prerequisite IDs matched the canonical sheet correctly, no correction needed).

- **One status field, not two**: the doc describes "Confirmed" (SOW already signed) and "Potential" (interview-first, SOW later) as if they were different workflow types, but its own Step 1 schema only adds one `confirmation_status` column — both paths converge on `CONFIRMED` the moment a real `sow_reference` is recorded (`confirm_demand_with_sow()`, AC-6: rejects an empty/whitespace-only reference).
- **`DemandAlignmentCall` is per-candidate, not folded into `Demand`** — a demand can have more than one candidate considered over its lifetime; the 3-way call and both fit confirmations are scoped to a specific employee being proposed for a specific demand.
- **`schedule_alignment_call()`**: no calendar integration (HRMS-1306) exists in this codebase — same "real function signature, transport deferred" posture as WhatsApp/virus-scan. Takes an injectable `scheduler` callable, defaults to "right now" (correct for the CONFIRMED path's own same-day BR).
- **Employee's decision is final, structurally**: `confirm_fit()` has no code path that lets a BU Head set the employee's confirmation (or vice versa), and once either participant's confirmation is recorded it can never be silently overwritten by a second call — matching the BR that an employee who declines a Core move "is not penalised" and can't be overridden without a formal HR process this codebase doesn't build.
- **`trigger_specialty_client_release()`** (HRMS-0534): the hard sequence gate is real — `confirmation_status=CONFIRMED` AND both fit confirmations are exactly `True`, checked together, no partial sequence. WROS still does not notify Specialty clients directly (same posture as S-353's Core-Pull notifications) — this notifies the Speciality RM, who coordinates the actual client handover.

17 new tests, 610/610 passing (1 xfailed). Marked S-372 Done in the canonical sheet.

**This closes out the backend for all four of Phase 4 Part A's stories** (S-353 Core-Pull Engine, S-373 Specialty Pool Guard, S-320/HRMS-1105 Resource Management Agent, S-372 Confirmed vs Potential Demand Workflow) plus the Part B foundation (bench_pool, utilization metrics, conflict log, Staffing Eligibility Engine) built earlier this session.

**CORRECTION (2026-07-22, same day)**: the line that used to be here said "Phase 4 is functionally complete" — wrong, and Avinash caught it. Backend-only (models/services/migrations/tests, no REST endpoint, no screen) is **In Progress**, not Done — see the new "Definition of Done" section at the top of `C:\Users\AvinashMukund\Documents\Claude\CLAUDE.md`. All four stories above were reverted from `Done` to `In Progress` in the canonical sheet. None of Phase 4 (or most of Phase 3) has an API or UI yet — that's the actual remaining work, not a follow-up nice-to-have.

## Session log — 2026-07-22 (continued): Resource Management Agent exclusivity hard-block

Avinash's explicit business call, not in HRMS-1105's story doc (that doc only describes a 2-step suggest-then-allocate flow): a bench employee already being actively pursued for one client ("in interview stage") must never be simultaneously pursued for a second. Confirmed scope directly rather than guessing — internal bench employees only (not the external Submission pipeline, which has the same class of gap but is explicitly out of scope for now), hard block not a warning.

- **New `IN_PROGRESS` stage** on `BenchAllocationRecommendation` (migration `ba6522085601`, widens the status CHECK constraint, adds `pursued_by`/`pursued_at`): `PENDING_RM_REVIEW` (suggestion) → `IN_PROGRESS` (RM is actively presenting this candidate — `start_pursuing_recommendation()`) → `APPROVED` (offer/placement, real allocation created) or `REJECTED` (from either PENDING or IN_PROGRESS).
- **`is_employee_actively_engaged()`**: true the moment any of an employee's recommendations is `IN_PROGRESS`, regardless of which demand.
- **`start_pursuing_recommendation()`** is the real enforcement point — hard-blocks (`EmployeeAlreadyActivelyEngaged`) if the employee is already `IN_PROGRESS` elsewhere. `approve_bench_recommendation()` now requires `IN_PROGRESS` as a precondition (was `PENDING_RM_REVIEW`) — you can no longer allocate straight from a raw suggestion, matching the real world (interview happens before offer).
- **`run_bench_scan()` skips actively-engaged employees entirely** — no new recommendations are even generated for someone already being pursued elsewhere, not just blocked at the pursue step. More conservative than strictly necessary, deliberately — presenting new options to an RM already committed to one client is noise at best.
- Existing `PENDING_RM_REVIEW` suggestions for *other* demands are left untouched when one is pursued — visibility into alternatives is preserved (in case the active one falls through), only the act of *pursuing* a second one is blocked.

6 new tests (the direct scenario: an employee `start_pursuing`'d for Client A, blocked from being pursued for Client B until A is rejected/resolved) + 1 existing test updated for the new precondition. 616/616 passing (1 xfailed).

**No UI or API exists for Core-Pull/Specialty Pool Guard/Resource Management Agent yet** (Avinash asked how to validate S-353/S-373 in the UI — same answer for this: none of Phase 4 has REST endpoints or a screen, only Thunder does, since that was built explicitly as a clickable demo). Flagged as open, not started this round.

## Session log — 2026-07-22 (continued): 205-story backend-priority sequencing decided; HRMS-1105 Resource Management Agent API + UI built (first story in that queue)

**Sequencing decision, confirmed with Avinash**: the 205 stories whose canonical-sheet Assignee is a backend variant are first priority, processed in Story ID order (S-002→S-401), each requiring the full Definition of Done (UI+API+backend+tests) and Avinash's explicit approval before the next story starts. Bounded exception, confirmed: finish Resource Management (S-320, S-353, S-372, S-373 — backend already built) first since S-320 is Avinash's own stated top business priority and falls ~140 stories deep in strict order, THEN resume strict order for the other ~201. Full detail/rationale: `docs/build-package/BACKEND-205-SEQUENTIAL-BUILD-PROMPT.md`.

**HRMS-1105/S-320 Resource Management Agent — API + UI built** (first story in the RM carve-out):
- `app/api/v1/endpoints/resource_management.py` (new) — `POST /resource-management/scan`, `GET /recommendations`, `POST /recommendations/{id}/pursue|approve|reject`, `GET /employees/{id}/actively-engaged`. Auth: `get_current_hr_or_admin` (any internal user), same posture as Thunder — no resource-management-specific RBAC permission exists yet in this codebase's permission set; that's a separate, unscoped RBAC story.
- `app/schemas/resource_management.py` (new) — response schemas, enriched with employee/demand/client names resolved server-side for display.
- Frontend: `src/screens/ResourceManagementScreen.js`, `src/services/api/resourceManagement.js`, registered at `/resource-management`, nav item added to Shell.js for SUPER_USER/ADMIN/HR Manager (closest existing-role proxy for "Partner/Resource Manager" — no such role exists yet in this codebase; flagged for Avinash to confirm/adjust).
- 9 new API-level tests (`tests/test_resource_management_api.py`), including the exact scenario Avinash described (pursue for Client A, hard-blocked 409 pursuing for Client B until resolved). Full suite: 625 passed, 1 xfailed (was 616).

**Two real pre-existing root-cause bugs found and fixed while trying to browser-verify this**, both `load_dotenv()` called bare (no path) in `app/core/database.py` and `app/core/config.py` — resolves `.env` via the process's CWD, not the file's location. This repo's dev-server preview launcher starts uvicorn with an unrelated CWD, so `DATABASE_URL`/`JWT_PRIVATE_KEY`/etc. silently came back `None` instead of a clear error. Fixed both to resolve `.env` via an absolute path relative to `__file__`. Three other bare `load_dotenv()` calls exist (`msgraph.py`, `tools/ats_scorer.py`, `tools/job_description_generator.py`) — not fixed, low priority, since `config.py`/`database.py` populate `os.environ` early enough in the real import order that these are very likely already no-ops in practice; worth the same fix opportunistically if one of them ever actually breaks.

**Full browser click-through NOT completed this round** — blocked by a separate, genuinely pre-existing environment gap: this machine has no "ODBC Driver 18 for SQL Server" installed (`Get-OdbcDriver` confirms only the legacy "SQL Server" driver is present), so `pyodbc` can't connect to the real remote MSSQL database (`46.224.149.7`) this dev server points at — every DB-touching request, not just this story's, fails with `IM002`. Installing system-level software is outside what I do unilaterally (see the "modifying system settings" boundary) — flagged to Avinash rather than installed silently. The API contract and business logic (including the exclusivity hard-block) are proven by the 9 new API-level tests against a real, fully-wired FastAPI app (mocked DB + mocked Gemini only) — genuine, but not the same as an actual click-through. Story is presented for approval on that basis; a live click-through can follow once the driver's installed.

**S-320 approved by Avinash** on that API-test-level basis (live click-through still pending the driver install). Marked `Done` in the canonical sheet + Change Log as soon as the sheet isn't locked open in Excel on his end (it was mid-session — retry next time it's accessible).

## Session log — 2026-07-22 (continued): S-353 Core-Pull Engine + S-373 Specialty Pool Guard API + UI built (second story in the RM carve-out)

- `app/api/v1/endpoints/core_pull.py` (new) — `GET /core-pull/specialty-pool-status`, `GET /events`, `POST /events/{id}/execute|override`, `POST /replacement-plans`. Same `get_current_hr_or_admin` auth posture; `override` additionally requires `current_user.UserRole == "BU Head"` (enforced inside `override_core_pull()` itself, 403 otherwise) — a real, existing role in this codebase's RBAC (`rbac_service.py`), not invented for this story.
- `app/services/core_pull_service.py` — added one small read-only `get_specialty_pool_status()` (current pool count, no "after a specific move" simulation — that's `check_specialty_pool_guard()`'s job, still called only from `execute_core_pull()`). No other business logic changed.
- `app/schemas/core_pull.py` (new).
- Frontend: `src/screens/CorePullScreen.js`, `src/services/api/corePull.js`, registered at `/core-pull`, nav item next to Resource Management (same role-scoping rationale/caveat).
- UI detail worth knowing: when Execute hits the guard's 409, the screen doesn't just show a generic error — it opens an inline replacement-plan form for that row (strategy + expected date), matching the real workflow (BU Head logs a plan, then re-executes).
- 12 new API-level tests (`tests/test_core_pull_api.py`), including execute-blocked-then-unblocked-by-plan, override-forbidden-for-non-BU-Head, and override-succeeds-for-BU-Head. Full suite: 637 passed, 1 xfailed (was 625).

**Live click-through still blocked by the same ODBC driver gap** — not re-attempted this round since the blocker hasn't changed; presented for approval on the same API-test-level basis as S-320.

**S-353/S-373 approved by Avinash.**

## Session log — 2026-07-22 (continued): Live bug fixed — Thunder hung indefinitely on a slow Gemini call; S-372 Confirmed vs Potential Demand Workflow API + UI built (third and final story in the RM carve-out)

**Live bug, reported directly by Avinash mid-session**: "I typed in thunder 'I need a Guidewire developer' thunder timed out." Root cause: `generate_thunder_reply()`'s `ChatGoogleGenerativeAI` call had no `timeout` set at all — this client's own default is `None`, so a slow/stuck Gemini response hangs the whole request indefinitely, no bound. Not specific to that message's content — Thunder has no special handling that would choke on an odd phrase, it just passes it straight to the LLM. Fixed in `app/services/thunder_service.py` (commit `b6f24b8`): `timeout=30` (a WhatsApp-style 2-4 sentence reply doesn't need more), and the `llm.invoke()` call is now wrapped so any failure raises the existing `ThunderReplyGenerationFailed` (→ 502 from the API) instead of hanging or propagating an unhandled exception. All 33 existing Thunder tests still pass. **If this still times out after the fix, that would point to something else (Gemini API key/quota) — dig into that specifically rather than re-assuming it's the same bug.**

**S-372/HRMS-0528 Confirmed vs Potential Demand Workflow — API + UI built** (last story in the RM carve-out):
- `app/api/v1/endpoints/demand_confirmation.py` (new) — confirm-sow / schedule-call / calls / confirm-fit / trigger-release. Same `get_current_hr_or_admin` auth posture.
- **Scope note, flagged not silently narrowed**: this codebase has no employee self-service login path at all (no `get_current_employee` dependency exists anywhere). Both EMPLOYEE and BU_HEAD fit confirmations are recorded by an internal user explicitly selecting which participant they're recording on behalf of — `confirm_fit()`'s own signature already takes `participant` as a parameter, not an inferred caller identity, so this doesn't change the service contract. A genuine employee self-service portal for this is a separate, unscoped story.
- `app/schemas/demand_confirmation.py` (new).
- Frontend: `src/screens/DemandConfirmationScreen.js`, `src/services/api/demandConfirmation.js`, registered at `/demand-confirmation`, nav item alongside the other two RM-carve-out screens. **No Demand/Employee browse screen exists yet in this app** (separate, later story), so this screen takes a Demand ID + Employee ID directly rather than a picker — consistent with the rest of the app's current state, not a shortcut specific to this story.
- 11 new API-level tests (`tests/test_demand_confirmation_api.py`), including the full release-gate sequence (blocked until both fits True AND confirmation_status==CONFIRMED with an SOW). Full suite: 648 passed, 1 xfailed (was 637).

## Session log — 2026-07-22 (continued): EPIC-05/Resource & Bench Management (S-245–S-256) built story-by-story; two critical MVP-chain gaps found and closed

**Avinash's direction this round**: finish Resource & Bench Management and the Timesheet/Revenue module completely before returning to strict Story-ID order — explicitly because the requirements are written as a dependency chain and jumping around risks rebuilding things inaccurately. Worked S-245 through S-255 in small vertical-slice groups, each committed/tested/pushed separately (see individual commits for full detail): S-245/S-246/S-247/S-248 (Employee Directory + bench lifecycle, `6064142`), S-249/S-250 (Submission Pipeline + Staffing Eligibility, `a3f88c6`), S-251/S-252 (Allocations, `8d0256e`), S-253 (Match Bench Resources, `b3843a5`), S-254/S-255 (Utilization/Bench Cost, `d5bc4c0`).

**Real, recurring pattern across this whole stretch**: most of EPIC-05's backend (Employee model, bench_pool lifecycle, allocation service, submission pipeline, timesheet service) was already built and tested from earlier sessions — the actual work was almost entirely REST API + UI, not new business logic. Two genuine exceptions, both flagged rather than silently resolved: (1) the real requirement doc for S-246 calls for a persistent `bench_periods` history table distinct from the existing `bench_pool` current-state table — added alongside it, confirmed with Avinash, migration `c1d2e3f4a5b6`; (2) S-249's doc references a "Submission Pipeline" that turned out to already exist in full (`submission_service.py`, HRMS-0711) with zero API — built that real pipeline's API instead of a duplicate standalone eligibility check.

**Avinash then asked me to test the full MVP chain** (add candidate → submit to job → interview → start onboarding → convert to employee → assign project → time tracking → resource management) and report what's next. Live click-through is still blocked by the missing ODBC driver, so this was a code-path audit instead — traced every step against real endpoints/services. Found two real breaks:
1. **No API ever called `convert_candidate_to_employee()`** (`employee_service.py`, HRMS-0708 minimal slice, fully built and correct) — a candidate finishing onboarding had no way to become an Employee record through the app at all.
2. **No Timesheet API existed** despite `timesheet_service.py` (HRMS-0901/HRMS-0902) being fully built and tested — weekly draft, entry upsert with the 60h cap, submit, approve/reject/reopen, bulk approve.

Both closed this round: `POST /employees/convert-candidate/{candidate_id}` (commit `d5bc4c0`, bundled with S-254/S-255) and the full `/timesheets` API (commit `3ae5ebb`) + `TimesheetsScreen.js`. **This makes the MVP chain Avinash described actually work end-to-end through the app for the first time**, not just as two disconnected systems (legacy ATS pipeline vs. the Employee/Allocation/Resource-Management system built this program).

**Role-gating gap, flagged not guessed**: HRMS-0902 BR-01 says only "RM/Admin" may approve a timesheet, but no "RM" role exists in this codebase's RBAC role list — approve/reject stayed at "any internal user" rather than inventing a role mapping.

**Still open in this cluster**: S-256 (Future Demand vs Bench Forecast — genuinely new logic, no existing backend) and the rest of the Timesheet/Revenue module (S-219 locale/currency, S-223 utilization calc from timesheets, S-224 revenue mapping, S-225 revenue leakage, S-226 invoicing, S-228 revenue dashboard, S-229 anomaly detection — S-220/221/222 now done, S-227 is RETIRED/merged into S-226). Live browser click-through for all of this is still blocked by the same ODBC Driver 18 gap (see earlier session log entry) — everything here is proven at the API-test level only.

## Session log — 2026-07-22 (continued): S-256 built (closes EPIC-05); employee bulk .xlsx import; two real production security bugs found and fixed; launch-scope question raised

**S-256/HRMS-0506 (canonical) Resource Demand Planning built** — genuinely new logic, no pre-existing backend (unlike almost everything else in EPIC-05). Content-matched against `Requirements/S-171_HRMS-0506.docx` (same ID-drift pattern documented elsewhere in this codebase — canonical sheet is the tiebreaker, not the filename). `app/services/resource_forecast_service.py` (new): `get_expiring_allocations()` (ACTIVE allocations with a planning `end_date` within 90 days, bucketed <30/30-60/60-90) and `get_skill_gap_analysis()` (per-skill projected bench supply — current bench + 30-day-expiring — vs open demand, `gap = supply - demand`, negative = shortage). `GET /resource-forecast/expiring`, `GET /resource-forecast/gap-analysis`. Read-only, per the source doc's own "WHAT NOT TO BUILD": no ML forecasting, no automated re-allocation. 3 new tests. Commits `05d6700`/`57af2db`. **This closes the entire EPIC-05 cluster (S-245–S-256).**

**Avinash asked to load 60 real employees and "make production secure."** Built `POST /employees/bulk-import` (.xlsx upload, each row created independently via the existing `create_employee_profile()` — one bad row is reported and skipped, doesn't abort the file; new dependency `openpyxl`). While doing the security pass, found two real, unrelated production bugs:
- **CORS was hardcoded to `allow_origins=["*"]` with `allow_credentials=True`** in `app/middleware/cors.py` — in that combination, starlette's `CORSMiddleware` echoes back whatever `Origin` header the request sends, so this let ANY website make authenticated (cookie/`Authorization`-header) requests to this API. The real, already-configured allowlist (`settings.CORS_ORIGINS` — includes the actual production server's own origins) sat commented out right next to it. Restored.
- **`/docs`, `/redoc`, `/openapi.json` were reachable unconditionally** on a system handling real PII (employee bank details, salaries, PAN numbers). Gated behind `settings.DEBUG` (already `False` by default) in `app/main.py`.
Commit `e995f76`, 3 new bulk-import tests, full suite 705 passed at the time.

**Launch-scope question raised, not yet resolved**: Avinash then said "finish all open items so we can launch tonight." The remaining canonical items (S-219 locale/currency, S-223-S-229 minus 220-222 already done) are Finance/Revenue features, NOT part of the MVP chain he originally described for tonight's launch (that chain is now fully connected and working — see the entry above). Recommended treating tonight's launch scope as MVP-chain + bulk-import + the two security fixes (all done), with Finance/Revenue as a fast-follow after launch — **asked him to confirm explicitly rather than assuming**, since it's a real launch-scope call. Also flagged as not yet done: HTTPS termination review, JWT/secret rotation and strength, a dependency vulnerability scan, admin-account hardening — the security pass so far only covered CORS + exposed docs.

## Session log — 2026-07-22/23 (continued): Finance/Revenue cluster finished (S-219, S-223–S-229) — closes the entire Timesheet & Revenue module Avinash asked for

**Avinash's direction**: "keep going through the Finance/Revenue cluster" — confirmed continuing past the MVP-chain launch scope into S-219/S-223-229. Built story-by-story, each round tested/committed/pushed:

**S-229/HRMS-0910 (Anomaly Detection) + Timesheet Dispute Resolution** (commits `7e40db9` backend / `ffa9e83` frontend) — both wired real, tested, pre-existing backend (`timesheet_anomaly_service.py`, `timesheet_dispute_service.py`) with zero API, same recurring pattern as this entire program. `POST/GET /timesheets/{id}/scan-anomalies|anomalies`, `POST/GET /timesheets/{id}/disputes`, `POST /timesheets/disputes/{id}/resolve`. Frontend: anomaly badges + scan button, raise/list/resolve dispute panel on `TimesheetsScreen.js`. 22 new tests. **Doc-corpus drift confirmed**: `timesheet_dispute_service.py`'s own docstring cites "HRMS-0904" but the canonical sheet's real HRMS-0904 is S-223 (Employee Utilization) — flagged in the module docstring, feature built regardless since it's real and `invoice_service.py` already depends on `has_open_dispute()`. Also fixed a stray `// eslint-disable-next-line react-hooks/exhaustive-deps` comment in `TimesheetsScreen.js` referencing a rule that isn't configured anywhere in this project's eslint setup — CRA treats an unknown rule name in a disable-comment as a hard build failure, not a warning; removed it (it was suppressing a complete, correct dependency array anyway).

**S-225/HRMS-0906 (Revenue Leakage) + HRMS-0903 (Reconciliation) + S-226/HRMS-0907 (Invoicing) + S-228/HRMS-0909 (Client Revenue Dashboard)** (commits `a595b69` backend / `0637635` frontend) — all four wired real, tested, pre-existing backend (`invoice_service.py`, `revenue_leakage_service.py`, `client_revenue_dashboard_service.py`) with zero API. R-10 (unapproved timesheet blocks invoicing) and HRMS-0904 BR-02 (open dispute blocks invoicing) are enforced entirely inside `generate_invoice()` already. New: `/invoices` (generate/approve/send/mark-paid/list/get), `/revenue/leakage/*`, `/revenue/reconciliation/*`, `/revenue/dashboard/clients/{id}`. Frontend: `InvoicesScreen.js`, `RevenueScreen.js`. 23 new tests. **Real cross-tenant data leak found and fixed before shipping**: `find_reconciliation_gaps()` had no tenant filter at all — wiring it straight to a REST endpoint would have let one tenant's reconciliation scan attribute another tenant's approved-but-uninvoiced timesheets to itself as `ReconciliationAlert` rows. Added an optional `tenant_id` filter (default `None` preserves the function's prior global-scan behavior for any other caller); the new endpoint always passes the caller's own tenant.

**S-223/HRMS-0904 (Employee Utilization Calculation) — real gap found and closed** (commit `142f646` backend / `a745f72` frontend): `record_weekly_utilization_metric()` computed exactly the canonical spec ("approved billable hours vs available hours") correctly and was tested, but had **zero callers anywhere in the app** — `EmployeeUtilizationMetric` could never actually be populated in production, so the S-254 utilization dashboard built earlier this session had nothing real to show. Added `POST /employees/{id}/record-utilization` + a "Record Utilization" trigger form on `UtilizationDashboardScreen.js`. This is the same "backend exists, nobody ever calls it" pattern as `convert_candidate_to_employee()` earlier this session — worth periodically re-checking newly-built service functions for zero-caller status, not just zero-API status.

**S-224/HRMS-0905 (Map Timesheet to Project Revenue) — confirmed already covered, no new code**: canonical spec is "billable hours × bill rate, store in project currency and USD" — `invoice_service.generate_invoice()`'s existing `InvoiceLineItem` computation (`amount = rate * hours`, USD cents base, `Invoice.currency` display) already satisfies this exactly.

**S-219/HRMS-0121 (Multi-Continent Locale & Currency Config) — built from scratch, genuinely greenfield** (commit `142f646` backend / `a745f72` frontend): no Tenant-config model/service/API existed at all. **Doc-corpus drift, worst case yet**: the file `S-219_HRMS-1212.docx` is entirely unrelated content ("Analytics Performance Optimization & Caching", EPIC-12, depends on analytics dashboards that don't exist in this codebase) — not just a wrong ID label, a completely different story. Built directly from the canonical sheet's own one-line spec instead: "Per-tenant: timezone, date format, currency display (USD/GBP/EUR/INR/CAD). Monetary fields store in USD base with local display." Added `Tenant.default_timezone/default_date_format/default_currency` (migration `b7c8d9e0f1a2`), `GET/PATCH /tenants/me/locale` (scoped to the caller's own tenant, not a path param — no reason a caller should ever read/edit another tenant's config), `TenantLocaleScreen.js`. **Explicitly did not implement any USD-to-local currency conversion** — no exchange-rate table/source exists anywhere in this codebase, and hardcoding a rate would be guessing a real-world number that goes stale immediately; display-currency label only, flagged in the code rather than faked.

**Regression found and fixed before commit**: the new `Tenant` columns were `NOT NULL` with only a Python-side ORM `default=`, which doesn't apply to raw SQL. A pre-existing test (`test_tenant_backfill_migration.py`) does a raw `INSERT INTO tenants` to simulate real migration-time backfill behavior, and started failing with a `NOT NULL constraint failed` the moment those columns landed. Fixed by adding matching `server_default=` to the ORM columns (the Alembic migration itself already had this right via `op.add_column(..., server_default=...)` — the ORM model just didn't mirror it). Caught by running the full suite before committing, not by the initial targeted test run — **worth remembering: a model change to a widely-shared table (Tenant is touched by nearly every test fixture in this codebase) needs the FULL suite, not just the new endpoint's own tests, before it's safe to commit.**

**This closes the entire Timesheet & Revenue module (S-219 through S-229) end to end.** Full suite: 752 passed, 1 xfailed.

**New pending backlog surfaced by Avinash, not yet scoped**: Employee Portal stories **S-096 to S-105**, and **"all stories in delivery engine HRMS-0535 to HRMS-0519"** (given as a descending range — verify actual direction/span against the canonical sheet, don't assume) plus **HRMS-P511**. Queued behind this cluster per the standing "finish clusters completely before jumping around" instruction — not yet cross-referenced against the canonical sheet.

**Canonical sheet still locked in Excel on Avinash's end this whole round** — the growing queue of rows to mark `Done` + Change Log entries now includes S-320, S-353, S-372, S-373, S-245 through S-256, S-219, S-223, S-224, S-225, S-226, S-228, S-229 (S-220/221/222 marked separately if not already). **Explicit instruction from Avinash, 2026-07-23: don't attempt a save to the sheet for 8 hours from ~03:15 EDT (i.e. hold off until ~11:15 EDT) — he's actively using it.** Read-only access works fine throughout (`openpyxl.load_workbook(..., read_only=True)` reads cleanly even while the file is open in his Excel) — keep reading it for scoping, just don't try to save. Batch-update everything in one pass once the window's up.

**Live click-through still blocked by the same ODBC driver gap.** This closes out the Resource Management carve-out (S-320, S-353, S-373, S-372 all API+UI built).

## Session log — 2026-07-23: Delivery Engine cluster (NEW-RM epic, S-351-S-359) started, per Avinash's "finish Employee Portal + Delivery Engine before strict order" direction

**Scoping**: "HRMS-0535 to HRMS-0519" + "HRMS-P511" turned out to be the entire NEW-RM epic (S-351 through S-359, 9 stories) — not a literal numeric range, those were just the epic's first/near-last WROS IDs plus one P-prefixed outlier. S-353 (Core-Pull Engine) already built earlier this session. S-352 (Core Eligibility Gate) genuinely can't be built yet — hard-depends on S-357 (AI Assessor, an agentic-bot story) and HRMS-0522 (a "90-day certification clock" that isn't in this epic's story list and doesn't exist anywhere in this codebase). S-355 and S-357 are both new AI/agentic features (same category as Thunder) — holding both for a design check-in with Avinash before building, same posture as the Thunder persona question earlier this session.

**S-351/HRMS-0512 (Delivery Engine Assignment) — turned out to be almost entirely already built** (`core_certified` + CHECK constraint from an early migration, `EmployeeEngineHistory` audit table, `convert_candidate_to_employee()`'s SPECIALITY-only enforcement, `set_core_delivery_engine()` already written as "the real, callable mutation a future HRMS-0513 build wires its BU Head approval into" per its own docstring). The only real gap: nothing exposed it. Added `GET /employees/{id}/engine-history` (deliberately read-only — the doc's own "Not In Scope" says no UI bypass for engine assignment) + badges on the Employee Directory. Commits `d062876` backend / `92c9449` frontend.

**S-358/HRMS-0519 (SI Partner Engagement Tagging) — surfaced a bigger gap while scoping it**: `project_service.py` has a complete, tested HRMS-0801/0804/0805/0806 backend (lifecycle, milestones, unfilled roles, revenue estimate) with **zero REST API** — nothing in this app could create a Project through the UI at all before this. Built the full `/projects` API and folded S-358's `delivery_engine`/`si_partner` fields into the same migration, denormalized `si_partner` onto `employee_allocations` at allocation time (which also meant giving `/allocations` a `project_id` field it never had either). RM Agent continuity-bonus (AC-3) explicitly deferred — documented why in `resource_management_agent_service.py`'s module docstring (the doc assumes a flat point-bonus scoring model this codebase's actual skill-match-float + LLM-confidence scoring doesn't have, and the bench-matching function only ever considers unallocated employees who by definition have no "current SI partner"). Commits `2c96f9b` backend / `a75835c` frontend.

**Real regression caught before commit**: `SiPartnerRequired` validation initially lived inside `create_project()`/`create_project_from_won_opportunity()` — broke ~25 unrelated tests (invoices, milestones, anomaly detection) that reuse those functions as plain fixture helpers with no si_partner. Moved the validation to the API endpoint layer, matching what the doc's own AC-1 actually describes ("API call ... returns HTTP 400"), not a service-layer invariant. **Lesson reinforced again**: a change to a function reused broadly as a test fixture needs the FULL suite before it's safe to commit, not just the new story's own tests — this is the second time this exact pattern has bitten a build this session (first was the Tenant `server_default` regression).

**S-354/HRMS-0515 (Performance Intelligence Store) — same pattern as S-351**: the real backend already existed (`EmployeePerformanceEvent` model, `write_performance_event()`/PerformanceStoreWriter, and two genuine already-shipped callers in `buddy_program_service.py`/`htd_phase_gate_service.py`). Only the read side was missing. Added `GET /employees/{id}/performance` (events + per-event-type score averages, computed from the actual JSON-blob event_data shape this codebase uses, not the doc's idealized normalized-column schema — migrating the two existing writers onto new columns is separate, real scope, not done here). LLM weekly-summary generation (the doc's Step 3) explicitly deferred, flagged in the service docstring.

**S-356/HRMS-0517 (Employee Milestone Tracker) — real table-name collision, flagged not silently resolved**: this story's own doc names its table `project_milestones`, colliding with the already-shipped `ProjectMilestone` (HRMS-0801/0804, built this session as part of the Projects API). The two are materially different (three-tier PERSONAL/PROJECT/ORG + performance-store integration vs. project-scoped-only binary complete) — extending the shipped, tested table would mean migrating a working API's enum shape for an unrelated feature. Built a new, distinctly-named `employee_milestones` table instead. Completion auto-writes `MILESTONE_COMPLETED` (score 100 on-time / 70 late, `completed_date` always system-set — structurally impossible to backdate) and an idempotent scan-overdue endpoint writes `MILESTONE_OVERDUE` as a negative AI-Assessor signal. Commits `978ac66` backend / `5b57e23` frontend.

**S-359/HRMS-P511 (HTD Intake Pause Engine) — genuinely new, no pre-existing backend**: monthly HTD→Core conversion-rate calculator (cohort = `htd_track` starts that month, converted = `core_certified` within 400 days) + auto-pause when the 2 most recently calculated months are both under 50%. Resume requires 200+ char audit findings AND corrective actions independently. Two things deliberately not wired, flagged rather than guessed: WhatsApp/email notification to "BU Head + Hemant" (no lookup resolves the already-referenced `HEMANT_BU_HEAD` role to a real email/number anywhere in this codebase) and blocking `HRMS-0307 SourcingWorkflowTrigger` (doesn't exist in this codebase). Used three dedicated tables instead of the doc's generic `system_config` key-value store — building a generic config mechanism for one flag would be over-building. Commits `9a415fd` backend / `77cf48c` frontend.

**This closes the Delivery Engine (NEW-RM) cluster** except S-352 (still blocked on S-357/AI Assessor + a missing HRMS-0522 story) and S-355/S-357 (AI-bot stories, held for a design check-in with Avinash — same posture as the Thunder persona question). Full suite: 795 passed, 1 xfailed.

## Session log — 2026-07-23 (continued): Employee Portal partial (S-105, S-102) — session closed here, next epic confirmed as EPIC-04

**S-105/HRMS-P210 (Portal Notification Center)** — same pattern as most of this session: HRMS-0113's notification engine (`send_notification()`/`get_unread_count()`/`mark_as_read()`) already existed and is already called by other stories, but its own model docstring flagged the missing nav-shell UI as the one real gap. Added `get_notifications_for_user()` + `GET /notifications` + `POST /notifications/{id}/mark-read`, wired to a real bell/badge/dropdown in `TopBar.js` (previously a static, non-functional icon). Commits `480cdd7` backend / `3450969` frontend.

**Scope check-in on the remaining 8 Employee Portal stories**: the 4 admin dashboards (S-096–099) + S-103 (BU Head Oversight) + S-104 (Org Role Mapping) reference business concepts that don't exist anywhere in this codebase — AI activity log, SLA alerts, RFP queue, headcount budget, workforce-plan-vs-actuals, "NO MARKET" violation alerts, client escalation flags, panel-split flags, and a BU-scoped role hierarchy (Director/Tech Manager/Tech Lead aren't real RBAC roles here). Asked Avinash how to proceed; he chose "finish S-102's real gap, then pause here" over building dashboards with fabricated/omitted data or scoping the missing concepts as their own stories.

**S-102/HRMS-P207 (Hiring Manager Candidate Review)** — real, scoped gap: `interviews.py` already has a comprehensive interview/feedback backend (feedback CRUD, `get_my_interviews`, statistics, interviewer workload), and its own `HMCandidateReviewListResponse`/`HMFeedbackDetail`/`HMInterviewRound`/`HMCandidateReviewItem` schemas were already fully defined and imported — but wired to no route. Added `get_hm_candidate_review_list()` in `interview_sequencing_service.py` (reusing the same round-level Hire/Reject vocabulary that module's R-05 gate already established, not a second competing definition) + `GET /interviews/hm-review/{hiring_manager_id}`. Deliberately not derived: a "Must Hire" tier (no threshold defined anywhere) and the "must view all feedback before approving" BR (belongs to the existing, separate `PUT /status/{candidate_id}` approval endpoint, out of scope for this gap-fill). Commits `22f0d64` backend / `be1dc54` frontend.

**Session closed here at Avinash's request.** Full suite: 808 passed, 1 xfailed. Both repos clean, everything pushed. Asked what's next — Avinash ruled out Sub-Vendor Portal ("last one, no direct business value") and named **EPIC-04 / Candidate Engagement & AI Recruiter Platform (S-001–S-080, 80 stories)** as the next cluster — see `[[wros_project_status]]` memory for the full scoping note and the real pre-existing service files found via a quick grep (`ai_conversation_service.py`, `outreach_agent_service.py`, `orchestration_router_service.py`, etc.). This is the same subject area as "Thunder," which was paused earlier this session — Avinash's redirect supersedes that pause. Given the epic's size and heavy AI/agentic surface (Supervisor Agent, Voice Calling Framework, AI Response Generation, Intent Detection), next session should scope it properly against the canonical sheet fresh (don't trust a stale cached list) before building, and expect to hold several genuinely-new AI pieces for a design check-in, same as S-355/S-357.

**Canonical sheet still Excel-locked at session close** — the growing queue of rows to mark `Done` + Change Log now spans nearly every cluster built this session (see `[[wros_project_status]]` for the full list). Avinash's 8-hour no-save window (from ~03:15 EDT) should have elapsed by next session, but confirm the file is actually closed before attempting a save, don't just trust the clock.

## Session log — 2026-08-05: DESIRE epic verified complete; priority pivot to a CEO P&L/predictive-analytics dashboard + net-new Partner ROI Agent; EPIC-02/03 scoped but NOT started — genuinely paused on a live cross-machine coordination issue, read this before touching anything

**DESIRE epic (S-346–350, "Desire Intelligence System") confirmed fully built end-to-end**, not just claimed by its own commit message — verified against all 5 requirements docs individually (chat widget, signal engine, profile builder, motivation engine, HR briefing tab), backend commit `478685e` / frontend commit `bb6ef17`. One real, deliberate spec deviation found and already documented in the code itself, not silently reconciled: `app/api/v1/endpoints/desire_intelligence.py`'s own docstring records that Avinash explicitly widened S-350's "HR/Director only" viewing restriction to "everyone who can see the candidate at all," keeping the edit-gate (`candidate.desire_intelligence.edit`) narrower (Partner/BU Head/HR Manager/Super User). Canonical sheet still shows S-346–350 as stale `NEW – Ready for Build` — **not yet flipped to Done**, deprioritized when the pivot below happened; do this first if picking DESIRE-adjacent work back up.

**Avinash pivoted mid-session** to a much bigger ask: predictive analytics + graphs + a real P&L screen, a single CEO-facing "FY Progress" dashboard (revenue standing, money leakage, what's needed to hit the quarter/month/year goal), and a **net-new Partner ROI-nurture agent** ("an agent Avinash that nurtures the partner" on spend-vs-return, e.g. travel spend). Mapped this against the real backlog rather than guessing scope — it lines up almost exactly with two entirely unbuilt epics:
- **EPIC-02, Revenue Visibility Engine** (S-230–244, 15 stories) — Executive Revenue Dashboard, Forecast vs Actual, Revenue Leakage Detection, Pipeline Coverage Ratio, Opportunity pipeline/forecast.
- **EPIC-03, Workforce Planning & Demand Forecasting** (S-267–280, 14 stories) — BU Revenue Target, 30/60/90-day hiring forecast, AI Revenue-to-Workforce Conversion, Plan vs Execution Variance.

**Genuine live coordination issue found before any of this was touched — this is why nothing got built yet.** A `git status` check at Avinash's own request turned up 18 uncommitted files in this repo (14 modified, 14 untracked — migrations, `app/core/bu_scope.py`, `app/core/revenue_visibility_scope.py`, `app/core/msgraph_session_store.py`, `app/services/msgraph_mail_sync_service.py`, `app/services/lifecycle_communication_linking_service.py`, matching tests) plus 4 in the frontend repo (`AuthPage.js`, `auth.js`, `AskThunderWidget.js`, `askThunder.js`). **Both repos are 0 ahead / 0 behind `origin/main`** — nothing committed-but-unpushed, so there was nothing to push. Investigated the uncommitted content thoroughly before reporting it (full diff read, app-import check, targeted test run — 73/73 passed, migration chain verified clean, frontend/backend call sites cross-checked) to be able to honestly answer "is this safe" — it is: coherent, complete, well-documented, no stubs/TODOs. It splits into two real threads, both dated 2026-08-05 in their own docstrings:
1. **EPIC-02/03 access-control foundation** — `revenue.view`/`revenue.view_pnl` RBAC permissions, a new `Finance` role, `Client.business_unit_id`, and `bu_scope.py`/`revenue_visibility_scope.py`'s BU-scoped visibility gates. Docstrings quote Avinash directly on the access spec ("Epic 2,3 is only visible to ceo, partner (only for their BU); BU head (only for their BU); finance & HR manager (no actual p&l)" and "a partner has it's own clients and the work is done in their BU only"). **This is directly the access layer EPIC-02/03 need** — good news, not wasted work.
2. **EPIC-14/S-435 lifecycle mail linking + candidate email 2FA** — unrelated to the above; M365 poll-based mail sync linking real emails to the Activity Timeline (metadata-only, no body content), plus an opt-in candidate email-OTP login challenge.

**Confirmed directly with Avinash: this is his own work on another machine, not this session's to touch.** Per explicit instruction, none of it has been staged, committed, or modified — he'll close it out himself once at his desk. **Any future session picking this up: check `git status` again before assuming this description is still current — if those files are gone, he's already committed them himself; don't try to "finish" or recreate anything from this list.**

**"Agent Avinash" already exists** — pulled up per Avinash's request: `8bd81a7` + `d76ad18`, "Executive Signal & Culture Agent," an advisory-only 2026-08-04 redesign of an earlier, more autonomous "Agent Avinash" spec that was rejected specifically because it staged messages as personally-written by Avinash without his sign-off and let an agent act on personnel matters unilaterally — both fixed by a strict draft-then-approve pattern (`RecognitionMessageDraft`, `approve_and_send_recognition()`) and read-only org-health aggregation (`org_health_service.get_org_health_snapshot()`, pulling real recruiting/revenue-leakage/bench/task signals, explicitly not fabricating Client/Project Health scores that don't exist). **This is the confirmed architectural precedent for the new Partner ROI Agent** — Avinash confirmed the new agent reports to the same person (him) and gave no naming constraint; proposed name **Partner ROI Agent** (plain descriptive, matching this codebase's existing "Executive Signal"/"Culture Agent" naming, not a second personified name).

**EPIC-02/03 scoping findings** (investigated, not yet built — see below for why):
- **Opportunity backend already built** (`eb40777`, `app/services/opportunity_service.py`) covering 5 of EPIC-02's 15 stories at the function level — `create_opportunity()`/`transition_stage()` (S-236), `calculate_weighted_forecast()`/`aggregate_weighted_forecast()` (S-238), `create_role_demand_from_opportunity()` (S-239), `calculate_revenue_potential()` (S-240), `calculate_pipeline_coverage_ratio()` (S-244) — zero API, zero UI, same recurring gap as everywhere else in this build.
- `business_units` table already exists (`app/models/rbac.py`, from S-205/HRMS-0107) — the foundation S-267 (Set BU Revenue Target) needs is already there.
- **S-243 (EPIC-02 Revenue Leakage Detection) is real and distinct from the already-shipped S-225/HRMS-0906 timesheet-based leakage** — S-243 is a 4-pattern engine (stalled opportunities, unfilled demand past need-date, unbilled time, sub-vendor cost overruns); reuse the existing timesheet check for the "unbilled time" pattern, build the other three new.
- **Real infra gap**: S-267 and S-242 both cite `analytics_fact_revenue`, an ETL fact table that doesn't exist anywhere in this codebase. Proposed fix, not yet confirmed: compute actuals directly from the already-real `Invoice`/`InvoiceLineItem` tables instead of inventing a fake ETL layer — same move this codebase already made for `org_health_service.py` and the S-256 forecast service.
- S-242 (Forecast vs Actual) depends on S-279/HRMS-0313 (Plan vs Execution Variance, itself in EPIC-03) — the two epics aren't strictly sequential, S-279 needs to land before S-242 can.
- S-278/HRMS-0312 ("Workforce Scenario Planning") reconfirmed as genuinely distinct from the Core-Pull engine that used to be miscited under the same ID earlier in this project's history — not a duplicate, just still unbuilt.

**Proposed build order** (given to Avinash, not yet approved for execution): 6 weekly dependency-ordered slices across EPIC-02+03 (Opportunity API/UI → BU Revenue Target + Exec Dashboard → Forecast-vs-Actual/Variance/Leakage → Revenue-to-Demand mapping → bench/gap matching, reusing the existing `resource_forecast_service.get_skill_gap_analysis()` rather than rebuilding it → sourcing/job-req/scenario-planning), a CEO "FY Progress" screen pulling from all of it once Week 3 has real numbers, and the Partner ROI Agent as a parallel, non-blocking track.

**Partner ROI Agent design proposal** (net-new, no requirements doc exists anywhere — confirmed via full-corpus search): `PartnerGoal` (per-Partner revenue target, sibling to the new `bu_revenue_targets` table, not a fork — a BU's target is the sum of its Partners' goals via the same join-not-copy discipline as `Client.business_unit_id`), a new `PartnerSpendRecord` (category/amount_usd_cents/note — **manual entry proposed as the MVP since no expense/accounting integration exists anywhere in this codebase**, flagged as the one real open question rather than assumed), an ROI calc (revenue from the Partner's clients vs. their logged spend over the same period as their `PartnerGoal`), and the agent itself following the Executive Signal precedent exactly — drafts a nurture message, never auto-sends, approved by BU Head or Avinash, its rollup folded into `org_health_service.get_org_health_snapshot()` as a new `partner_roi` section so it surfaces in the same place Avinash already checks.

**Status at this log entry: nothing in EPIC-02/03 or the Partner ROI Agent has been built yet.** This is confirmed scope + a proposed plan only, deliberately paused on two real blockers, not started prematurely: (1) Avinash's own uncommitted work above needs to be closed out on his end first so nothing gets duplicated or overwritten, (2) the spend-tracking data-source question (manual entry vs. a real integration) is still open. **Next session: check whether both are resolved before writing any code** — re-run `git status` to confirm the other machine's work landed, and confirm the spend-tracking answer, rather than assuming either from this entry alone.

**Follow-up, same day — blocker (1) resolved, confirmed independently, not just taken on the other session's word.** The other machine's session closed and reported back; re-ran `git fetch`/`git status`/`git log` on both repos before trusting the report — both clean, both 0 ahead/behind `origin/main`, commits exactly as reported: `49223eb` backend / `5c986f6` frontend.

**What actually landed** (this is now real, on `main`, not proposed): EPIC-14 M365 Stage 1 (S-379, S-435 — the mail-linking/sync work from the entry above; Teams/S-381 explicitly deferred, his own call); 3 Group B bugs closed (HR Manager BU visibility → `bu_scope.py`, candidate email-2FA opt-in, Ask Thunder cross-message memory via client-held history in the LLM prompt); and the full EPIC-02/03 RBAC foundation described above (`Finance` role, `revenue.view`/`revenue.view_pnl`, `Client.business_unit_id`, `bu_scope.py`/`revenue_visibility_scope.py`) — matches what was already documented here, now confirmed merged rather than sitting uncommitted. Regression: 2003 passed, 1 pre-existing flaky.

**Real detail refining the Partner ROI Agent design above, from Avinash directly**: the actual mechanism he wants is **Thursday weekly Partner check-ins, agent-driven** — not just an on-demand ROI read. This should shape the agent's trigger model (a scheduled weekly cycle, same posture as the Culture Agent's quarterly cycle, not purely event-triggered) once building starts. He also named a third dashboard component not previously scoped: **bench-vs-market P&L per opportunity** — needs its own scoping pass against EPIC-02/03/EPIC-05 before building, not yet checked against the requirements corpus.

**4 production bugs surfaced by the other session, flagged for triage, not yet investigated by this one**:
1. Dashboard shows 3 jobs when the real count is 15.
2. Feedback form needs its hire-decision field split (unclear into what — needs the other session's own clarification or a fresh look at the actual form).
3. AI interview decision isn't visible on screen anywhere.
4. Interview feedback field assignment needs clarification (vague as reported — don't assume a fix without re-scoping what's actually wrong).

**Two new schema-unknown areas raised, real decisions needed before any build touches them**: vertical org growth (job levels/seniority tracking — no such model exists anywhere in this codebase today) and internal activity visibility for non-revenue-generating roles (distinct from the revenue.view/EPIC-02/03 access model above, which is revenue-generator-scoped by design). Not scoped against the requirements corpus yet.

**Remaining blocker for EPIC-02/03/Partner-ROI-Agent build start: only the spend-tracking data-source question (manual entry vs. real integration).** Everything else that was pausing this work is now resolved — next session can move straight to Week 1 of the proposed build order once that one question is answered, or make the manual-entry MVP call and flag it, consistent with how this project has always handled a single remaining low-stakes open question.

## Session log — 2026-08-05 (continued): Revenue Visibility Agent — weekly scoring workflow confirmed with Avinash; BlitzenX Operating Model v1.0 formalized

**Real doc-drift caught while brainstorming the agent's core calculation**: S-242 (Forecast vs Actual) cites "HRMS-0313 Plan vs Actuals" as its source for "actual closed revenue per BU per month." Read the real S-279/HRMS-0313 doc directly — it's **hiring headcount variance** (planned heads vs actual hires, `analytics_fact_hiring_funnel`), not revenue at all. S-242 doesn't actually depend on S-279; revenue actuals still have to come from `Invoice`/`InvoiceLineItem` either way, same as the `analytics_fact_revenue` non-existence already flagged above. Don't build S-279 as a revenue-actuals prerequisite for S-242 — they're unrelated stories that happen to share a doc's mistaken cross-reference.

**Two open design questions from the earlier entry, answered directly by Avinash — and they reshape the whole agent, not just fill in a blank**:

1. **CEO sets the target, not BU Head/Partner.** "Ceo sets the target that's why the agent should think and work like avinash." This overrides S-267's own written BR ("Only BU Head, Director and Admin Can Set Revenue Targets") for the Partner-level goal specifically — S-267's BU-level target-setting rule stands as-is for `bu_revenue_targets`, but the new `PartnerGoal` is CEO-set, and the agent's whole design center is that it reasons about revenue the way Avinash would, not as a neutral calculator.

2. **One weekly touchpoint, not two** — spend/ROI folds into the same Thursday cycle rather than a separate ping, confirmed.

**The actual weekly workflow, as Avinash described it — this is now the authoritative design, not the earlier speculative trigger list**:
- **Thursday morning**: the agent asks BU Head for their inputs first.
- Agent then tells the Partner "this is where we are" and asks the Partner for their inputs.
- Agent **calculates the score** from both sides' inputs — the agent computes it, no one manually enters a score.
- **Nobody can manually change the score except the CEO.** BU Heads and Partners supply inputs; only Avinash can override the computed number.
- Output: a weekly progress report.

**The actual scoring inputs, given directly — this is the real spec for what `get_revenue_position()`/the agent has to gather, more concrete than the KPI lists in the Operating Model section above**:
- **Partner side**: Who are the target clients? → Did we make any contact there? → **If no progress, red flag.** (A pure activity/pipeline-motion check, not a revenue-number check — matches the Partner KPI list's Net New Logos/Practice Growth emphasis.)
- **BU Head side**: Current headcount → delivery cadence → hiring cadence → **does it match what the Partner is building/forecasting?** (An alignment check between delivery capacity and commercial pipeline, not a standalone delivery-quality score — matches the BU Head KPI list's Hiring Velocity/Forecast Accuracy emphasis, and is the concrete mechanism for BR-0212-01-style "one shared calculation" thinking: the BU Head side is only meaningful relative to what the Partner side is forecasting, not scored independently.)

**Net effect on the build plan from the prior entry**: `get_revenue_position()` needs to become two real input-gathering steps (BU Head weekly input, Partner weekly input) feeding one CEO-owned, agent-computed score — not a passive read of `Invoice`/`Opportunity` data alone. Actual revenue/forecast numbers (from `Invoice` and `calculate_weighted_forecast()`) are the objective backing data the agent reasons over; the weekly BU Head/Partner inputs are the qualitative signal layer on top, similar in spirit to how DESIRE's signal-collection layer feeds its profile builder rather than a single number. Still not built — this is confirmed design, not yet code. Same remaining blocker as the prior entry: the spend-tracking data-source question is still open, and the exact score formula (how BU Head input + Partner input + objective revenue data combine into one number) still needs to be worked out before writing `get_revenue_position()` for real.

**BlitzenX Operating Model v1.0 formalized in its own section above** (see "BlitzenX Operating Model (v1.0)") — the enterprise structure (AXION/Troy+Hemant, PRISM/Curtis+Manian), the US=Partner/India=BU Head delivery-ownership rule, BU-scoped resource ownership, the Talent Engine (SPECIALITY/HTD/TIP), and the Partner vs BU Head KPI lists. This is now the standing reference for any future EPIC-02/03/05/Partner-ROI-Agent work — read it, don't re-derive it from scattered conversation history.

## Session log — 2026-08-05 (continued): Specialty-vs-Partner-revenue attribution decided, against Avinash's real finance model — structure/logic only, no figures ever go in this file

**Avinash shared BlitzenX's actual live FY26-27 finance operating-model workbook (Finance/Admin Expenses, a "Testing" build) directly, with an explicit instruction: the real data in it must never leave that workbook** — not into this file, not into any commit, not into any output. Read it once, locally, to extract structure and formula *logic* only (sheet layout, column/field names, the shape of existing formulas, status-band thresholds) — no dollar figures, salaries, invoice amounts, or the individual employee roster are recorded anywhere below or ever will be. **Standing rule for every future session: if this workbook (or any real financial data) is shared again, read for structure/decisions only — never paste actual figures into this repo.**

**What the real workbook already encodes, and why it settles the Specialty-attribution question**: every invoice/roster row in the real system carries a `Line Type` field — values include `Core`, `Specialty`, `BU Head`, `Partner`, plus corporate-function tags. Revenue is also tagged `Home BU` + `Client` + `Employee`. **Real finding worth flagging, not fixed (not this session's file to touch)**: the live "Partner Revenue" formula in that workbook currently sums a BU's invoices by `Home BU` only — it does **not** filter out `Line Type = Specialty`, so Specialty-billed revenue is currently flowing into the Partner's own revenue ratio alongside Core. Worth Avinash's own look at whether that's intentional.

**Decision for WROS's scoring model** (the `get_revenue_position()`/Partner-vs-BU-Head design from the entries above): **Specialty revenue counts toward BU Head's operational score only; Core and direct-onsite revenue counts toward the Partner's own revenue-attainment number.** Rationale: Specialty's whole purpose per the Operating Model is protecting bench economics/zero idle cost — a BU Head operational concern, not Partner-sourced new business. This reuses the split via the already-existing `Employee.delivery_engine` field (SPECIALITY/CORE, from the Core-Pull work) rather than inventing a second taxonomy — **flagged for explicit reconciliation before building**: confirm `delivery_engine`'s two values map cleanly onto the real workbook's `Line Type` concept, since they should be the same thing under two names.

**Real, already-in-production Partner performance metric found, worth reusing rather than re-deriving**: the workbook already computes a Partner ratio (this month's BU revenue ÷ the Partner's own loaded cost), banded Exceeding/On Track/Safe Mode/At Risk. The new Partner ROI Agent's scoring should be consistent with this existing yardstick, not a competing formula invented from scratch.

**Project revenue signal — decided it feeds both dimensions, but as two different metrics, not one number double-counted**: a project's revenue trend feeds the P&L dimension (Partner, if Core/onsite); its milestone/utilization data separately feeds the delivery dimension (whichever side — Partner or BU Head — owns that resource's `delivery_owner`). Gated by the same Line Type/delivery_owner split above — a Specialty project's revenue never reaches the Partner's P&L number, only BU Head's operational one.

**Employee pulse-check cadence**: going with **monthly** as the default (Avinash offered monthly-or-quarterly; monthly keeps the Thursday cycle from running on sentiment up to a quarter stale, still well clear of weekly survey fatigue) — flag if he wants quarterly pinned instead once this is actually built.

**`delivery_owner` on `EmployeeAllocation` confirmed as real, needed scope** — Avinash: "Yes this is something we need to capture." Per-allocation (not per-employee), same reasoning as the entry above: an India-based person can be flown onsite for one engagement and be fully offshore for the next, so ownership has to live on the allocation, not permanently on the employee.

**Status: scoring model design is now substantially validated** (weakest-link-of-three structure from the prior entry, Specialty/Core attribution now resolved against real accounting logic, project-signal dual-feed resolved, pulse-check cadence set) — still not built. Remaining before code: confirm the `delivery_engine`-vs-`Line Type` reconciliation, and the original spend-tracking data-source question from the earlier entry is still open.

## Session log — 2026-08-05 (continued): remaining design questions closed; real EPIC-14 scoping correction found; readiness check across EPIC-02/03/09/14

**Three remaining open items from the prior entry, answered directly by Avinash**:
- **`Employee.delivery_engine` IS the Partner-vs-BU-Head delivery split, confirmed 1:1**: "core is only by partners and specialty is only by offshore BU." This removes the need for a separate `delivery_owner` field on `EmployeeAllocation` — deriving it from `delivery_engine` (CORE→Partner, SPECIALITY→BU Head) at read time is sufficient and avoids two fields that could drift apart. One small addition instead of a new field: snapshot `delivery_engine` onto `EmployeeAllocation` at creation time, purely for historical accuracy (a later Core-Pull on the employee shouldn't retroactively reclassify what an old, already-ended allocation was at the time).
- **Expense/spend tracking is manual entry**, shaped to match the real workbook's `Expense Ledger` structure (Avinash: "counted same like the excel"). Real column structure captured (headers/schema only, no figures, per the standing no-real-data rule two entries up): Month, Year, Expense Category, Subcategory, Employee/Partner, Location, Client, Home BU, Amount (USD), Description, Invoice/Receipt Ref, Approved By, Date, Payment Status. The new WROS spend-tracking table should mirror this shape rather than inventing a different one.
- **Pulse-check cadence: monthly**, confirmed as the working default from the prior entry's proposal.

**Real scoping correction, not just confirmation — EPIC-14 does NOT give us the Thursday BU-Head/Partner cycle mechanism**, contrary to the working assumption when Avinash asked whether EPIC-14 was ready. Read all three "Role-Based Check-In Scheduler" stories directly (S-382/HRMS-1404 Dynamic Reporting Hierarchy Engine, S-383/HRMS-1405 Check-In Cadence Configuration, S-384/HRMS-1406 Auto-Scheduler & Calendar Booking Agent): this is a **generic manager↔direct-report 1:1 calendar-booking engine** — resolves each employee's org level from the reporting chain, applies a cadence per level (baseline: BU Head 90 days, Principal Architect 30 days, Technical Manager 7 days), books real Outlook calendar invites via Graph API, handles decline/reschedule. No Partner cadence in its baseline, no structured input collection, no scoring — it just makes sure human 1:1s get booked. Genuinely useful, genuinely buildable, but **an independent track, not a prerequisite for EPIC-02/03 or the Partner ROI Agent.**

**What EPIC-14's own docs point to instead**: S-384's "Not In Scope" section names **S-355/HRMS-0516 (Reporting Manager Weekly Input Bot)** as "the separate, distinct performance-input mechanism" — this is the story already flagged as held pending a design check-in, back in the 2026-07-23 NEW-RM cluster session log entry ("S-355 and S-357 are both new AI/agentic features... holding both for a design check-in with Avinash before building"). Read it now: a Friday-4pm WhatsApp bot that asks each reporting manager 6 structured questions (1-5 scored) per direct report, parses the reply, escalates to the manager's own BU Head after 3 consecutive missed weeks (a real accountability signal against the *manager*, not just a data gap), one report per employee per week with no backfilling. **Scoped to individual employee performance** (feeds `HRMS-0515` Performance Store) — genuinely different subject matter from BU/Partner revenue visibility, still a separate open decision, not resolved by anything in this thread. But its **mechanical pattern (scheduled dispatch → structured reply parsing → one reminder → escalation-on-repeated-non-response, tracked against the non-responder)** is exactly what the Thursday BU-Head/Partner cycle needs, and should be reused rather than re-designed from scratch. Open question for Avinash, not yet answered: build a new, distinct BU-Head/Partner-scoped sibling bot now, or fold S-355 itself into scope at the same time since the two would share almost all their dispatch/parse/escalate plumbing.

**EPIC-09 (Time Tracking & Revenue Capture) — likely already fully built, not new work**: canonical sheet shows S-223 through S-229 as `Done`, but S-220/HRMS-0901 (Create Weekly Timesheet), S-221/HRMS-0902 (Validation & Submission Lock), S-222/HRMS-0903 (Approval) still show `Planned` — same stale-sheet pattern flagged repeatedly elsewhere in this file. Per this project's own build history (Timesheet/TimesheetEntry submission+approval built early, full `/timesheets` API added later during the MVP-chain fix), this is very likely already done in full. **Needs a verification pass + sheet correction next session, not a new build.**

**Readiness verdict, given directly in response to "do we have everything you need for EPIC 2,3 and the 2 finance epics (14 and 9)"**:
- **EPIC-02/03**: yes, ready to start — scoring model (weakest-link-of-three), Specialty/Core attribution, project-signal dual-feed, and `delivery_engine`-derived ownership are all resolved.
- **EPIC-09**: nothing to build, pending a quick verification pass to confirm and correct the sheet.
- **EPIC-14**: real and buildable but independent of EPIC-02/03 — don't block on it.
- **The actual missing piece for the Thursday cycle**: a new BU-Head/Partner-scoped weekly input bot, sibling to S-355, not yet built, not yet formally scoped as its own story — reuse S-355's dispatch/parse/escalate pattern when building it.

## Session log — 2026-08-05 (continued): "BU Head doesn't drive revenue" confirmed against real Client Master structure; EPIC-16 status corrected (genuinely 0/15 built); EPIC-09 confirmed built; green light given for EPIC-02/03/16/09 non-stop development

**Real Client Master structure confirms Avinash's point directly, no inference needed**: `Client Name | Line Type | Originating BU | Client Owner (Partner) | Date Added | Notes | Client Bonus Status`. Revenue ownership attaches to a **Partner directly** (`Client Owner (Partner)` — a real field, values include Curtis Matthews/Troy Towsley/Avinash Mukund), with `Originating BU` as a separate, secondary field. **Confirms and hardens the scoring design from two entries up: BU Head Score has zero revenue-attainment component, full stop.** Revenue-vs-target lives entirely on the Partner side (and the CEO, who sets it); BU Head's score is 100% delivery/operational (headcount, hiring cadence, delivery cadence, utilization, bench health).

**Also confirms a design refinement to make before building**: `Client.business_unit_id` alone (what the other session's RBAC-foundation work added) is a reasonable proxy today given the 1:1 Partner:BU pairing, but the real system's actual source of truth is Partner-level ownership on the client itself — worth adding a direct owner-partner reference on `Client`, not just BU, both because the real data models it that way and because Avinash-as-CEO can personally own a client independent of any BU's Partner (goes to Corporate, see the client-attribution law below).

**EPIC-16 (Finance & Accounting Operations) status corrected — this file's own top section has been wrong about this all session.** The original master-context note ("Some still pending: P&L Engine, Reserve Fund, Hiring Affordability, Partner Incentive Calculator, Executive Dashboard, GST placeholder") implied the other ~9 EPIC-16 stories were done. **Verified by grep, not assumption: zero EPIC-16 concepts exist anywhere in this codebase** — no `reserve_fund`, `hiring_affordability`, `partner_incentive`, `fully_loaded_cost`, `min_bill_rate`, `accounts_receivable`, or `intercompany_settlement` in any service or model file. **All 15 EPIC-16 stories (S-387–401) are genuinely unbuilt** — the canonical sheet's `Ready for Build` status is the accurate one here, not the old prose note. Real silver lining: the actual FY26-27 finance workbook Avinash shared (see two entries up) already encodes working versions of several of these as manual Excel formulas — Fully Loaded Cost (Roster columns), Blended Delivery Rate, BXIN/BXUS P&L (Location P&L/BU P&L sheets), Reserve Fund Ledger, Hiring Affordability sheet, Intercompany Ledger, a Partner incentive ratio. **Build these WROS engines to match the real formulas already in production use in that workbook, not a reinvented calculation** — structure/logic only carries over per the standing no-real-data rule, but the calculation shape (e.g., fully-loaded-cost = salary + statutory components + admin overhead allocation, not a simplified version) should match what Avinash already trusts.

**EPIC-09 (Time Tracking & Revenue Capture) confirmed built, not just assumed**: checked `app/api/v1/endpoints/timesheets.py` directly — weekly-draft, entries upsert, submit, approve, reject, reopen, bulk-approve, list, get all real and routed. S-220/HRMS-0901, S-221/HRMS-0902, S-222/HRMS-0903 are done despite showing `Planned` in the canonical sheet — **sheet correction needed, not a build task.**

**New hard business rule, given directly, folded into the Operating Model section above ("Client attribution locking")**: a client's BU is permanently locked to whichever Partner's own org tree sourced it — Curtis's clients → PRISM only, Troy's → AXION only, Avinash's own → Corporate, never cross-assignable. If a Partner later hires their own sales org underneath them, every client that org sources still rolls up to the same Partner's BU — the tree never crosses group no matter how many layers get added. **This is a real validation rule for `Client`/Opportunity creation, not just descriptive** — BU (and owner-partner) must be derived from the sourcing user's own org position at creation time, never a freely-editable field a user can misassign (accidentally or otherwise). Full `S-382`-style reporting-hierarchy walk doesn't exist yet — MVP enforcement uses the creating user's own `business_unit_id` directly (already on `Users`/`Employee` from the other session's RBAC work) until S-382 lands, at which point this can walk the real chain instead of relying on direct BU membership alone.

**Green light given: "EPIC 2,3,16,9 - are all now go non stop development."** Corrects the earlier "14 and 9" — the two finance-adjacent epics are **16 and 9**, not 14 (14 is the unrelated check-in scheduler, see entry above). Starting execution now, story-by-story per this file's standing Definition of Done (UI+API+backend+tests), same rhythm as every other cluster this project has built. First concrete slice: client/opportunity BU-attribution enforcement (the law above) + Opportunity API/UI (S-236/237/239/240 — backend already real, zero API/UI, see two entries up).

**New Partner dashboard requirement, given directly, folds into the Partner ROI Agent / `PartnerGoal` design (task 14 in this session's tracked task list)**: a Partner's FY revenue variance (+/-) is asymmetric across year boundaries — **a negative year carries forward as an accumulated deficit into the next FY** (doesn't reset — the Partner still owes it), **but a positive year does NOT carry forward as credit against a future target** — it's shown as a separate, distinct "+" surplus figure for that FY only. Two fields on the Partner's own dashboard, not one netted number: a running `cumulative_deficit_usd_cents` that only ever accumulates on negative years (reduced only when a Partner actually earns their way out of it), and a `current_fy_surplus_usd_cents` that's purely descriptive of the current year, never banked forward. This is deliberate asymmetry, not an oversight — avoids the classic sandbagging problem where a good year just raises next year's bar.

**First real implementation shipped this round, not just planned**: `create_client()` in `client_service.py` — the first client-creation path that has ever existed in this codebase (confirmed by grep before writing it: the old GET-only `/clients` endpoint's own docstring claimed a create path existed "elsewhere," it didn't — another doc-drift instance, fixed in the same commit as the code). Enforces the attribution-locking law above: BU is taken from the creating user's own `business_unit_id`, never a caller-supplied field; a BU-less creator (Super User/CEO/Corporate function) falls back to the `Corporate` BU row if one exists, else the client is left unassigned (Org-Pool-until-claimed, matching the model's own pre-existing docstring) rather than guessed. `POST /clients` added alongside the existing `GET /clients`. 4 new tests (`test_client_creation.py`), full suite green. **`BusinessUnit` rows are admin-created via the RBAC endpoint, not auto-seeded with fixed names** — confirmed by grep — so a real deployment needs an Admin to actually create the `Corporate` BU row for the fallback to resolve to anything other than null.

## Session log — 2026-08-05 (continued): "finish everything you can build on your own" — Client Management UI + Create Job company-field fix shipped, 3 pre-existing build-breaking bugs fixed

**Avinash flagged a real, concrete UI gap while this was in progress**: Create Job's "Company / Client" field was free-form text, not backed by the real `Client` table at all — a job could reference a "company" that isn't a tracked client anywhere. He also confirmed there was no client add/edit screen anywhere in the UI (matching the backend gap found two entries up).

**Closed both, backend + frontend, full Definition of Done**:
- `update_client_details()` (`client_service.py`) — general "edit client" path. Deliberately excludes `business_unit_id` (locked, never editable through a general update — see the attribution law), `status` (its own sanctioned path is `set_client_status()`, which enforces the BR-01 contact requirement — bypassing it here would skip that check), and `markup_rate_pct` (BR-02 confidential, restricted to `MARKUP_VISIBLE_ROLES` at read time — a write path here would let a broader-access edit endpoint bypass that restriction). `GET /clients/{id}` and `PATCH /clients/{id}` added. `GET /clients` gained an `active_only` filter (default `True`, excludes `INACTIVE`) and now returns `status` on each row.
- `ClientManagementScreen.js` (new) — the first client add/edit screen in this app. List + inline add form + inline edit form, wired at `/client-management`, nav item added next to Core-Pull (same 4-role-branch pattern every other Resource-Management-adjacent screen already uses). UI explicitly does not expose BU as editable, matching the backend lock.
- `JobCreate.js` — "Company / Client" is now a `Select` populated from the real active client list (`GET /clients`), not a free-text `Input`. Storage is unchanged (`company_name` string, matching the legacy Jobs table's existing free-text column — not migrated to a `client_id` FK, since that boundary is the same flagged-not-resolved Demand-vs-Jobs architectural question noted elsewhere in this file, out of scope for a UI fix). The dropdown just constrains the *value* to a real client name instead of letting anything be typed.
- 3 new backend tests (`test_client_creation.py`, now 7 total) plus the existing 4, full backend suite still green, app imports clean.

**Real, pre-existing, unrelated bug found and fixed while verifying the frontend build**: `npm run build` failed outright — not from anything touched this round. Three files (`BUSwitcher.js`, `BuddyProgramScreen.js`, `ErrorLogScreen.js`) each had a stray `// eslint-disable-next-line react-hooks/exhaustive-deps` comment referencing a rule that isn't configured anywhere in this project's eslint setup — CRA treats an unknown rule name in a disable-comment as a hard compile error, not a warning. Exact same bug, same root cause, already fixed once before in `TimesheetsScreen.js` earlier this session — recurred in three more files since. Removed all three (each one's own dependency array was already complete; the disable comment was pure dead weight, not suppressing a real issue). **Confirmed the real build succeeds after the fix** (`npm run build`, no `CI=true` — that env flag promotes normal warnings to hard failures and isn't how this project's actual build runs; a separate, pre-existing, unrelated `antd`/`postcss-calc` CSS-minifier warning about modern `tan()/atan2()` CSS functions shows up only under that stricter mode and was left alone — vendor CSS, not something to chase as a side effect of this fix).

**Status**: this closes the Client CRUD gap end-to-end (create, list, view, edit — all wired UI-to-DB) and the Create Job data-integrity fix. Continuing into the tracked task list (Opportunity API/UI next) in the same session.
