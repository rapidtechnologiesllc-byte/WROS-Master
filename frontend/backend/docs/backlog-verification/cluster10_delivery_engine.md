# Cluster 10 — Delivery Engine & Performance Intelligence (NEW-RM + NEW-RM-EXT)

Verified directly against code in `OnboardingModule-Backend` and `OnboardingModule-Frontend-main`.
Sheet's Status column was NOT trusted as evidence — every row below is backed by a file:line citation.

Read first: `docs/build-package/PHASE-4-UI-INTEGRATION-COMPLETION-PROMPT.md` — establishes that as of the
start of this cluster's build round, Core-Pull Engine (S-353), Specialty Pool Guard (S-373), Resource
Management Agent (HRMS-1105, not itself in this 24-story list), and Confirmed vs Potential Demand
(S-372) had real, tested backends but **no API and no UI**. That gap has since been closed for those
four — API + UI + tests now exist and are routed in the frontend (`Approutes.jsx`). This verification
pass confirms that closure and checks the remaining 20 stories against the same standard (UI + API +
business rules + tests, per Avinash's stated Definition of Done).

## NEW-RM (9 stories) — sheet claims 6/9 Done

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-351 | HRMS-0535 | Delivery Engine Assignment — Speciality vs Core | Done | **CONFIRMED-DONE** | `app/models/employee.py:148,151` (`delivery_engine` enum, `core_certified` bool + DB CHECK at `:170-173`); `app/services/employee_service.py`; `app/api/v1/endpoints/employees.py`; `tests/test_employee_entity.py`, `tests/test_employees_api.py` | All hires default SPECIALITY, DB-level CHECK constraint blocks CORE without `core_certified=1`. Sheet's WROS ID was corrected mid-session from a colliding HRMS-0512 to HRMS-0535 per the cluster brief — code itself just says "S-351" in comments, doesn't reference either WROS code, which is consistent with the correction note. |
| S-352 | HRMS-0513 | Core Eligibility Gate — Performance Gate Workflow | Ready for Build | **NOT-DONE** | No hits for `S-352` or `HRMS-0513` anywhere except 3 incidental hits in `HRMS-0513`-adjacent files that are not this feature | No sequential two-step approval workflow exists. Its own dependencies (S-357 AI assessment, S-366 90-day cert clock) are also not built, so it structurally cannot exist yet. Sheet is accurate. |
| S-353 | HRMS-0514 | Core-Pull Conflict Rule Engine — Core Wins Policy | Done | **CONFIRMED-DONE** | `app/models/core_pull.py` (`CorePullEvent`); `app/services/core_pull_service.py:173-346` (`detect_core_pull_conflict`, `execute_core_pull`, `override_core_pull`); `app/api/v1/endpoints/core_pull.py:134-198` (execute/override/replacement-plan routes); frontend `src/screens/CorePullScreen.js` (execute/override UI, wired at `Approutes.jsx:482,707`); `tests/test_core_pull_engine.py` (22 tests), `tests/test_core_pull_api.py` (12 tests) | This is one of the 4 stories the Phase-4 doc flagged as backend-only; API + UI have since been added and are real, not stubs (override requires 100-char justification, enforced both client and server side). |
| S-354 | HRMS-0515 | Employee Performance Intelligence Store | Done | **CONFIRMED-DONE** | `app/models/performance_store.py:25` (`EmployeePerformanceEvent`, append-only); `app/services/performance_store_service.py`; `app/api/v1/endpoints/employees.py:535-549` (`GET /{employee_id}/performance`, explicitly BU Head/RM/HR-only, never the employee); frontend `src/screens/EmployeeDirectoryScreen.js:472-500` (`PerformanceMilestonesPanel` calling `getEmployeePerformance`) | Confirmed real end to end, folded into the Employee Directory detail view rather than a standalone screen — reasonable UI choice, not a gap. |
| S-355 | HRMS-0516 | Reporting Manager Weekly Input Bot | Ready for Build | **NOT-DONE** | `app/models/timesheet_nag.py:4`, `app/services/timesheet_nag_service.py:3`, `app/services/performance_store_service.py:26` — all three only reference "S-355's design" as a *pattern reused for a different feature* (timesheet reminders), not the actual Friday WhatsApp manager-input bot | No WhatsApp-triggered weekly manager input flow exists. Sheet is accurate. |
| S-356 | HRMS-0517 | Employee Milestone Tracker — Personal, Project & Org | Done | **CONFIRMED-DONE** | `app/models/employee_milestone.py:36` (`EmployeeMilestone`); `app/services/employee_milestone_service.py`; `app/api/v1/endpoints/employee_milestones.py`; frontend `src/screens/EmployeeDirectoryScreen.js:404-566` (`AddMilestoneForm`, milestone list/complete UI, comment explicitly notes PERSONAL/ORG types here, PROJECT milestones live on the project itself); `tests/test_employee_milestones_api.py` (11 tests) | Full stack confirmed. |
| S-357 | HRMS-0518 | Core Eligibility AI Assessment — Agentic Bot | Ready for Build | **NOT-DONE** | `app/models/employee_milestone.py:14`, `app/models/performance_store.py:4`, `app/services/performance_store_service.py:26` — all reference HRMS-0518 only as a *future consumer* of the milestone/performance data they build ("feed the Core Eligibility AI Assessor"), not the assessor itself | No agentic weighted-scoring bot exists. Sheet is accurate. |
| S-358 | HRMS-0519 | SI Partner Engagement Tagging | Done | **CONFIRMED-DONE** | `app/models/project.py:99-100` (`si_partner` enum column); frontend `src/screens/ProjectsScreen.js:3,404,461` (field displayed, required for Speciality-engine projects, labeled "revenue attribution and RM Agent continuity scoring depend on it") | Confirmed. Note the code comment at `ProjectsScreen.js:52-53`: deliberately no separate SI-Partner picker — `client_id` IS the SI partner — a documented design choice, not a missing field. |
| S-359 | HRMS-P511 | HTD Intake Pause Engine — Conversion Rate Breach | Done | **CONFIRMED-DONE** | `app/models/htd_intake_pause.py:27,60` (`HtdIntakeStatus`, `HtdPauseLogEntry`); `app/services/htd_intake_pause_service.py:47-143` (`calculate_monthly_conversion_rate`, `check_and_apply_breach`, `resume_htd_intake`); `app/api/v1/endpoints/htd_intake_pause.py:52-118` (calculate/check-breach/status/resume/pause-log routes); frontend `src/screens/HtdIntakeScreen.js` (routed at `Approutes.jsx:489,714`); `tests/test_htd_intake_pause_api.py` (11 tests) | Full stack confirmed — 2-consecutive-month <50% conversion auto-pause, BU Head resume with documented audit trail. |

**NEW-RM tally: 5 CONFIRMED-DONE, 3 NOT-DONE (S-352, S-355, S-357), 1 unresolved — wait, recount:** S-351 Done, S-352 Not-Done, S-353 Done, S-354 Done, S-355 Not-Done, S-356 Done, S-357 Not-Done, S-358 Done, S-359 Done = **6 CONFIRMED-DONE / 3 NOT-DONE**. Sheet claimed 6/9 Done — **the count matches**, and (unlike other clusters checked this session) the specific rows also match: the same 6 the sheet marks Done are genuinely done.

## NEW-RM-EXT (15 stories) — sheet claims 4/15 Done

| Story ID | WROS ID | Summary | Sheet Status | Real Status | Evidence (file:line) | Notes |
|---|---|---|---|---|---|---|
| S-364 | HRMS-0520 | 30-Day Buddy Program — 35 KPI Framework & Tracking | Done | **CONFIRMED-DONE** | `app/models/buddy_program.py:25,45` (`BuddyProgramRecord`, `BuddyKPIScore`); `app/services/buddy_program_graduation_service.py`; `app/api/v1/endpoints/buddy_program.py`; frontend `src/screens/BuddyProgramListScreen.js`, `BuddyProgramScreen.js` (routed at `Approutes.jsx:511-512,736-737`); `tests/test_buddy_program.py` | Matches this session's own change-log note: service layer pre-existed, this pass added the REST + UI layer. Confirmed real, not a stub. |
| S-365 | HRMS-0521 | Buddy Program Graduation Gate — BU Head Approval | Done | **CONFIRMED-DONE** | Same `buddy_program_graduation_service.py`; `tests/test_buddy_graduation_gate.py` (8 tests); same API/UI as S-364 | Built on the same service as S-364. Confirmed. |
| S-366 | HRMS-0522 | Specialty Deployment 90-Day Certification Clock | Ready for Build | **NOT-DONE** | Zero hits for `S-366` or `HRMS-0522` anywhere in `app/` or `tests/` | No billable-days tracking / reset-pause clock exists. Sheet is accurate. |
| S-367 | HRMS-0523 | Escalation Classification Engine — Resource vs Environment | Ready for Build | **NOT-DONE** | Zero hits for `S-367`/`HRMS-0523`. `app/services/escalation_detection_service.py` exists but is unrelated — it's S-035/HRMS-0435 Human Escalation Detection (candidate-conversation escalation, not resource-vs-environment classification) | No such engine exists. Sheet is accurate. |
| S-368 | HRMS-0524 | Peer Trust Pulse Survey — Week 6 and Week 12 | Ready for Build | **NOT-DONE** | Zero hits for `S-368`/`HRMS-0524`/`peer_trust`/`trust_pulse` anywhere | Not built. Sheet is accurate. |
| S-369 | HRMS-0525 | Core Certification Scorecard — Full AI Assessment Output | Ready for Build | **NOT-DONE** | Zero hits for `S-369`/`HRMS-0525`/`certification_scorecard` | Depends on S-357 (also not built) and S-352 (also not built) — structurally can't exist yet. Sheet is accurate. |
| S-370 | HRMS-0526 | HTD 365-Day Specialty Certification Track | Ready for Build | **NOT-DONE** | Zero hits for `S-370`/`HRMS-0526` | Not built. Sheet is accurate. |
| S-371 | HRMS-0527 | Curtis Rule — Partner Intent ML Engine | Ready for Build | **PARTIAL** | `app/models/partner_intent.py:1-27` (`PartnerIntentProfile` model); `app/services/partner_intent_service.py:1-50` (`compute_partner_intent_profile()`) — both real and per the module's own docstring, tested | Genuinely real inference math, but self-documented as incomplete: (1) no API endpoint — not registered in `app/api/v1/routes.py`; (2) no UI; (3) function takes historical-demand data as a parameter rather than querying `Demand` directly, because `Demand` has no `delivery_engine`/`partner_user_id` columns yet — the nightly-batch data path described in the story doesn't exist. Sheet under-claims nothing (still says Ready for Build) but this is worth flagging: it's further along than a cold start, just not wired to real data or reachable by any user. |
| S-372 | HRMS-0528 | Confirmed vs Potential Demand Workflow | Done | **CONFIRMED-DONE** | `app/models/demand_confirmation.py:34` (`DemandAlignmentCall`); `app/services/demand_confirmation_service.py:40-134` (`confirm_demand_with_sow`, `schedule_alignment_call`, `confirm_fit`, `trigger_specialty_client_release`); `app/api/v1/endpoints/demand_confirmation.py:113-217` (SOW/schedule/get-calls/confirm-fit/trigger-release routes); frontend `src/screens/DemandConfirmationScreen.js:13-236` (SOW form, per-participant EMPLOYEE/BU_HEAD fit confirmation, release trigger button, routed at `Approutes.jsx:484,709`); `tests/test_demand_confirmation_api.py` (11), `tests/test_demand_confirmation_workflow.py` (17) | Another of the 4 stories the Phase-4 doc flagged as backend-only — API + UI now real and confirmed. The `trigger_specialty_client_release` action inside this story only sends a notification to the Speciality RM (correctly scoped to S-372's own requirements doc) — it is NOT the full S-378 workflow; see S-378 below. |
| S-373 | HRMS-0529 | Specialty Pool Minimum 40 Core-Certified Guard | Done | **CONFIRMED-DONE** | `app/services/core_pull_service.py:35` (`SPECIALTY_POOL_MINIMUM = 40`), `:64-116` (`check_specialty_pool_guard`, `get_specialty_pool_status`, `log_replacement_plan`, AC-6 41→40 edge alert at `:90`); `app/api/v1/endpoints/core_pull.py:100-112,199-230` (pool-status, replacement-plan routes); frontend `src/screens/CorePullScreen.js:2-3,135,214` (`PoolStatusBanner`, replacement-plan form) | Same file/feature pairing as S-353 by design (guard has no independent lifecycle) — both confirmed real, tested, and reachable in the UI. |
| S-374 | HRMS-0530 | BU Head ML Dashboard — Workforce Command Centre | Ready for Build | **NOT-DONE** | Zero hits for `S-374`/`HRMS-0530`/`workforce_command`/`command_centre`. `app/api/v1/endpoints/risk_dashboard.py:1` and `app/services/risk_dashboard_service.py` exist but are an unrelated feature (S-063/HRMS-0463 Candidate Risk Dashboard) | Not built. Sheet is accurate. |
| S-375 | HRMS-0531 | Individual Employee Scorecard — 35 KPI Live View | Ready for Build | **NOT-DONE** | Zero hits for `S-375`/`HRMS-0531`/`individual_scorecard`/`employee_scorecard` | Not built (distinct from the milestone/performance panel built for S-354/S-356, which is not a 5-tab scorecard). Sheet is accurate. |
| S-376 | HRMS-0532 | Predictive Demand ML Engine | Ready for Build | **NOT-DONE** | Zero hits for `S-376`/`HRMS-0532`/`predictive_demand` | Not built. Sheet is accurate. |
| S-377 | HRMS-0533 | Interviewer Quality Scoring — Client Pass Rate | Ready for Build | **NOT-DONE** | Zero hits for `S-377`/`HRMS-0533`/`interviewer_quality` | Not built. Sheet is accurate. |
| S-378 | HRMS-0534 | Specialty Client Release Approval Workflow | Ready for Build | **NOT-DONE** | `app/services/demand_confirmation_service.py:143` references "HRMS-0534 Specialty Client Release" but only as the name for S-372's minimal notify-the-RM trigger. Requirements doc `Requirements/S-378_HRMS-0534.md` calls for: a dedicated `specialty_client_releases` table, `client_response` tracking (APPROVED/NEGOTIATING/BLOCKED), and — critically — a hard gate blocking new `employee_allocations` creation until release is APPROVED. None of that exists. | This is the one row worth double-checking by hand: it's easy to mistake S-372's `trigger_specialty_client_release()` for S-378 because they share vocabulary. They are not the same thing — S-372 sends one notification; S-378 is a full response-tracking + allocation-blocking workflow with its own table. Sheet is accurate that this is not done. |

**NEW-RM-EXT tally: 4 CONFIRMED-DONE (S-364, S-365, S-372, S-373), 1 PARTIAL (S-371), 10 NOT-DONE.** Sheet claimed 4/15 Done — **matches**, and the same 4 rows.

## Summary

- **CONFIRMED-DONE: 10** (S-351, S-353, S-354, S-356, S-358, S-359, S-364, S-365, S-372, S-373) — all ten have real models/services, registered API routes, a reachable frontend screen wired into `Approutes.jsx`, and passing tests. Four of these (S-353, S-373, S-372, and HRMS-1105 Resource Management Agent, which isn't itself one of the 24 stories) are exactly the four the Phase-4 doc flagged as backend-only when this cluster's build round began — that gap has been closed with real API + UI, not stubs.
- **PARTIAL: 1** (S-371, Curtis Rule) — real, tested inference math exists, but has no API endpoint, no UI, and isn't wired to live `Demand` data (missing `delivery_engine`/`partner_user_id` columns on `Demand`). Backend-real-but-unreachable, one step short of even the "backend done, no UI" pattern since it's also not registered as an API.
- **NOT-DONE: 13** (S-352, S-355, S-357, S-366, S-367, S-368, S-369, S-370, S-374, S-375, S-376, S-377, S-378) — zero real implementation found beyond forward-references in comments of other stories.
- **CANT-DETERMINE: 0**

Unlike other clusters flagged this session as having a stale Status column, **this cluster's sheet Status is accurate** on both the aggregate count (6/9 and 4/15) and on which specific rows are Done — the 24-story Status column here can be trusted, with the one caveat that S-371 is further along than "Ready for Build" implies (real backend logic, just unreachable).
