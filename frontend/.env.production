# Loaded automatically by `npm run build` (CRA sets NODE_ENV=production).
# The ONLY place the real production backend URL should be configured --
# explicit and deliberate, never a silent fallback. See client.js.
REACT_APP_API_BASE_URL=http://46.224.149.7:8080
